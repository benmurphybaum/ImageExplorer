#pragma TextEncoding = "UTF-8"
#pragma rtGlobals=3		// Use modern global access method and strict wave access.
#pragma rtFunctionErrors=1

static constant kProjectID=22059
static strconstant ksShortTitle="Image Explorer" // the project short title on IgorExchange

#if exists("JSONXOP_Parse")
	#define JSONXOP
#endif

//*************************************************************************************************************************
//************************************ SUMMARY ****************************************************************************

// Image Explorer
// Ben Murphy-Baum, 2023

// Image Explorer is a data visualization tool for 3D image stacks.

// It is also capable of loading complex image stacks generated by ScanImage microscopy software. See bottom of comment for more info.
// Although only ScanImage tif files can be loaded at this time, the visualization tools will work on any 3D wave.
// For non-ScanImage 3D waves, load them into a folder structure like this:
	
//	root:Scans:ParentFolder:ChildFolder:ScanWaves
// ParentFolder might represent a day's experiments, while ChildFolder might represent a single trial. 

//*************************************************************************************************************************
//********************************** HOW TO USE ***************************************************************************

// Image Explorer can be opened from the 'Analysis' menu.
// Once open, load in your scans. You might need to click 'Refresh' to update the explorer.
// Select from 'Scan Groups' and 'Scanfields' to select which scans you want to view.
// Click 'Display' to open the viewer and explore your data. Multiple scans can be displayed at the same time.

// CONTROLS:

//TOP ROW
//     Play : Begins playback of each frame in the image stack
//       << : Slow down playback speed
//	      >> : Speed up playbac speed
//	    Step : Step through the image stack one frame at a time
//     Auto : Auto-scale the image in X and Y
// Max Proj : Displays a max projection of the image stack. Clicking Play from here will go back to the normal single frame display.
//     Live : Opens a 'Live ROI' display. Mousing over the image displays the time-varying fluorescence data (Z)
//				  averaged over the indicated region. The size of the ROI is controlled in microns as a SetVariable control.
//				  -Cumulative scaling : Scales the graph using the smallest and largest values encountered so far during Live ROI.
//						-Unchecking this auto-scales the data. 

//BOTTOM ROW
//	 Sliders : Control the black and white points of the image display. 
// 	    S : Performs an 'auto-stretch' on the image (automatically adjusts black and white points to optimal values).
//	      -1 : Inverts the image.
//	    ROIs : Opens a side panel for creating ROIs using different methods.
//Crosshair : Overlays crosshairs on the image. 
//				  White is the center of the display window.
//				  Green is (0,0) on the actual image coordinates
//	    L.A. : Lock aspect ratio. Selecting this will lock the aspect ratio to be the actual image aspect based on its X/Y scaling.
//	  Square : Displays the current contents of the image viewer into a new image graph.

//	ROIs:
// Select an ROI group from the dropdown, or click '+ Grp' to create a new group with a custom name.
//  Marquee : Select an area of the image and click + to add the ROI.
//    Click : Automatically creates an ROI of the defined size at the location of a mouse click. This is my preferred method by far.
//     Draw : After clicking 'Start', click and drag around the image to create arbitrary ROI shapes.
//				  This is slightly finicky, make sure the ROI name is not already taken.


//*************************************************************************************************************************
//********************************** LOADING SCANS ************************************************************************

// ScanImage .tif files can be loaded as normal tiffs for single channel, single scanfield, single frame per slice image stacks. 
// Anything else requires de-interleaving the data in multiple ways.   

//	There are undoubtedly some scan configurations that have not been tested for loading. This package currently supports:

//	Time-varying frame scans containing:
//			Multiple channels
//			Multiple scanfields (ROIs)
//			Multiple z positions (e.g. using fast-z functionality from an electrically tunable lens (ETL) or piezoelectric actuator lens).
//			Z-stacks with multiple frames per z position

//	Each scanfield and channel will result in its own image wave. 
//	So a two channel scan (e.g. red and green) with 2 scanfields will produce 4 image waves in the destination folder.

//	Z-stacks will produce a single image wave.

//	Time-varying scans with multiple z planes will split each z plane into a different output image wave.
//	So, if I have two channels, and two scanfields that are present in two z planes using an ETL, the loader will
// produce 8 output image waves, one for each z plane per scanfield per channel.

//	Meta-data about the scan configuration and parameters is saved as a ScanInfo string in the destination folder.


StrConstant IE = "root:Packages:ImageExplorer"
StrConstant IE_LIGHT_FONT = "Calibri Light"

Menu "Analysis"
	SubMenu "Packages"
		SubMenu "Image Explorer"
				"Open Image Explorer",/Q, IE_OpenImageExplorer()
				"Generate Sample Data",/Q, IE_GenerateSampleData()
		End
	End
End

#ifdef JSONXOP
	Function/S IE_BrowseScanImage()	
		//Opens a browser for the user to select .tif files to load
		//Returns list of selected paths to the .tif files
		
		Variable refnum
		String extension = ".tif"
		
		//Browse the folder
		String message = "Select one or more ScanImage .tif files"
		
		String fileFilters = "ScanImage Files (*.tif,*.tiff):.tif,.tiff;"
		fileFilters += "All Files:.*;"
		
		Open/D/R/F=fileFilters/MULT=1/M=message refnum
		
		//Convert to semi-colon separated list of files paths
		S_filename = ReplaceString("\r",S_filename,";")
		
		//Loads the images as waves
		IE_LoadScanImage(S_filename)
		return S_filename 
	End
#endif

Function/DF IE_CreateDestinationFolder(filePath)
	String filePath
	//INPUT: full path to the .tif file
	//Make the data folder to hold the scan waves

	filePath = ReplaceString("/",filePath,":")

	//This is the full path of the folder to be created in Igor that will hold the scan wave
	String folder = "root:Scans:" + ParseFilePath(0,filePath,":",1,1)
	
	If(!DataFolderExists("root:Scans"))
		NewDataFolder root:Scans
	EndIf
	
	//Remove any whitespaces
	folder = ReplaceString(" ",folder,"")
	
	If(!DataFolderExists(folder))
		NewDataFolder $folder
	EndIf
	
	//add the scan name folder on and make the folder
	folder += ":" + RemoveEnding(ParseFilePath(0,filePath,":",1,0),".tif")
	
	folder = ReplaceString(" ",folder,"_")
	
	If(!DataFolderExists(folder))
		NewDataFolder $folder
	EndIf
	
	SetDataFolder $folder
	
	return $folder
End

Function/WAVE IE_GetHeader(fileRef,length,offset)
	Variable fileRef,length,offset
	//INPUT: valid file reference to a .tif file
//				length of the header, from FRAME_DATA_LENGTH tag
//				offset of the header

	//Returns the ScanImage Big Tiff HEADER from the open file reference

	FStatus fileRef
	
	String path = S_path + S_fileName
	
	Variable i = 0
	
	//Set position to start of HEADER
	FSetPos fileRef,offset
	
	//Read in the binary data into the correctly sized string for the number of bytes in the header
	String/G root:str = ""
	SVAR str = root:str
	str = PadString(str,length,0)
	FBinRead/B=3/F=3 fileRef,str
	
	String param = IE_GetParamStr()
	
	Make/O/N=(ItemsInList(param,";"),2)/T/FREE header
	
	For(i=0;i<ItemsInList(param,";");i+=1)
		String theParam = StringFromList(i,param,";")
		strswitch(theParam)
			case "SI.hFastZ.enable":
			case "SI.hStackManager.enable":
				header[i][0] = RemoveListItem(0,theParam,".")
				break
			default:
				header[i][0] = ParseFilePath(0,theParam,".",1,0)
				break
		endswitch
		
		header[i][1] = StringByKey(theParam,str," = ","\n")
	EndFor
	
	//Append the file location to the header
	Variable row = DimSize(header,0)
	Redimension/N=(row + 2,2) header
	header[row][0] = "File Path"
	header[row][1] = path
	header[row+1][0] = "Stimulus Path"
	header[row+1][1] = ""
	
	//Try to append the path to potential stimulus .h5 file
	String fileList = IndexedFile(image,-1,".h5") //image is the pre-defined path to the image, as defined in the calling function
	String stimFile = RemoveEnding(S_fileName,".tif") + ".h5"
	
	If(stringmatch(fileList,"*" + stimFile + "*"))
		header[row+1][1] = S_path + stimFile
	EndIf
	
	return header
End

#ifdef JSONXOP
	Function IE_LoadScanImage(fileList)
		String fileList
		//INPUT: string list of full paths to .tif files
		//Business function. This actually loads the data from the .tif files, 
		//and parses the data into individual waves depending on the scan configuration.
	
		Variable fileRef
		
		Variable j,numFiles = ItemsInList(fileList,";")
			
		String imageList = ""
		
		For(j=0;j<numFiles;j+=1)
		
			Variable ref = StartMSTimer	
			
			//the file path
			String file = StringFromList(j,fileList,";")
			DFREF destFolder = IE_CreateDestinationFolder(file)
			
			//Parse the full path into folder and file components
			String fileName = ParseFilePath(0,file,":",1,0)
			String folderPath = ParseFilePath(1,file,":",1,0)
			
			//Open the file
			folderPath = RemoveEnding(folderPath,":") + ":" //ensures ending colon
			NewPath/O/Q image,folderPath
			
			Open/P=image/R fileRef as file;
		
				
			//Read binary data, little endian, 64 bit integer (8 byte)
			String str = ""
			Variable var
			
			Variable IFD_HEADER_OFFSET,MAGIC,VERSION,FRAME_DATA_LENGTH,ROI_DATA_LENGTH
		
			//IFD HEADER OFFSET
			FSetPos fileRef,8
			FBinRead/B=3/F=2 fileRef,IFD_HEADER_OFFSET
	
			//STATIC METADATA
			Variable pos = 16
			
			//Magic # - confirms this is a scanimage big tiff file
			FSetPos fileRef,pos
			FBinRead/B=3/F=3/U fileRef,MAGIC
			
			pos += 4 //20
			FSetPos fileRef,pos
			FBinRead/B=3/F=3/U fileRef,VERSION
			
			pos += 4 //24
			FSetPos fileRef,pos
			FBinRead/B=3/F=3/U fileRef,FRAME_DATA_LENGTH
			
			pos += 4 //28
			FSetPos fileRef,pos
			FBinRead/B=3/F=3/U fileRef,ROI_DATA_LENGTH
			
			//Non-Varying Frame Data
			pos += 4 //32
			
			//Get the Non-Varying Frame Data (HEADER)
			Wave/T header_return = IE_GetHeader(fileRef,FRAME_DATA_LENGTH,pos)
			Duplicate/O header_return,destFolder:scanInfo
			Wave header = destFolder:scanInfo
			
			//Get the ROI Group Data (HEADER)
			pos += FRAME_DATA_LENGTH
			str = ""
			String roiStr = PadString(str,ROI_DATA_LENGTH,0)
			FSetPos fileRef,pos
			FBinRead/B=3/F=3/U fileRef,roiStr
		
			//Extracts the ROI data
			Wave/WAVE roiWaveRefs = IE_GetROIData(roiStr,file,header)
	
			//Use the first ROI wave to get the number of ROIs
			Variable numROIs = DimSize(roiWaveRefs,0)
			
			Close/A
	
			//Native Igor load of the tiff. This results in a single 3D image wave that
			//will need to be de-interleaved into multiple image waves, depending on the scan configuration.
			ImageLoad/O/T=tiff/S=0/BIGT=1/C=-1/Q/LR3D file
			Wave theImage = destFolder:$StringFromList(0,S_waveNames,";")
		
			SetDataFolder destFolder
			
			//Number of frames will be a multiple of the # Z levels
			Variable frame,numFrames = DimSize(theImage,2)
			
			Variable prevZ,Z,offsetY,offsetZ,numZs
			
			//Was this a Z stack enabled scan?
			Variable stackEnable = bool2int(IE_GetSIParam("hStackManager.enable",header))
			
			//Is multiROI enabled?
			Variable mroiEnable = bool2int(IE_GetSIParam("mroiEnable",header))
					
			If(stackEnable)
				String stackMode = IE_GetSIParam("stackDefinition",header)
				
				//arbitrary Zs were used
				strswitch(stackMode)
					case "'arbitrary'":
						//get the Z levels
						String zs = ReplaceString(" ",IE_GetSIParam("zs",header),";")
						zs = ReplaceString("]",ReplaceString("[",zs,""),";")
						
						//Number of Z levels
						numZs = str2num(IE_GetSIParam("actualNumSlices",header))
						
						//Number of time steps in each volume (i.e. number of frames)
						Variable numTimeFrames = str2num(IE_GetSIParam("actualNumVolumes",header))
						
						Variable framesPerSlice = str2num(IE_GetSIParam("framesPerSlice",header))
						
						//Volume rate (i.e. frame rate)
						Variable frameRate = str2num(IE_GetSIParam("scanVolumeRate",header))
						
						break
					case "'bounded'":
					case "'uniform'":
						//get the Z levels
						zs = ReplaceString(" ",IE_GetSIParam("zs",header),";")
						zs = ReplaceString("]",ReplaceString("[",zs,""),";")
						
						//Number of Z levels
						numZs = str2num(IE_GetSIParam("actualNumSlices",header))
						
						//Number of time steps in each volume (i.e. number of frames)
						numTimeFrames = str2num(IE_GetSIParam("actualNumVolumes",header))
						
						framesPerSlice = str2num(IE_GetSIParam("framesPerSlice",header))
						
						//Volume rate (i.e. frame rate)
						frameRate = str2num(IE_GetSIParam("scanVolumeRate",header))
						break
					
					default:
			
						zs = "0"
						numZs = 1
						numTimeFrames = str2num(IE_GetSIParam("framesPerSlice",header))
						frameRate = str2num(IE_GetSIParam("scanFrameRate",header))
						break
				endswitch
			
			Else
				zs = "0"
				numZs = 1
				numTimeFrames = numFrames
				framesPerSlice = 1
				frameRate = str2num(IE_GetSIParam("scanFrameRate",header))
			EndIf
	
			//Number of ROIs determines how many waves will be created
			Variable i
			String baseName = RemoveEnding(fileName,".tif")
			
			prevZ = -1
			offsetY = -1
			offsetZ = 0
			
			//Load all the channels
			String channelList = IE_GetSIParam("channelsActive",header)
			channelList = ReplaceString(" ",channelList,";")
			channelList = ReplaceString("[",channelList,"")
			channelList = ReplaceString("]",channelList,"")
			Variable numChannels = ItemsInList(channelList,";")
			
			//Microns per degree of mirror angle
			Variable objResolution = str2num(IE_GetSIParam("objectiveResolution",header)) * 1e-6
							
			//Makes the waves for each ROI.
			//Depending on the scan configuration, de-interleaving the data will also occur within this loop.
			For(i=0;i<numROIs;i+=1)
				Wave/T theROI = roiWaveRefs[i]
				Variable xPixels = IE_GetROIVar("XPixels",theROI)
				Variable yPixels = IE_GetROIVar("YPixels",theROI)
				
				String name = IE_GetROIString("Name",theROI)
				
				If(stringmatch(name,"Default*"))
					name = ""
				EndIf
				
				If(strlen(name))
					String ROIname = baseName + "_" + name
				Else
					ROIname = baseName + "_R" + num2str(i)
				EndIf
						
				//remove whitespace in the naming
				ROIname = ReplaceString(" ",ROIname,"_")
				
				numZs = (numZs == 0) ? 1 : numZs
				
				If(framesPerSlice > 1)
					//Z stack (probably) with multiple frames per slice
					Make/N=(xPixels,yPixels,numZs,framesPerSlice)/O/W $ROIname/Wave=scanROI
				Else
					If(numROIs > 1 || numZs == 1)
						//Not a Zstack (not necessarily true, but probably true for our purposes)
						//Standard for multi ROI multi-plane imaging
						
						//Old method, create wave before filling it.
	//					Make/N=(xPixels,yPixels,(numFrames / numZs))/O/W $ROIname/Wave=scanROI
						
						//Just make this so the WaveDim call below works, not optimal but much faster than making the wave prior to duplication below.
						//Should clean this up to avoid the wave creation step entirely.
						Make/N=(1,1,1)/O/W $ROIname/Wave=scanROI
					Else
						//Z stack, single frame per slice, potentially multiple volumes taken (numTimeFrames)
						Make/N=(xPixels,yPixels,numZs,numTimeFrames)/O/W $ROIname/Wave=scanROI
					EndIf
				EndIf
								
				imageList += GetWavesDataFolder(scanROI,2) + ";"	
				
				//These are built in scan mirror offsets used to center the image. By removing these, the scaling of the image reflects the actual stage position,
				//which is a lot more useful for our analysis.
				Variable XOffset = str2num(IE_GetSIParam("scanAngleShiftFast",header))
				Variable YOffset = str2num(IE_GetSIParam("scanAngleShiftSlow",header))
				
				Variable XCenter = str2num(theROI[1]) - XOffset
				Variable YCenter = str2num(theROI[2]) - YOffset
	
				//Get the z plane of the scan ROI, and it's frame offset
				String Zstr = theROI[5]
				Z = str2num(Zstr)
				
				//Put the frames into the output waves
				offsetZ = WhichListItem(Zstr,zs,";")
				
				If(offsetZ == -1)
					offsetZ = 0
				EndIf
				
				offsetY = (Z == prevZ) ? Ypixels : 0
			
				Variable k			
				
				//First step of de-interleaving the data for the z positions and frames per slice
				//'theImage' contains all of the data, and we're pulling it out selectively into individual image waves
				//depending on the scan configuration.
				If(framesPerSlice == 1)
					If(WaveDims(scanROI) == 4)			
						Multithread scanROI[][][][] = theImage[p][offsetY + q][offsetZ + r * numZs]
					Else
						If(offsetY == 0 && numROIs == 1)
					
							Duplicate/O theImage,scanROI //much faster than indexed wave assignment
				
						Else
	
							//Using duplication followed by matrix op is much faster than the below multithreaded wave assignment 
							Variable endY = offsetY + yPixels - 1
						
							//Duplicate, including all of the Z pixels, which are interleaved still for multiple scanfield ROIs
							Duplicate/FREE/RMD=[][offsetY,offsetY + yPixels-1][] theImage,temp
							Variable endZ = DimSize(temp,2)-1
							
							//De-interleave the data
							MatrixOP/O scanROI = temp[][][offsetZ,endZ,numZs]
				
							//Old, slower method
	//						Multithread scanROI[][][] = theImage[p][offsetY + q][offsetZ + r * numZs]
	
						EndIf
					EndIf
				Else
					//Multiple frames per slice.
					//In the tiff file, frames per slice are in the 3rd dimension, we want them in the 4th dimension in our final waves
					For(k=0;k<numZs;k+=1)
						Variable startZ = k*framesPerSlice
						endZ = startZ + framesPerSlice - 1
						MatrixOP/FREE chunk = theImage[][][startZ,endZ]
						Multithread scanROI[][][k][] = chunk[p][q][s]
					EndFor
				EndIf		
				
	
				//Final step in de-interleaving the data according to channel.
				//Channels for each frame are interleaved.
				//Here, we're operating on the scanROI wave, which is an individual scanfield (ROI) with interleaved channel data.
				//The output from this loop is the final output image wave.
				Variable c
				For(c=0;c<numChannels;c+=1)
					String ch = "ch" + StringFromList(c,channelList,";")
					String scanName = NameOfWave(scanROI)
					If(numChannels > 1)
						Duplicate/O scanROI,$(scanName + "_" + ch)
						Wave scanChannel = $(scanName + "_" + ch)
						Redimension/N=(-1,-1,DimSize(scanROI,2) / numChannels) scanChannel
						scanChannel = scanROI[p][q][c + r * numChannels]
					Else
						Duplicate/O scanROI,$(scanName + "_" + ch)
					EndIf		
					
					Wave scanChannel = $(scanName + "_" + ch)
					
					//Set the scale for the final image.
					SetScale/I x,objResolution * (XCenter - (str2num(theROI[3]) / 2)),objResolution * (XCenter + (str2num(theROI[3]) / 2)),"m",scanChannel
					
					//Reverse scaling from what I originally had, so now the image is oriented the same as in MATLAB when the data is taken
		//			SetScale/I y,objResolution * (theROI[1] - (theROI[3] / 2)),objResolution * (theROI[1] + (theROI[3] / 2)),"m",scanROI
					SetScale/I y,objResolution * (YCenter - (str2num(theROI[4]) / 2)),objResolution * (YCenter + (str2num(theROI[4]) / 2)),"m",scanChannel
					
					SetScale/P z,0,str2num(theROI[12]),"s",scanChannel
							
				EndFor
		
				KillWaves/Z scanROI
				
				prevZ = Z
				offsetY += yPixels
			EndFor
					
			Variable size = str2num(StringByKey("SIZEINBYTES",WaveInfo(theImage,0))) / (1e6)
			
			KillWaves/Z theImage
			
			print "Loaded " + file + ":", StopMSTimer(ref) / (1e6),"s (" + num2str(size) + " MB)"
			
		EndFor
	
	End
#endif	

//Colon separated string of all the parameter fields in the header
Function/S IE_GetParamStr()
	String param = ""
	
	param += "SI.acqState;"
	param += "SI.acqsPerLoop;"
	param += "SI.loopAcqInterval;"
	param += "SI.objectiveResolution;"
	param += "SI.hBeams.powers;"
	param += "SI.hChannels.channelsActive;"
	param += "SI.hFastZ.enable;"
	param += "SI.hFastZ.waveformType;"
	param += "SI.hRoiManager.linePeriod;"
	param += "SI.hRoiManager.linesPerFrame;"
	param += "SI.hRoiManager.mroiEnable;"
	param += "SI.hRoiManager.pixelsPerLine;"
	param += "SI.hRoiManager.scanAngleShiftFast;"
	param += "SI.hRoiManager.scanAngleShiftSlow;"
	param += "SI.hRoiManager.scanFramePeriod;"
	param += "SI.hRoiManager.scanFrameRate;"
	param += "SI.hRoiManager.scanRotation;"
	param += "SI.hRoiManager.scanType;"
	param += "SI.hRoiManager.scanVolumeRate;"
	param += "SI.hRoiManager.scanZoomFactor;"
	param += "SI.hScan2D.bidirectional;"
	param += "SI.hScan2D.fillFractionSpatial;"
	param += "SI.hScan2D.fillFractionTemporal;"
	param += "SI.hScan2D.fovCornerPoints;"
	param += "SI.hScan2D.scanPixelTimeMaxMinRatio;"
	param += "SI.hScan2D.scanPixelTimeMean;"
	param += "SI.hStackManager.actualNumSlices;"
	param += "SI.hStackManager.actualNumVolumes;"
	param += "SI.hStackManager.actualStackZStepSize;"
	param += "SI.hStackManager.arbitraryZs;"
	param += "SI.hStackManager.enable;"
	param += "SI.hStackManager.framesPerSlice;"
	param += "SI.hStackManager.numFramesPerVolume;"
	param += "SI.hStackManager.numSlices;"
	param += "SI.hStackManager.numVolumes;"
	param += "SI.hStackManager.stackDefinition;"
	param += "SI.hStackManager.stackFastWaveformType;"
	param += "SI.hStackManager.stackMode;"
	param += "SI.hStackManager.stackZEndPos;"
	param += "SI.hStackManager.stackZStartPos;"
	param += "SI.hStackManager.stackZStepSize;"
	param += "SI.hStackManager.zs;"
	param += "SI.hStackManager.zsRelative;"
	
	return param
End

Function/S IE_GetSIParamStr()
	String param = ""
	
	param += "SI.LINE_FORMAT_VERSION;"
	param += "SI.TIFF_FORMAT_VERSION;"
	param += "SI.VERSION_COMMIT;"
	param += "SI.VERSION_MAJOR;"
	param += "SI.VERSION_MINOR;"
	param += "SI.acqState;"
	param += "SI.acqsPerLoop;"
	param += "SI.extTrigEnable;"
	param += "SI.imagingSystem;"
	param += "SI.loopAcqInterval;"
	param += "SI.objectiveResolution;"
	
	
	param += "SI.hStackManager.framesPerSlice;"
	param += "SI.hStackManager.numSlices;"
	param += "SI.hStackManager.shutterCloseMinZStepSize;"
	param += "SI.hStackManager.slowStackWithFastZ;"
	param += "SI.hStackManager.stackReturnHome;"
	param += "SI.hStackManager.stackSlicesDone;"
	param += "SI.hStackManager.stackStartCentered;"
	param += "SI.hStackManager.stackZEndPos;"
	param += "SI.hStackManager.stackZStartPos;"
	param += "SI.hStackManager.stackZStepSize;"
	param += "SI.hStackManager.stageDependentZs;"
	param += "SI.hStackManager.stepSizeLock;"
	param += "SI.hStackManager.zPowerReference;"
	param += "SI.hStackManager.zs;"
	
	param += "SI.hUserFunctions.userFunctionsCfg;"
	param += "SI.hUserFunctions.userFunctionsUsr;"	
	
	
	param += "SI.hWSConnector.communicationTimeout;"
	param += "SI.hWSConnector.enable;"
	
	
	param += "SI.hWaveformManager.optimizedScanners;"
	
	param += "SI.hScan2D.beamClockDelay;"
	param += "SI.hScan2D.beamClockExtend;"
	param += "SI.hScan2D.bidirectional;"
	param += "SI.hScan2D.channelOffsets;"
	param += "SI.hScan2D.channels;"
	param += "SI.hScan2D.channelsAdcResolution;"
	param += "SI.hScan2D.channelsAutoReadOffsets;"
	param += "SI.hScan2D.channelsAvailable;"
	param += "SI.hScan2D.channelsDataType;"
	param += "SI.hScan2D.channelsFilter;"
	param += "SI.hScan2D.channelSInputRanges;"
	param += "SI.hScan2D.channelsSubtractOffsets;"
	param += "SI.hScan2D.fillFractionSpatial;"
	param += "SI.hScan2D.fillFractionTemporal;"
	param += "SI.hScan2D.flybackTimePerFrame;"
	param += "SI.hScan2D.flytoTimePerScanfield;"
	param += "SI.hScan2D.fovCornerPoints;"
	param += "SI.hScan2D.hasResonantMirror;"
	param += "SI.hScan2D.hasXGalvo;"
	param += "SI.hScan2D.keepResonantScannerOn;"
	param += "SI.hScan2D.linePhase;"
	param += "SI.hScan2D.linePhaseMode;"
	param += "SI.hScan2D.logAverageFactor;"
	param += "SI.hScan2D.logFramesPerFile;"
	param += "SI.hScan2D.logFramesPerFileLock;"
	param += "SI.hScan2D.logOverwriteWarn;"
	param += "SI.hScan2D.mask;"
	param += "SI.hScan2D.maskDisableAveraging;"
	param += "SI.hScan2D.maxSampleRate;"
	param += "SI.hScan2D.name;"
	param += "SI.hScan2D.nominalFovCornerPoints;"
	param += "SI.hScan2D.pixelBinFactor;"
	param += "SI.hScan2D.sampleRate;"
	param += "SI.hScan2D.scanMode;"
	param += "SI.hScan2D.scanPixelTimeMaxMinRatio;"
	param += "SI.hScan2D.scanPixelTimeMean;"
	param += "SI.hScan2D.scannerFrequency;"
	param += "SI.hScan2D.scannerToRefTransform;"
	param += "SI.hScan2D.scannerType;"
	param += "SI.hScan2D.settleTimeFraction;"
	param += "SI.hScan2D.SImulated;"
	param += "SI.hScan2D.stripingEnable;"
	param += "SI.hScan2D.trigAcqEdge;"
	param += "SI.hScan2D.trigAcqInTerm;"
	param += "SI.hScan2D.trigNextEdge;"
	param += "SI.hScan2D.trigNextInTerm;"
	param += "SI.hScan2D.trigNextStopEnable;"
	param += "SI.hScan2D.trigStopEdge;"
	param += "SI.hScan2D.trigStopInTerm;"
	param += "SI.hScan2D.uniformSampling;"
	param += "SI.hScan2D.useNonlinearResonantFov2VoltsCurve;"
	
	
	param += "SI.hRoiManager.forceSquarePixelation;"
	param += "SI.hRoiManager.forceSquarePixels;"
	param += "SI.hRoiManager.imagingFovDeg;"
	param += "SI.hRoiManager.imagingFovUm;"
	param += "SI.hRoiManager.linePeriod;"
	param += "SI.hRoiManager.linesPerFrame;"
	param += "SI.hRoiManager.mroiEnable;"
	param += "SI.hRoiManager.pixelsPerLine;"
	param += "SI.hRoiManager.scanAngleMultiplierFast;"
	param += "SI.hRoiManager.scanAngleMultiplierSlow;"
	param += "SI.hRoiManager.scanAngleShiftFast;"
	param += "SI.hRoiManager.scanAngleShiftSlow;"
	param += "SI.hRoiManager.scanFramePeriod;"
	param += "SI.hRoiManager.scanFrameRate;"
	param += "SI.hRoiManager.scanRotation;"
	param += "SI.hRoiManager.scanType;"
	param += "SI.hRoiManager.scanVolumeRate;"
	param += "SI.hRoiManager.scanZoomFactor;"
	
	
	param += "SI.hPmts.autoPower;"
	param += "SI.hPmts.bandwidths;"
	param += "SI.hPmts.gains;"
	param += "SI.hPmts.names;"
	param += "SI.hPmts.offsets;"
	param += "SI.hPmts.powersOn;"
	param += "SI.hPmts.tripped;"
	
	
	param += "SI.hPhotostim.allowMultipleOutputs;"
	param += "SI.hPhotostim.autoTriggerPeriod;"
	param += "SI.hPhotostim.compensateMotionEnabled;"
	param += "SI.hPhotostim.completedSequences;"
	param += "SI.hPhotostim.laserActiveSIgnalAdvance;"
	param += "SI.hPhotostim.lastMotion;"
	param += "SI.hPhotostim.logging;"
	param += "SI.hPhotostim.monitoring;"
	param += "SI.hPhotostim.monitoringSampleRate;"
	param += "SI.hPhotostim.nextStimulus;"
	param += "SI.hPhotostim.numOutputs;"
	param += "SI.hPhotostim.numSequences;"
	param += "SI.hPhotostim.sequencePoSItion;"
	param += "SI.hPhotostim.sequenceSelectedStimuli;"
	param += "SI.hPhotostim.status;"
	param += "SI.hPhotostim.stimImmediately;"
	param += "SI.hPhotostim.stimSelectionAsSIgnment;"
	param += "SI.hPhotostim.stimSelectionDevice;"
	param += "SI.hPhotostim.stimSelectionTerms;"
	param += "SI.hPhotostim.stimSelectionTriggerTerm;"
	param += "SI.hPhotostim.stimTriggerTerm;"
	param += "SI.hPhotostim.stimulusMode;"
	param += "SI.hPhotostim.syncTriggerTerm;"
	param += "SI.hPhotostim.zMode;"
	
	
	param += "SI.hMotors.azimuth;"
	param += "SI.hMotors.backlashCompensation;"
	param += "SI.hMotors.dimNonblockingMoveInProgress;"
	param += "SI.hMotors.elevation;"
	param += "SI.hMotors.motorFastMotionThreshold;"
	param += "SI.hMotors.motorPoSItion;"
	param += "SI.hMotors.motorPoSItionTarget;"
	param += "SI.hMotors.motorStepLimit;"
	param += "SI.hMotors.motorToRefTransform;"
	param += "SI.hMotors.motorToRefTransformAbsolute;"
	param += "SI.hMotors.motorToRefTransformValid;"
	param += "SI.hMotors.nonblockingMoveInProgress;"
	param += "SI.hMotors.scanimageToMotorTF;"
	param += "SI.hMotors.userDefinedPoSItions;"
	
	
	param += "SI.hMotionManager.correctionBoundsXY;"
	param += "SI.hMotionManager.correctionBoundsZ;"
	param += "SI.hMotionManager.correctionDeviceXY;"
	param += "SI.hMotionManager.correctionDeviceZ;"
	param += "SI.hMotionManager.correctionEnableXY;"
	param += "SI.hMotionManager.correctionEnableZ;"
	param += "SI.hMotionManager.correctorClassName;"
	param += "SI.hMotionManager.enable;"
	param += "SI.hMotionManager.estimatorClassName;"
	param += "SI.hMotionManager.motionHistoryLength;"
	param += "SI.hMotionManager.motionMarkersXY;"
	param += "SI.hMotionManager.resetCorrectionAfterAcq;"
	param += "SI.hMotionManager.zStackAlignmentFcn;"
	
	
	param += "SI.hIntegrationRoiManager.enable;"
	param += "SI.hIntegrationRoiManager.enableDisplay;"
	param += "SI.hIntegrationRoiManager.integrationHistoryLength;"
	param += "SI.hIntegrationRoiManager.postProcessFcn;"
	
	
	param += "SI.hFastZ.actuatorLag;"
	param += "SI.hFastZ.discardFlybackFrames;"
	param += "SI.hFastZ.enable;"
	param += "SI.hFastZ.enableFieldCurveCorr;"
	param += "SI.hFastZ.flybackTime;"
	param += "SI.hFastZ.hasFastZ;"
	param += "SI.hFastZ.nonblockingMoveInProgress;"
	param += "SI.hFastZ.numDiscardFlybackFrames;"
	param += "SI.hFastZ.numFramesPerVolume;"
	param += "SI.hFastZ.numVolumes;"
	param += "SI.hFastZ.poSItionAbsolute;"
	param += "SI.hFastZ.poSItionAbsoluteRaw;"
	param += "SI.hFastZ.poSItionTarget;"
	param += "SI.hFastZ.poSItionTargetRaw;"
	param += "SI.hFastZ.useArbitraryZs;"
	param += "SI.hFastZ.userZs;"
	param += "SI.hFastZ.volumePeriodAdjustment;"
	param += "SI.hFastZ.waveformType;"
	
	
	param += "SI.hDisplay.autoScaleSaturationFraction;"
	param += "SI.hDisplay.channelsMergeEnable;"
	param += "SI.hDisplay.channelsMergeFocusOnly;"
	param += "SI.hDisplay.displayRollingAverageFactor;"
	param += "SI.hDisplay.displayRollingAverageFactorLock;"
	param += "SI.hDisplay.enableScanfieldDisplays;"
	param += "SI.hDisplay.lineScanHistoryLength;"
	param += "SI.hDisplay.renderer;"
	param += "SI.hDisplay.scanfieldDisplayColumns;"
	param += "SI.hDisplay.scanfieldDisplayRows;"
	param += "SI.hDisplay.scanfieldDisplayTilingMode;"
	param += "SI.hDisplay.selectedZs;"
	param += "SI.hDisplay.showScanfieldDisplayNames;"
	param += "SI.hDisplay.volumeDisplayStyle;"
	
	param += "SI.hDisplay.scanfieldDisplays.enable;"
	param += "SI.hDisplay.scanfieldDisplays.name;"
	param += "SI.hDisplay.scanfieldDisplays.channel;"
	param += "SI.hDisplay.scanfieldDisplays.roi;"
	param += "SI.hDisplay.scanfieldDisplays.z;"
	
	
	param += "SI.hCycleManager.cycleIterIdxTotal;"
	param += "SI.hCycleManager.cyclesCompleted;"
	param += "SI.hCycleManager.enabled;"
	param += "SI.hCycleManager.itersCompleted;"
	param += "SI.hCycleManager.totalCycles;"
	
	
	param += "SI.hChannels.channelAdcResolution;"
	param += "SI.hChannels.channelDisplay;"
	param += "SI.hChannels.channelInputRange;"
	param += "SI.hChannels.channelLUT;"
	param += "SI.hChannels.channelMergeColor;"
	param += "SI.hChannels.channelName;"
	param += "SI.hChannels.channelOffset;"
	param += "SI.hChannels.channelSave;"
	param += "SI.hChannels.channelSubtractOffset;"
	param += "SI.hChannels.channelType;"
	param += "SI.hChannels.channelsActive;"
	param += "SI.hChannels.channelsAvailable;"
	param += "SI.hChannels.loggingEnable;"
	
	param += "SI.hConfigurationSaver.cfgFilename;"
	param += "SI.hConfigurationSaver.usrFilename;"
	
	
	param += "SI.hBeams.beamCalibratedStatus;"
	param += "SI.hBeams.directMode;"
	param += "SI.hBeams.enablePowerBox;"
	param += "SI.hBeams.flybackBlanking;"
	param += "SI.hBeams.interlaceDecimation;"
	param += "SI.hBeams.interlaceOffset;"
	param += "SI.hBeams.lengthConstants;"
	param += "SI.hBeams.powerLimits;"
	param += "SI.hBeams.powers;"
	param += "SI.hBeams.pzAdjust;"
	param += "SI.hBeams.pzCustom;"
	param += "SI.hBeams.stackEndPower;"
	param += "SI.hBeams.stackStartPower;"
	param += "SI.hBeams.stackUseStartPower;"
	param += "SI.hBeams.stackUserOverrideLz;"
	param += "SI.hBeams.powers;"
	param += "SI.hBeams.powerBoxEndFrame;"
	param += "SI.hBeams.powerBoxStartFrame;"
	
	
	param += "SI.hBeams.powerBoxes.name;"
	param += "SI.hBeams.powerBoxes.oddLines;"
	param += "SI.hBeams.powerBoxes.evenLines;"
	param += "SI.hBeams.powerBoxes.rect;"
	
	return param
End

//Returns integer 1 or 0 for true or false input string
Static Function bool2int(String bool)
	If(!cmpstr(bool,"true"))
		return 1
	ElseIf(!cmpstr(bool,"false"))
		return 0
	Else
		//bad input
		return -1
	EndIf
End

Function/S IE_GetSIParam(param,header)
	String param
	Wave/T header
	String value = ""
	
	value = header[tableMatch(param,header)][1]
	return value
End

Function IE_GetROIVar(param,header)
	String param
	Wave/T header //ROI data wave
	
	Variable index = FindDimLabel(header,0,param)
	If(index == -2)
		return index
	EndIf
	
	return str2num(header[index])
End

Function/S IE_GetROIString(param,header)
	String param
	Wave/T header //ROI data wave
	
	Variable index = FindDimLabel(header,0,param)
	If(index == -2)
		return ""
	EndIf
	
	return header[index]
End

#ifdef JSONXOP
	Function/WAVE IE_GetROIData(roiStr,file,header)
		String roiStr,file
		Wave/T/Z header
		
		//Extracts the position,rotation, z plane, etc. data for all ROIs, puts into ROI output wave
		
		Variable imagingROIpos,photoStimROIpos,integrationROIpos
		imagingROIpos = strsearch(roiStr,"imagingRoiGroup",0)
		photoStimROIpos = strsearch(roiStr,"photostimRoiGroups",0)
		integrationROIpos = strsearch(roiStr,"integrationRoiGroup",0)
		
		JSONXOP_Parse roiStr
		Variable jsonID = V_Value
		
		//Type of JSON object (array or object, depending on if there are >1 ROI)
		JSONXOP_GetType jsonID, "RoiGroups/imagingRoiGroup/rois"
		
		//How many ROIs are we extracting?
		If(V_Value)
			JSONXOP_GetArraySize jsonID,"RoiGroups/imagingRoiGroup/rois"
			Variable numROI = V_Value
			String roiPath = "RoiGroups/imagingRoiGroup/rois/"
		Else
			numROI = 1
			roiPath = "RoiGroups/imagingRoiGroup/rois"
		EndIf
		
		Variable i,j,k,count = 0
		
		
		//Wave reference wave that will hold the ROI data waves
		Make/FREE/WAVE/N=(0) roiWaveRefs
		
		For(i=0;i<numROI;i+=1)
		
			If(numROI > 1)
				String roiFullPath = roiPath + num2str(i)
			Else
				roiFullPath = roiPath
			EndIf
				
			JSONXOP_GetKeys jsonID,roiFullPath,keys
			
			//check for more than one scanfield in the ROI
			JSONXOP_GetType jsonID,roiFullPath + "/scanfields"
			
			If(V_Value)
				JSONXOP_GetArraySize jsonID,roiFullPath + "/scanfields"
				Variable numScanFields = V_Value
			Else
				numScanFields = 1
			EndIf
			
			For(j=0;j<numScanFields;j+=1)
				Redimension/N=(DimSize(roiWaveRefs,0) + 1) roiWaveRefs
				
				//Get the ROI name
				If(numScanFields > 1)
					//take name from the scanfields if the ROI has multiple defined
					JSONXOP_GetValue/T jsonID,roiFullPath + "/scanfields/" + num2str(j) + "/name"
					String name = S_Value
				Else
					JSONXOP_GetValue/T jsonID,roiFullPath + "/name"
					name = S_Value
				EndIf	
				
				//Will hold the ROI data
				If(strlen(name))
					String roiName = "ROI_" + name
					roiName = ReplaceString(" ",roiName,"")
				Else
					roiName = "ROI_" + num2str(count)
				EndIf
				
				If(stringmatch(name,"Default*"))
					roiName = "ROI_" + num2str(count)
				EndIf
				
				KillWaves/Z $(":" + roiName)
				Make/O/N=20/T $(":" + roiName)/Wave=ROI
				
				ROI[0] = name 
			
				If(numScanFields > 1)
					String suffix = "/" + num2str(j)
				Else
					suffix = ""
				EndIf
						
				JSONXOP_GetKeys jsonID,roiFullPath + "/scanfields" + suffix,scanKeys
				
				//Get Z plane of each Scanfield in the ROI
				JSONXOP_GetValue/V jsonID,roiFullPath + "/zs" + suffix
				ROI[5] = num2str(V_Value)
				
				JSONXOP_GetValue/V jsonID,roiFullPath + "/discretePlaneMode"
				ROI[17] = num2str(V_Value)
				
				//Single plane ROIs, or volumetric ROIs
			
			
				For(k=0;k<DimSize(scanKeys,0);k+=1)
					strswitch(scanKeys[k])
						case "centerXY": //Rows 0-1 in the ROI wave
							Wave wv = JSON_GetWave(jsonID,roiFullPath + "/scanfields" + suffix + "/" + scanKeys[k])
							ROI[1,2] = num2str(wv[p-1])
							break
						case "sizeXY":	//Rows 2-3 in the ROI wave
							Wave wv = JSON_GetWave(jsonID,roiFullPath + "/scanfields" + suffix + "/" +scanKeys[k])
							ROI[3,4] = num2str(wv[p-3])
							break
						case "rotationDegrees": //Row 5 in the ROI wave
							JSONXOP_GetValue/V jsonID,roiFullPath + "/scanfields" + suffix + "/" + scanKeys[k]
							ROI[6] = num2str(V_Value)
							break
						case "pixelResolutionXY": //Rows 6-7 in the ROI wave
							Wave wv = JSON_GetWave(jsonID,roiFullPath + "/scanfields" + suffix + "/" + scanKeys[k])
							ROI[7,8] = num2str(wv[p-7])
							break
						case "enable": //Row 13 in the ROI wave
							JSONXOP_GetValue/V jsonID,roiFullPath + "/scanfields" + suffix + "/" + scanKeys[k]
							ROI[14] = num2str(V_Value)
							break					
					endswitch
				EndFor
				
				
				ROI[10] = num2str(str2num(ROI[3]) / str2num(ROI[7])) //microns per pixel X
				ROI[11] = num2str(str2num(ROI[4]) / str2num(ROI[8]))  //microns per pixel Y
				
				
				Variable index = tableMatch("framesPerSlice",header)
				If(index != -1)
					ROI[9] = header[index][1] //number of frames per slice
				EndIf
				
				index = tableMatch("numSlices",header)
				If(index != -1)
					ROI[9] = num2str( str2num(ROI[9]) * str2num(header[index][1]) )  //number of frames total
				EndIf
				
				index = tableMatch("scanVolumeRate",header)
				If(index != -1)
					ROI[13] = header[index][1] //volume rate
					ROI[12] = num2str(1 / str2num(ROI[13])) //seconds per volume
				EndIf
				
				ROI[15] = num2str(numROI	)  //number of ROIs in the scan
				
				ROI[16] = num2str(numScanFields) //number of scanfields in the ROI
				
				//Get the slow and fast mirror offsets
				ROI[18] = IE_GetSIParam("scanAngleShiftFast",header)
				ROI[19] = IE_GetSIParam("scanAngleShiftSlow",header)
				
				//Label the ROI waves
				SetDimLabel 0,0,Name,ROI
				SetDimLabel 0,1,CenterX,ROI
				SetDimLabel 0,2,CenterY,ROI
				SetDimLabel 0,3,SizeX,ROI
				SetDimLabel 0,4,SizeY,ROI
				SetDimLabel 0,5,Z,ROI
				SetDimLabel 0,6,Rotation,ROI
				SetDimLabel 0,7,XPixels,ROI
				SetDimLabel 0,8,YPixels,ROI
				SetDimLabel 0,9,Slices,ROI
				SetDimLabel 0,10,XScale,ROI
				SetDimLabel 0,11,YScale,ROI
				SetDimLabel 0,12,TimeScale,ROI
				SetDimLabel 0,13,FrameRate,ROI
				SetDimLabel 0,14,Enable,ROI
				SetDimLabel 0,15,ROIs,ROI
				SetDimLabel 0,16,ScanFields,ROI
				SetDimLabel 0,17,DiscretePlane,ROI
				SetDimLabel 0,18,ScanAngleShiftFast,ROI
				SetDimLabel 0,19,ScanAngleShiftSlow,ROI
				
				roiWaveRefs[count] = ROI
				
				count += 1
			EndFor	
			
		EndFor
		
		//cleanup
		KillWaves/Z wv,scanKeys,keys
				
		JSONXOP_Release jsonID
		
		//return wave reference wave holding all ROI data
		return roiWaveRefs
	End
#endif


//Same as table match, but returns a string list of all the matched indexes, not just the first encountered
static Function/S filterTable(str,tableWave,[startp,endp,returnCol])
	String str
	Wave/T tableWave
	Variable startp,endp,returnCol//for range
	Variable i,j,size = DimSize(tableWave,0)
	Variable cols = DimSize(tableWave,1)
	String indexList = ""
	
	If(cols == 0)
		cols = 1
	EndIf
	
	If(ParamIsDefault(startp))
		startp = 0
	EndIf
	
	If(ParamIsDefault(endp))
		endp = size - 1
	EndIf
	
	If(ParamIsDefault(returnCol))
		returnCol = 0
	EndIf
	
	If(startp > DimSize(tableWave,0) - 1)
		return ""
	EndIf
	
	If(endp < DimSize(tableWave,0) - 1)
		return ""
	EndIf
	
	For(j=0;j<cols;j+=1)
		For(i=startp;i<endp+1;i+=1)
			If(stringmatch(tableWave[i][j],str))
				If(returnCol)
					indexList += num2str(j) + ";"
				Else
					indexList += num2str(i) + ";"
				EndIf
			EndIf
		EndFor
	EndFor
	
	return indexList
End

//If str matches an entry in the tableWave, returns the row, otherwise return -1
Static Function tableMatch(str,tableWave,[startp,endp,whichCol,returnCol,startFrom])
	String str
	Wave/T tableWave
	Variable startp,endp,whichCol,returnCol//for range
	Variable startFrom //start from beginning is default (0) or start from end is 1
	Variable i,j,size = DimSize(tableWave,0)
	Variable cols = DimSize(tableWave,1)
	
	If(cols == 0)
		cols = 1
	EndIf
	
	If(ParamIsDefault(startp))
		startp = 0
	EndIf
	
	If(ParamIsDefault(endp))
		endp = size - 1
	EndIf
	
	If(ParamIsDefault(returnCol))
		returnCol = 0
	EndIf
	
	If(ParamIsDefault(whichCol))
		whichCol = 0
	EndIf
	
	If(ParamIsDefault(startFrom))
		startFrom = 0
	EndIf
	
	If(startp > DimSize(tableWave,0) - 1)
		return -1
	EndIf
	
	If(endp < DimSize(tableWave,0) - 1)
		return -1
	EndIf
	
	If(!ParamIsDefault(whichCol))
		If(startFrom)
			//Backwards
			For(i=endp;i>startp-1;i-=1)
				If(stringmatch(tableWave[i][whichCol][0],str))
					return i
				EndIf
			EndFor
		Else
			//Forwards 
			For(i=startp;i<endp+1;i+=1)
				If(stringmatch(tableWave[i][whichCol][0],str))
					return i
				EndIf
			EndFor
		EndIf
				
		return -1
	EndIf
	
	For(j=0;j<cols;j+=1)
		If(startFrom)
			//Backwards
			For(i=endp;i>startp-1;i-=1)
				If(stringmatch(tableWave[i][j][0],str))
					If(returnCol)
						return j
					Else
						return i
					EndIf
				EndIf
			EndFor
		Else
			//Forwards
			For(i=startp;i<endp+1;i+=1)
				If(stringmatch(tableWave[i][j][0],str))
					If(returnCol)
						return j
					Else
						return i
					EndIf
				EndIf
			EndFor
		EndIf
	EndFor
	
	return -1
End

//***************************************************************************************************
//*********************************** IMAGE BROWSER *************************************************

//Create the controls for the ScanImage package
Function IE_CreatePackage()
	
	If(!DataFolderExists("root:Packages:"))
		NewDataFolder root:Packages
	EndIf
	
	If(!DataFolderExists("root:Packages:ImageExplorer"))
		NewDataFolder root:Packages:ImageExplorer
	EndIf
	
	//Make the ScanImage ROI folder
	If(!DataFolderExists("root:Packages:ImageExplorer:ROIs"))
		NewDataFolder root:Packages:ImageExplorer:ROIs
	EndIf
	
	DFREF NTSI = $IE
	
	//Housekeeping strings and variables
	Variable/G NTSI:numImages
	NVAR numImages = NTSI:numImages
	numImages = 0
	
	//reports which layer is being shown in the display panel
	Variable/G NTSI:imagePlane
	NVAR imagePlane = NTSI:imagePlane
	imagePlane = 0
	
	//switch for if we're playing frames in the image or not
	Variable/G NTSI:isPlaying
	NVAR isPlaying = NTSI:isPlaying
	isPlaying = 0
	
	//switch for whether a max projection is being shown or not
	Variable/G NTSI:isMaxProj
	NVAR isMaxProj = NTSI:isMaxProj
	isMaxProj = 0
	
	//number of ticks between background function calls
	Variable/G NTSI:numTicks
	NVAR numTicks =NTSI:numTicks
	numTicks = 1
	
	//number of frames to jump between for each tick refresh when playing frames
	Variable/G NTSI:deltaPlane
	NVAR deltaPlane = NTSI:deltaPlane
	deltaPlane = 1
	
	//Diameter of the dynamic ROI integration area
	Variable/G NTSI:dynamicROI_Size
	NVAR dynamicROI_Size = NTSI:dynamicROI_Size
	dynamicROI_Size = 10
	
	//Is the dynamic ROI scaling auto or cumulative?
	Variable/G NTSI:scaleCumulative
	NVAR scaleCumulative = NTSI:scaleCumulative
	scaleCumulative = 0
	
	String/G NTSI:dynamicROI_Image
	SVAR dynamicROI_Image =  NTSI:dynamicROI_Image
	dynamicROI_Image = ""

	Variable/G NTSI:lockAspect
	NVAR lockAspect = NTSI:lockAspect
	lockAspect = 1
	
	Variable/G NTSI:isInverted
	NVAR isInverted = NTSI:isInverted 
	isInverted = 0
	
	Variable/G NTSI:displayROIMasks
	NVAR displayROIMasks = NTSI:displayROIMasks
	displayROIMasks = 0
	
	Variable/G NTSI:isDynamicROI
	NVAR isDynamicROI = NTSI:isDynamicROI
	isDynamicROI = 0
	
	//Marquee ROI type indicator
	String/G NTSI:ROIType
	SVAR ROIType = NTSI:ROIType
	ROIType = ""
	
	//Hook time out. This works around a possible bug where the resize hook is triggered multiple times
	//when the Image Browser panel is first opened.
	Variable/G NTSI:resizeTimeOut
	
	Make/O/N=5 NTSI:resizeListBoxPositions/Wave = resizeListBoxPositions
	
	Variable/G NTSI:columnDrag
	NVAR columnDrag = NTSI:columnDrag
	columnDrag = 0
	
	//list and selection waves for the ROI list box
	//second layer will record the ROI Group assigned to each ROI...
	//...which is only defined by the folder the ROI lives in
	Make/O/T/N=(0,1,2) NTSI:ROIListWave /Wave = ROIListWave
	Make/O/N=0 NTSI:ROISelWave /Wave = ROISelWave
	
	//list and selection waves for the ROI Group list box
	Make/O/T/N=0 NTSI:ROIGroupListWave /Wave = ROIGroupListWave
	Make/O/N=0 NTSI:ROIGroupSelWave /Wave = ROIGroupSelWave
	
	//ROI groups, select first group
	Wave/T listWave = IE_GetROIGroups() 
	Redimension/N=(DimSize(listWave,0)) ROIGroupListWave,ROIGroupSelWave
	ROIGroupListWave = listWave
	If(DimSize(ROIGroupSelWave,0) > 0)
		ROIGroupSelWave = 0
		ROIGroupSelWave[0] = 1
		String group = ROIGroupListWave[0]
	Else
		group = ""
	EndIf
	
	Variable/G NTSI:ROI_Engaged
	NVAR ROI_Engaged = NTSI:ROI_Engaged
	ROI_Engaged = 0
	
	Variable/G NTSI:drawROI_Engaged
	NVAR drawROI_Engaged = NTSI:drawROI_Engaged
	drawROI_Engaged = 0
	
	Variable/G NTSI:mouseStartX,NTSI:mouseStartY
	
	Make/O/N=0 NTSI:drawROIX,NTSI:drawROIY
	
	Make/O/WAVE/N=2 NTSI:clickedROIRef
	
	Variable/G NTSI:Nudge_Engaged
	NVAR Nudge_Engaged = NTSI:Nudge_Engaged
	Nudge_Engaged = 0
	
	Variable/G NTSI:ROI_Width
	NVAR ROI_Width = NTSI:ROI_Width
	ROI_Width = 10
	Variable/G NTSI:ROI_Height
	NVAR ROI_Height = NTSI:ROI_Height
	ROI_Height = 10
	
	Variable/G NTSI:ROI_PctThreshold
	NVAR ROI_PctThreshold = NTSI:ROI_PctThreshold
	ROI_PctThreshold = 50
	
	//ROIs within the selected group
	Wave/T listWave = IE_GetROIs(group)
	Redimension/N=(DimSize(listWave,0),-1,-1) ROIListWave,ROISelWave
	ROIListWave = listWave
	
	//Mouse drag variable
	Variable/G NTSI:mouseDrag
	NVAR mouseDrag = NTSI:mouseDrag
	mouseDrag = 0
	
	String/G NTSI:SIDisplay_ImagePaths
	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
	SIDisplay_ImagePaths = ""
	
	//Create custom color table wave for ROI masks
	Make/O/N=(10,4) NTSI:ROI_ColorTable
	Wave color = NTSI:ROI_ColorTable
	
	//Set all to cyan transparency 25% initially
	color[0][] = 0
	color[1,*][0] = 0
	color[1,*][1] = 43690
	color[1,*][2] = 65535
	color[1,*][3] = 16384
	
	If(!DataFolderExists("root:Packages:ImageExplorer:CustomColors"))
		NewDataFolder root:Packages:ImageExplorer:CustomColors
	EndIf

	String whichColor = "Geo" //more pleasant color contrast than Rainbow or Spectrum
	Wave/Z colorTab = root:Packages:ImageExplorer:CustomColors:$whichColor
	If(!WaveExists(colorTab))
		SetDataFolder root:Packages:ImageExplorer:CustomColors
		ColorTab2Wave $whichColor
		Duplicate/O root:Packages:ImageExplorer:CustomColors:M_colors, root:Packages:ImageExplorer:CustomColors:$whichColor
		Wave colorTab = root:Packages:ImageExplorer:CustomColors:$whichColor
		KillWaves/Z root:Packages:ImageExplorer:CustomColors:M_colors
	EndIf

	//Records double click events
	Variable/G NTSI:doubleClick
	NVAR doubleClick = NTSI:doubleClick
	doubleClick = 0
	
	//Saves the name of an edited ROI Group temporarily
	String/G NTSI:oldROIGroupName
	SVAR oldROIGroupName = NTSI:oldROIGroupName
	oldROIGroupName = ""
	
//	//list and selection waves for loading SI files
//	Wave/T ScanLoadListWave = NTSI:ScanLoadListWave
//	
//	If(!WaveExists(ScanLoadListWave))
//		Make/O/T/N=(0,2) NTSI:ScanLoadListWave /Wave = ScanLoadListWave
//		Make/O/N=(0) NTSI:ScanLoadSelWave /Wave = ScanLoadSelWave
//	Else
//		Wave ScanLoadSelWave = NTSI:ScanLoadSelWave
//		Redimension/N=(DimSize(ScanLoadListWave,0)) ScanLoadSelWave
//	EndIf
//	
//	String/G NTSI:ScanLoadPath
//	SVAR ScanLoadPath = NTSI:ScanLoadPath
//	ScanLoadPath = ""
	
	//Scan monitor path string
	String/G NTSI:SIDisplay_ImageNameList = ""

	String/G NTSI:measure
	
	//Outer folder - Scan File, which may contain multiple scans of ROIs and Zs
		//Selection shows the Scan ROIs within that scan group. Z level will be shown as text somewhere else.
	
	//The scan folder - this is the parent folder for a set of groups, and is the subfolder that the group of tiff files are actually kept in.
	//Mine are typically the experiment date (i.e. R4_200326), so multiple days of experiments can be organized in a single Igor pxp.
	Make/O/T/N=(0,1,3) NTSI:ScanFolderListWave /Wave = ScanFolderListWave
	Make/O/N=(0) NTSI:ScanFolderSelWave /Wave = ScanFolderSelWave

	//The scan group
	Make/O/T/N=(0,1,3) NTSI:ScanGroupListWave /Wave = ScanGroupListWave
	Make/O/N=(0) NTSI:ScanGroupSelWave /Wave = ScanGroupSelWave
	
	//The scans themselves
	Make/O/T/N=(0,1,3) NTSI:ScanFieldListWave /Wave = ScanFieldListWave
	Make/O/N=(0) NTSI:ScanFieldSelWave /Wave = ScanFieldSelWave
	
	//Get the scan group list wave
	Wave/T listWave = IE_GetScanFolders()
	Redimension/N=(DimSize(listWave,0),-1,-1) ScanFolderListWave,ScanFolderSelWave
	
	ScanFolderListWave = listWave
	
	If(DimSize(ScanFolderListWave,0) > 0)
		Wave/T listWave = IE_GetScanGroups(folder=ScanFolderListWave[0][0][0])
		Redimension/N=(DimSize(listWave,0),-1,-1) ScanGroupListWave,ScanGroupSelWave
		ScanGroupListWave = listWave
	EndIf
	
	//initialize to the first scan folder
	If(DimSize(ScanGroupListWave,0) > 0)
		ScanGroupSelWave[0][0][0] = 1
		Wave/T listWave = IE_GetScanFields(ScanFolderListWave[0][0][0],group=ScanGroupListWave[0][0][0])
	EndIf
	
	If(DimSize(ScanFieldListWave,0) > 0)
		ScanFieldSelWave[0][0][0] = 1
	EndIf
	
	//Scan Field match string
	String/G NTSI:scanFieldMatchStr
	SVAR scanFieldMatchStr = NTSI:scanFieldMatchStr
	scanFieldMatchStr = ""
	
	//Scan Registration variables, etc.
	If(!DataFolderExists("root:Packages:ImageExplorer:Registration"))
		NewDataFolder root:Packages:ImageExplorer:Registration
	EndIf
	
	DFREF NTSR = root:Packages:ImageExplorer:Registration
	String/G NTSR:roiXlist
	String/G NTSR:roiYlist
	Variable/G NTSR:hidden
	
End

// Generates randomly distributed sample data to use in Image Explorer.
// With this data, you can explore the image, create regions of interest (ROIs),
// and extract the time-varying fluorescence data.
Function IE_GenerateSampleData()
	DFREF dataFolder = IE_CreateDestinationFolder("root:Scans:SampleExperiment:Data")
	
	// Create 5 trials
	int i, j
	For (i= 0; i < 5; i++)
		String name = "SampleData_Trial" + num2str(i+1)
		Make/O/N=(256,256,100) dataFolder:$name /Wave = w
		
		SetScale/I x, -50e-6, 50e-6, "m", w // 100 micron range
		SetScale/I y, -50e-6, 50e-6, "m", w // 100 micron range
		SetScale/I z, 0, 10, "s", w // 10 second range
		
		w = gnoise(3)
	EndFor

	// 10 different ROIs with potential signals
	Make/FREE/N=(10,2) signalCoordinates
	signalCoordinates = enoise(-39e-6, 39-6)
	
	Variable halfWidthX = (5e-6) / DimDelta(w, 0) 
	Variable halfWidthY = (5e-6) / DimDelta(w, 1) 
	
	int size = DimSize(signalCoordinates, 0)
	For(i=0; i < size; i++)

		Variable amplitude = 1.5 + enoise(1)
		Variable tau = 0.3
		Variable offset = 5 + enoise(4)
		
		Variable xCoord = ScaleToIndex(w, signalCoordinates[i][0], 0)
		Variable yCoord = ScaleToIndex(w, signalCoordinates[i][1], 1)
		
		// Set the data for repeated trials
		For (j= 0; j < 5; j++)
			name = "SampleData_Trial" + num2str(j+1)
			Wave w = dataFolder:$name
			
			amplitude = amplitude + gnoise(0.3) // additional amplitude noise
			w[xCoord - halfWidthX,xCoord + halfWidthX][yCoord - halfWidthY, yCoord + halfWidthY][] = \
			(z > offset) ? 0.5 + gnoise(3) + (amplitude * (z - offset)/tau * exp(-(z - offset - tau)/tau)) : 0.5 + gnoise(3)
		EndFor
	EndFor
	
	IE_UpdateImageBrowserLists()
End

Function IE_OpenImageExplorer()
	//Create the ScanImage Browsing panel
	
	Variable width = 575
	Variable height = 155
	
	GetWindow/Z IE wsize
	If(!V_flag)
		DoWindow/F IE
		return 0
	Else			
		NewPanel/N=IE/K=1/W= (0, 0, width, height) as "Image Browser"
	EndIF

	DFREF NTSI = $IE
	
	If(!DataFolderRefStatus(NTSI))
		IE_CreatePackage()
	EndIf
	
	DFREF NTSI = $IE
	
	Wave/T ScanFolderListWave = NTSI:ScanFolderListWave
	Wave ScanFolderSelWave =  NTSI:ScanFolderSelWave

	//The scan group
	Wave/T ScanGroupListWave =  NTSI:ScanGroupListWave
	Wave ScanGroupSelWave = NTSI:ScanGroupSelWave
	
	//The scans themselves
	Wave/T ScanFieldListWave = NTSI:ScanFieldListWave
	Wave ScanFieldSelWave =  NTSI:ScanFieldSelWave 
	
	//ROIs
	Wave/T ROIListWave = NTSI:ROIListWave 
	Wave ROISelWave = NTSI:ROISelWave 
	
	//Scan Field match string
	String/G NTSI:scanFieldMatchStr
	SVAR scanFieldMatchStr = NTSI:scanFieldMatchStr
	scanFieldMatchStr = ""
	
	//list and selection waves for the ROI Group list box
	If(!WaveExists(NTSI:ROIGroupListWave))
		Make/O/T/N=0 NTSI:ROIGroupListWave /Wave = ROIGroupListWave
		Make/O/N=0 NTSI:ROIGroupSelWave /Wave = ROIGroupSelWave
	Else
		Wave/T ROIGroupListWave = NTSI:ROIGroupListWave 
		Wave ROIGroupSelWave = NTSI:ROIGroupSelWave
	EndIf	
	
	Variable colWidth = floor((width - 6 * 5) /5)
	Variable leftPos = 5
	
	Wave resizeListBoxPositions = NTSI:resizeListBoxPositions
	
	DefineGuide/W=IE guide1 = {FL, 0.167, FR}
	DefineGuide/W=IE guide2 = {FL, 0.33, FR}
	DefineGuide/W=IE guide3 = {FL, 0.667, FR}
	DefineGuide/W=IE guide4 = {FL, 0.833, FR}
	
	SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs, textxjust=1
	DrawText/W=IE leftPos + 0.5 * colWidth, 39,"Experiment"
	ListBox scanFolders win=IE,pos={leftPos,40},size={colWidth, 110},title="",listWave=ScanFolderListWave,selWave=ScanFolderSelWave,mode=2,proc=IE_ListBoxProc,disable=0; 
	resizeListBoxPositions[0] = leftPos - 3 // Experiment
	leftPos += colWidth + 5
	
	CheckBox selectAllScanGroups win=IE,pos={leftPos + 5,23},font=$IE_LIGHT_FONT,fsize=12,size={20,20},title="Scan Groups",value=0,proc=IE_CheckBoxProc,disable=0
	ListBox scanGroups win=IE,pos={leftPos,40},size={colWidth, 110},title="",listWave=ScanGroupListWave,selWave=ScanGroupSelWave,mode=9,proc=IE_ListBoxProc,disable=0
	resizeListBoxPositions[1] = leftPos - 3 // Scan Groups
	leftPos += colWidth + 5
	
	CheckBox selectAllScanFields win=IE,pos={leftPos + 5,23},font=$IE_LIGHT_FONT,fsize=12,size={20,20},title="Scan Fields",value=0,proc=IE_CheckBoxProc,disable=0
	ListBox scanFields win=IE,pos={leftPos,40},size={2 * colWidth, 90},title="",listWave=ScanFieldListWave,selWave=ScanFieldSelWave,mode=9,proc=IE_ListBoxProc,disable=0
	SetVariable scanFieldMatch win=IE,pos={leftPos,226},size={2 * colWidth,20},title=" ",value=scanFieldMatchStr,proc=IE_VarProc,disable=0
	resizeListBoxPositions[2] = leftPos - 3 // Scan Fields
	leftPos += 2 * colWidth + 5

	ListBox roiGroups win=IE,pos={leftPos,40},size={0.5 * colWidth, 90},title="",listWave=ROIGroupListWave,selWave=ROIGroupSelWave,mode=9,proc=IE_ListBoxProc,disable=0
	Button extractROIs win=IE, pos={leftPos,224}, size = {100, 20}, disable=0, title = "Extract ROIs", focusRing = 0, proc=IE_ButtonProc
	resizeListBoxPositions[3] = leftPos - 3 // ROI Groups
	leftPos += 0.5 * colWidth + 5
	
	SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs, textxjust=1
	DrawText/W=IE leftPos,39,"ROI Groupings"
	ListBox rois win=IE,pos={leftPos,40},size={0.5 * colWidth, 90},title="",listWave=ROIListWave,selWave=ROISelWave,mode=9,proc=IE_ListBoxProc,disable=0
	resizeListBoxPositions[4] = leftPos - 3 //ROIs
	
	Button displayScanField win=IE,pos={4,1},size={60,20},title="Display", focusRing = 0, font=$IE_LIGHT_FONT,proc=IE_ButtonProc,disable=0
	Button updateImageBrowser win=IE,pos={70,1},size={70,20},title="Refresh", focusRing = 0, font=$IE_LIGHT_FONT,proc=IE_ButtonProc,disable=0 

#ifdef JXONXOP
	Button getNewScans win=IE,pos={145,1},size={100,20},title="Load Scans", focusRing = 0, font=$IE_LIGHT_FONT,proc=IE_ButtonProc,disable=0
#endif
	
	Checkbox displayROIMasks win=IE,pos={310,3},size={0,20},value=0,title="Show ROI Masks",font=$IE_LIGHT_FONT,fsize=12,proc=IE_CheckBoxProc,disable=1
	
	//set resize hook on the Image Browser for vertical sizing only
	SetWindow IE hook(resizeHook) = IE_ResizeHook
	
	//Start a mouse hook for hovering over the in-between spaces between list boxes for resizing purposes
	SetWindow IE hook(ImageBrowserMouseHook) = IE_ImageBrowserMouseHook
End

Function IE_ImageBrowserMouseHook(s)
	STRUCT WMWinHookStruct &s
	DFREF NTSI = $IE
	
	//Are we currently resizing a column?
	NVAR columnDrag = NTSI:columnDrag
	
	//Holds the current hook positions for resizing the Image Browser
	Wave resizeListBoxPositions = NTSI:resizeListBoxPositions
	String ctrlNames = "scanFolders;scanGroups;scanFields;roiGroups;rois;"
	
	Variable hookResult = 0
	switch(s.eventCode)
		case 0: //Activate
			break
		case 1: //Deactivate
			break
		case 4: //Mouse Moved
			Variable xPos = s.mouseLoc.h
			Variable yPos = s.mouseLoc.v
			Variable i
			
			//Resizing list box
			If(columnDrag > 0)
				Variable r = ScreenResolution / 72
				GetWindow IE wsize
				
				Variable height = s.winRect.bottom
				
				Variable vSize = height - 45
				Variable vPos = height - 19
			
				For(i=0;i<ItemsInList(ctrlNames,";");i+=1)
					String theCtrl = StringFromList(i,ctrlNames,";")
					
					//Don't resize list boxes to the left of the resized list boxt
					If(i < columnDrag - 1)
						continue
					EndIf
					
					//resize and shift list boxes to the right
					ControlInfo/W=IE $theCtrl
					If(i == columnDrag - 1)
						Variable xExpand = xPos  - V_right
						
						
						//Limit to the smallest width of any column
						//custom minimum sizes for each list box
						switch(i)
							case 0:
							case 3:
							case 4:
								If(V_width + xExpand < 60)
									return 0
								EndIf
								break
							case 1:
							case 2:
								If(V_width + xExpand < 80)
									return 0
								EndIf
							break
						endswitch

						
						If(i > 1)
							ListBox $theCtrl win=IE,size={V_width + xExpand,vSize-19},pos = {V_left,V_top}
							
						Else
							ListBox $theCtrl win=IE,size={V_width + xExpand,vSize},pos = {V_left,V_top}							
							
						EndIf
						
					Else
						ListBox $theCtrl win=IE,pos = {V_left + xExpand,V_top}
					EndIf
					
					resizeListBoxPositions[i] = V_right + xExpand
				EndFor
				
				//Shift checkboxes and text
				DrawAction/W=IE delete
				
				If(columnDrag < 2)

					ControlInfo/W=IE selectAllScanGroups
					CheckBox selectAllScanGroups win=IE,pos = {V_left + xExpand,V_top}
									
					ControlInfo/W=IE selectAllScanFields
					CheckBox selectAllScanFields win=IE,pos = {V_left + xExpand,V_top}
					
					ControlInfo/W=IE scanfieldMatch
					SetVariable scanfieldMatch win=IE,pos = {V_left + xExpand,V_top}
					
					ControlInfo/W=IE extractROIs
					Button extractROIs win=IE,pos = {V_left + xExpand,V_top}

				ElseIf(columnDrag < 3)
					ControlInfo/W=IE selectAllScanFields
					CheckBox selectAllScanFields win=IE,pos = {V_left + xExpand,V_top}

					ControlInfo/W=IE scanfieldMatch
					SetVariable scanfieldMatch win=IE,pos = {V_left + xExpand,V_top}
					
					ControlInfo/W=IE extractROIs
					Button extractROIs win=IE,pos = {V_left + xExpand,V_top}
				EndIf				
				
								
				//Set the drawn text labels
				SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs
				DrawText/W=IE 34,39,"Experiment"
				
				ControlInfo/W=IE roiGroups
				SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs
				DrawText/W=IE V_right - 37,39,"ROI Groupings"
				
				ControlInfo/W=IE scanFieldMatch

				//Resize the panel as well
				GetWindow IE wsize
				height = s.winRect.bottom
				Variable top = V_top
				Variable left = V_left
				Variable right = V_right
				
				MoveWindow/W=IE left,top,right + xExpand/r,top + height/r
			Else
				//Hovering
				ControlInfo/W=IE scanFolders
				Variable topPos = V_top
				
				For(i=0;i<5;i+=1)
					If(yPos > topPos && xPos > resizeListBoxPositions[i] && xPos < resizeListBoxPositions[i] + 5)
						//Change the cursor to the left right drag icon
						s.doSetCursor = 1
						s.cursorCode = 5
						break
					EndIf
				EndFor
			EndIf
		
			break
		case 3: //mouse down
			xPos = s.mouseLoc.h
			For(i=0;i<5;i+=1)
				If(xPos > resizeListBoxPositions[i] && xPos < resizeListBoxPositions[i] + 5)
					//which resize column was clicked?
					columnDrag = i + 1
				EndIf
			EndFor
			
			break
		case 5: //mouse up
			columnDrag = 0
	endswitch
	
	return hookResult
End

Function IE_SliderProc(sa) : SliderControl
	STRUCT WMSliderAction &sa
	DFREF NTSI = $IE
	
	switch( sa.eventCode )
		case -3: // Control received keyboard focus
		case -2: // Control lost keyboard focus
		case -1: // Control being killed
			break
		default:
			If( sa.eventCode & 1 ) // value set
				Variable curval = sa.curval / 100
				
		
				//resets all the scaling on the displayed images
				NVAR numImages = NTSI:numImages
				NVAR imagePlane = NTSI:imagePlane
				
				Variable i
				For(i=0;i<numImages;i+=1)
					//Get the min/max setting for the 
					String graphRef = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
					String list = ImageNameList(graphRef,";")
					Wave image = ImageNameToWaveRef(graphRef,StringFromList(0,list,";"))
					
					Duplicate/R=[][][imagePlane]/FREE image,plane
				
					Variable minVal = WaveMin(plane)
					Variable maxVal = WaveMax(plane)
					
					strswitch(sa.ctrlName)
						case "darkValueSlider":
							ControlInfo/W=IEDisplay#control brightValueSlider
							ModifyImage/W=$graphRef $StringFromList(0,list,";"),ctab={minVal + curval * (maxVal - minVal),minVal + (V_Value/100) * (maxVal - minVal),Grays,0}
							break
						case "brightValueSlider":
							ControlInfo/W=IEDisplay#control darkValueSlider
							ModifyImage/W=$graphRef $StringFromList(0,list,";"),ctab={minVal + (V_Value/100) * (maxVal - minVal),minVal + curval * (maxVal - minVal),Grays,0}
						break
					endswitch
				EndFor
			EndIf
			break
	endswitch

	return 0
End

//Handles all pop up menu actions in SI panel
Function IE_PopProc(pa) : PopupMenuControl
	STRUCT WMPopupAction &pa

	switch( pa.eventCode )
		case 2: // mouse up
			Variable popNum = pa.popNum
			String popStr = pa.popStr
			
			strswitch(pa.ctrlName)
				case "roiType":
					//handle ROI creation control displays
					IE_SwitchROIControls(popStr)
					break
			endswitch
			
			break
		case -1: // control being killed
			break
	endswitch

	return 0
End

Function IE_PrintHoverROI(String ROI)
	SetDrawLayer/W=IEDisplay#control UserBack
	DrawAction/L=UserBack/W=IEDisplay#control getgroup=HoverROIText,delete
	SetDrawEnv/W=IEDisplay#control fsize=12,fname="Helvetica Light",xcoord=abs,ycoord=abs,textxjust=0,textyjust=1,gname=HoverROIText,gstart
	DrawText/W=IEDisplay#control 360,10,ROI
	SetDrawEnv/W=IEDisplay#control gstop
End

//Window hook function for handling mouse scrolls for zooming, and drag and drop image movements
Function IE_ZoomScrollHook(s)
	STRUCT WMWinHookStruct &s
	DFREF NTSI = $IE
	DFREF NTSR = root:Packages:ImageExplorer:ROIs
	NVAR scaleCumulative = NTSI:scaleCumulative
	NVAR ROI_Engaged = NTSI:ROI_Engaged
	NVAR Nudge_Engaged = NTSI:Nudge_Engaged	
	NVAR drawROI_Engaged = NTSI:drawROI_Engaged
	Wave/Wave clickedROIRef = NTSI:clickedROIRef
	Wave drawROIX = NTSI:drawROIX
	Wave drawROIY = NTSI:drawROIY
	
	SVAR ROITypeStr = NTSI:ROIType
	
	If(!SVAR_Exists(ROITypeStr))
		String/G NTSI:ROIType
		SVAR ROITypeStr = NTSI:ROIType
		ROITypeStr = ""
	EndIf
//	DebuggerOptions debugOnError=0
						
	Variable hookResult = 0
	switch(s.eventCode)
		case 0: //Activate
			break
		case 1: //Deactivate
			break
		case 3: //mouse down
			
			If(!cmpstr(ROITypeStr,"Marquee"))
				return 0
			EndIf
			
			Variable/G NTSI:mouseDrag
			NVAR mouseDrag = NTSI:mouseDrag
			
			
			NVAR mouseStartX = NTSI:mouseStartX
			NVAR mouseStartY = NTSI:mouseStartY
			
			If(s.eventMod == 17) //right click
				break
			EndIf
			
			//Get the mouse starting position on the click
			mouseStartX = s.mouseLoc.h
			mouseStartY = s.mouseLoc.v
			
			//If we're creating an ROI, don't engage the drag hook function unless Nudge ROI is engaged
			If(ROI_Engaged && !Nudge_Engaged)
				mouseDrag = 0
				
				//Get the ROI type
				ControlInfo/W=IEDisplay#ROIPanel roiType
				String roiType = S_Value
				
				If(!cmpstr(roiType,"Click"))
					drawROI_Engaged = 0
				ElseIf(!cmpstr(roiType,"Draw"))
					drawROI_Engaged = 1
				EndIf
				
			ElseIf(Nudge_Engaged)
				mouseDrag = 1
				
				//get the target graph window
				Variable sw =  WhichSubWindow()
				
				If(sw == -1)
			 		return 0
			 	EndIf
			 		 	
				String graphRef = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
				
				//Get the clicked ROI, put into a wave reference wave
				IE_ClickedROI(mouseStartX,mouseStartY,graphRef)
			ElseIf(drawROI_Engaged)
				mouseDrag = 0
			Else
				mouseDrag = 1
			EndIf

			hookResult = 1
			return hookResult
			break
		case 4: //mouse moved
			//This is allowed to execute during ROI creation, so dynamic ROI is 
			//available simultaneously...
		
			NVAR isDynamicROI = NTSI:isDynamicROI	
			NVAR mouseDrag = NTSI:mouseDrag
			
			//Which image frame is being controlled?
			sw = whichSubWindow()

		 	If(sw == -1)
		 		return 0
		 	EndIf

			//Which ROI are we hovering over?
			String target = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
			Variable xLoc = AxisValFromPixel(target,"Top",s.mouseLoc.h)
			Variable yLoc = AxisValFromPixel(target,"Left",s.mouseLoc.v)
			
			String ROITraceList = TraceNameList(target,";",1)
			
			//ROI the mouse is hovering over
			Variable i
			String WhichROI = ""
			For(i=0;i<ItemsInList(ROITraceList,";");i+=1)
				Wave/Z ROIWaveY = TraceNameToWaveRef(target,StringFromList(i,ROITraceList,";"))
				
				If(!WaveExists(ROIWaveY))
					continue
				EndIf
				
				Wave/Z ROIWaveX = XWaveRefFromTrace(target,StringFromList(i,ROITraceList,";"))
				
				Variable minX,maxX,minY,maxY
				
				If(DimSize(ROIWaveY,0) != 5 && DimSize(ROIWaveX,0) != 5)
					break
				EndIf
				
				minX = min(ROIWaveX[0],ROIWaveX[2])
				maxX = max(ROIWaveX[0],ROIWaveX[2])
				minY = min(ROIWaveY[0],ROIWaveY[1])
				maxY = max(ROIWaveY[0],ROIWaveY[1])
				
				If(xLoc >= minX && xLoc <= maxX && yLoc >= minY && yLoc <= maxY)
					WhichROI = RemoveEnding(NameOfWave(ROIWaveY),"_y")
					break
				Else
					WhichROI = ""
				EndIf
			EndFor
			
		 	IE_PrintHoverROI(WhichROI)
		 	
		 	If(!cmpstr(ROITypeStr,"Marquee"))
				return 0
			EndIf
					 	
			//Skip dynamic ROI if we're dragging the frame around
			If(!mouseDrag)
				target = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
				
				If(drawROI_Engaged)
					//Horizontal index of image
					Variable hPixel = AxisValFromPixel(target,"top",s.mouseLoc.h)
					
					//Vertical index of image
					Variable vPixel = AxisValFromPixel(target,"left",s.mouseLoc.v)
					
					//Add the pixel scale value into the draw ROI waves
					Variable dim = DimSize(drawROIX,0)
					Redimension/N=(dim + 1) drawROIX,drawROIY
					drawROIX[dim] = hPixel
					drawROIY[dim] = vPixel
					
					String traces = TraceNameList(target,";",0)
					If(WhichListItem("drawROIY",traces,";") == -1)
						AppendToGraph/W=$target/L/T drawROIY vs drawROIX
					EndIf
				ElseIf(isDynamicROI)
					NVAR size = NTSI:dynamicROI_Size
				 	
				 	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
				 	
					SVAR dynamicROI_Image = NTSI:dynamicROI_Image
					
					//If the image being hovered over isn't already on the dynamic ROI plot
					String theImagePath = StringFromList(sw,SIDisplay_ImagePaths,";")
					If(cmpstr(dynamicROI_Image,theImagePath))
						IE_StartDynamicROI($theImagePath)
					EndIf
					
					dynamicROI_Image = StringFromList(sw,SIDisplay_ImagePaths,";")
					
					Wave theImage = $dynamicROI_Image
					
					If(!WaveExists(theImage))
						return 1
					EndIf
					
					Variable rows,cols
					rows = DimSize(theImage,0)
					cols = DimSize(theImage,1)
																				
					//Horizontal index of image
					hPixel = AxisValFromPixel(target,"top",s.mouseLoc.h)
					
					//Vertical index of image
					vPixel = AxisValFromPixel(target,"left",s.mouseLoc.v)					
					
					//Kill the indicator box
					DrawAction/L=Overlay/W=$target getgroup=dynamicROIIndicator,delete
					
					//Ensure valid index
					Variable hPixelTest = ScaleToIndex(theImage,hPixel,0)
					Variable vPixelTest = ScaleToIndex(theImage,vPixel,1)
					If(hPixelTest > rows - 1 || vPixelTest > cols - 1 || hPixelTest < 0 || vPixelTest < 0)
						return 1
					EndIf
				
					Wave dROI = NTSI:dynamicROI
					Wave block = NTSI:block

					Variable left = hPixel - 0.5 * (size *1e-6)
					Variable right = hPixel + 0.5 * (size *1e-6)
					Variable bottom = vPixel - 0.5 * (size *1e-6)
					Variable top = vPixel + 0.5 * (size *1e-6)
					Variable xEndPix = xEnd(theImage)
					Variable yEndPix = yEnd(theImage)
					
					left = (left < DimOffset(theImage,0)) ? DimOffset(theImage,0) : left
					right = (right > xEndPix) ? xEndPix : right
					bottom = (bottom < DimOffset(theImage,1)) ? DimOffset(theImage,1) : bottom
					top = (top > yEndPix) ? yEndPix : top
									
					SetDrawLayer/W=$target Overlay
					SetDrawEnv/W=$target gname=dynamicROIIndicator,gstart
					SetDrawEnv/W=$target linethick=1,linefgc=(0,43690,65535),xcoord=top,ycoord=left,fillpat=0
					DrawRect/W=$target left,top,right,bottom
					SetDrawEnv/W=$target gstop
					
					Variable leftPix = ScaleToIndex(theImage,left,0)
					Variable rightPix = ScaleToIndex(theImage,right,0)
					Variable topPix = ScaleToIndex(theImage,top,1)
					Variable bottomPix = ScaleToIndex(theImage,bottom,1)
					
			
					try
						//Significantly faster than indexed wave assignment by about 12x
						Duplicate/R=(left,right)(top,bottom)/O theImage,block;AbortOnRTE
					catch
						Variable error = GetRTError(1)
						return 1
					endtry
			
					MatrixOP/FREE meanLayers = sum(block)
					MatrixOP/O dROI = transposeVol(meanLayers,3)
					Redimension/S/N=(-1,0) dROI
					
					Variable numPixels = (DimSize(block,0) * DimSize(block,1))
					dROI /= numPixels
				
					NVAR maxVal = NTSI:dynamicROI_MaxVal
					NVAR minVal = NTSI:dynamicROI_MinVal
					
					Variable dROI_min = WaveMin(dROI)
					Variable dROI_max = WaveMax(dROI)
					
					//Accumulates min and max scaling such that the scale is the widest scale encountered during mouse movement
					If(scaleCumulative)	
						If(dROI_max > maxVal)
							maxVal = dROI_max
							SetAxis/W=dynamicROI left,minVal,maxVal
						EndIf
						
						If(dROI_min < minVal)
							minVal = dROI_min
							SetAxis/W=dynamicROI left,minVal,maxVal
						EndIf
					Else
						//always autoscales to the data limits
						maxVal = 0
						SetAxis/Z/W=dynamicROI left,dROI_min,dROI_max
					EndIf
										
//					//Get the ROI number we are hovering over
//					String list = ImageNameList("IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw),";")
//					String ROIname = StringFromList(1,list,";")
//					
//					If(strlen(ROIname))
//						Wave roiMask = NTSR:$(ROIname + "Num")
//						Variable roiNumber = roiMask[hPixel][vPixel]
//						print roiNumber	
//					EndIf

				EndIf
				
			
				If(strlen(target))					
					//Draw ROI square
					IE_DrawROISquare(target,AxisValFromPixel(target,"top",s.mouseLoc.h),AxisValFromPixel(target,"left",s.mouseLoc.v))
				EndIf
				
				return 1
			Else
				//Drag the image around
				NVAR mouseStartX = NTSI:mouseStartX
				NVAR mouseStartY = NTSI:mouseStartY
				
				
				s.doSetCursor = 1
				s.cursorCode = 13 //hand cursor	
			 	
			 	graphRef = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
			 	
			 	//Are there ROI masks laid on top of the 
			 	//difference in pixels from last step to now
				Variable diffX =  AxisValFromPixel(graphRef,"top",s.mouseLoc.h) - AxisValFromPixel(graphRef,"top",mouseStartX)
				Variable diffY =  AxisValFromPixel(graphRef,"left",s.mouseLoc.v) - AxisValFromPixel(graphRef,"left",mouseStartY)
				
				//If nudge ROI is engaged, we may have clicked an ROI, in which case this is the wave reference
				If(WaveExists(clickedROIRef))
					Wave/Z xROI = clickedROIRef[0]
					Wave/Z yROI = clickedROIRef[1]
				EndIf
						
				If(WaveExists(xROI) && WaveExists(yROI))
					Variable doNudge = 1
				EndIf
				
				If(!doNudge)
					//shift the axis of the entire image
					GetAxis/W=$graphRef/Q top
					SetAxis/W=$graphRef top,V_min - diffX,V_max - diffX
					
					Variable xMin = V_min - diffX
					Variable xMax = V_max - diffX
					GetAxis/W=$graphRef/Q left
					SetAxis/W=$graphRef left,V_min - diffY,V_max - diffY
					
					Variable yMin = V_min - diffY
					Variable yMax = V_max - diffY
					
					Variable scaleLength = round(0.1 * abs(xMax - xMin) * 10^6) * 10^-6 //should get rounded microns number at 10% total width					
					Variable leftPos = xMin + 0.5 * scaleLength //5% from left side of the image
					Variable rightPos = leftPos + scaleLength
					
					//Adjust a scale bar position
					SetDrawLayer/W=$graphRef UserFront
					DrawAction/W=$graphRef/L=UserFront getgroup=$("ScaleBar_" + num2str(sw)),delete
					SetDrawEnv/W=$graphRef gname=$("ScaleBar_"  + num2str(sw)),gstart
					SetDrawEnv/W=$graphRef linefgc=(0xffff,0xffff,0xffff),textrgb=(0xffff,0xffff,0xffff),linethick=2,textxjust=0,xcoord=top,ycoord=abs
					DrawLine/W=$graphRef leftPos,30,rightPos,30
					SetDrawEnv/W=$graphRef textxjust=1,textrgb=(0xffff,0xffff,0xffff),linethick=2,xcoord=top,ycoord=abs
					DrawText/W=$graphRef 0.5 * (rightPos + leftPos),25,num2str(scaleLength * 10^6) + " µm"
					SetDrawEnv/W=$graphRef gstop
					
					
				ElseIf(Nudge_Engaged && doNudge)
					//Move the ROI position according to mouse movement
					xROI += diffX
					yROI += diffY
				EndIf
				
				
				//reset the start points
				mouseStartX = s.mouseLoc.h
				mouseStartY = s.mouseLoc.v
					
				hookResult = 1
			EndIf
			
			return hookResult
			break
		case 5: //mouse up
		
			If(!cmpstr(ROITypeStr,"Marquee"))
				return 0
			EndIf
			
			Variable/G NTSI:mouseDrag
			NVAR mouseDrag = NTSI:mouseDrag
			NVAR mouseStartX = NTSI:mouseStartX
			NVAR mouseStartY = NTSI:mouseStartY
			
			//Reset the clicked ROI reference if it exists
			If(WaveExists(clickedROIRef))
				clickedROIRef = $""
			EndIf
					
			//Get target image for drawing
			target = "IEDisplay"
				
			If(!cmpstr(target,"IEDisplay"))
				sw = WhichSubWindow()
				If(sw == -1) //in case of window focus error or something
					sw = 0
				EndIf
				target = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
			EndIf
			
			mouseDrag = 0
			drawROI_Engaged = 0
			
			//Create a new ROI if it's engaged
			If(ROI_Engaged)
				ControlInfo/W=IEDisplay#ROIPanel roiType
			 	roiType = S_Value
				
				//What is the width and height?
				ControlInfo/W=IEDisplay#ROIPanel roiWidth
				Variable width = V_Value
				
				ControlInfo/W=IEDisplay#ROIPanel roiHeight
				Variable height = V_Value
				
				ControlInfo/W=IEDisplay#ROIPanel roiName
				String baseName = S_Value
				
				ControlInfo/W=IEDisplay#ROIPanel roiGroupSelect
				String group = S_Value
			
				strswitch(roiType)
					case "Click": 
						
						IE_CreateROIFromClick(mouseStartX,mouseStartY,width,height,target,group,baseName)
						
						//set mouse cursor to crosshairs
						s.doSetCursor = 1
						s.cursorCode = 3
						break
					case "Grid":
						break
					default:
						break
				endswitch
			Else
				s.doSetCursor = 1
				s.cursorCode = 3 //hand cursor
			EndIf
			
			break
		case 22: //mouse scroll
			//If we're creating an ROI, don't perform hook function
			//Instead, allow for ROI thresholding for masking out the dendrite
			
			SVAR imageList = NTSI:SIDisplay_ImageNameList
			SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
			NVAR displayROIMasks = NTSI:displayROIMasks
			
			GetMouse/W=IEDisplay
			Variable mouseX = V_left,mouseY = V_top
			
			If(ROI_Engaged)
				If(displayROIMasks)
					sw = whichSubWindow()
			 	
				 	If(sw == -1)
				 		return hookResult
				 	EndIf
				 	
				 	//Graph the mouse is hovering over
				 	graphRef = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
				 	
				 	//Image the mouse is hovering over
				 	String imagePath = StringFromList(sw,SIDisplay_ImagePaths,";")
				 	
				 	Wave image = $imagePath
				 	If(!WaveExists(image))
				 		break
				 	EndIf
				 	
				 	Wave/Z maxImage = $(GetWavesDataFolder(image,2) + "_max")
				 	If(!WaveExists(maxImage))
				 		IE_GetMaxProj(imagePath,noReplace=1)
				 		Wave/Z maxImage = $(GetWavesDataFolder(image,2) + "_max")
				 	EndIf
				 	
				 	xLoc = AxisValFromPixel(graphRef,"Top",mouseX)
				 	yLoc = AxisValFromPixel(graphRef,"Left",mouseY)
				 	
				 	ROITraceList = TraceNameList(graphRef,";",1)
				 	
					//ROI the mouse is hovering over
					For(i=0;i<ItemsInList(ROITraceList,";");i+=1)
						Wave/Z ROIWaveY = TraceNameToWaveRef(graphRef,StringFromList(i,ROITraceList,";"))
						
						If(!WaveExists(ROIWaveY))
							continue
						EndIf
						
						Wave/Z ROIWaveX = XWaveRefFromTrace(graphRef,StringFromList(i,ROITraceList,";"))
						
						//Dendrite threshold variable 
						String thresholdPath = RemoveEnding(GetWavesDataFolder(ROIWaveY,2),"y") + "Threshold"
						NVAR/Z threshold = $thresholdPath
						
						If(!NVAR_Exists(threshold))
							Variable/G $thresholdPath
							NVAR threshold = $thresholdPath
							threshold = 0
						EndIf
						
						Variable LeftPt = x2pnt(maxImage,ROIWaveX[0])
						Variable RightPt = x2pnt(maxImage,ROIWaveX[2])
						Variable TopPt = ScaleToIndex(maxImage,ROIWaveY[0],1)
						Variable BottomPt = ScaleToIndex(maxImage,ROIWaveY[1],1)
						
						//Determining how much to move the threshold for every scroll wheel call
						Duplicate/R=(ROIWaveX[0],ROIWaveX[2])(ROIWaveY[0],ROIWaveY[1])/FREE maxImage,maxImageROI
		
						Variable meanROIVal = sum(maxImageROI) / numpnts(maxImageROI)
						Variable delta = meanROIVal / 20 //10 of the mean pixel value
						variable maxROIVal = WaveMax(maxImageROI)
						
						If(xLoc >= ROIWaveX[0] && xLoc <= ROIWaveX[2] && yLoc >= ROIWaveY[0] && yLoc <= ROIWaveY[1])
							//adjust the threshold of the hovered ROI
							threshold += delta * s.wheeldy
							
							//Check for threshold going too high or below zero
							threshold = (threshold > maxROIVal) ? maxROIVal : threshold
							threshold = (threshold < 0) ? 0 : threshold
	
							//re-calculate the ROI mask
							String ROIMaskPath = RemoveEnding(GetWavesDataFolder(ROIWaveY,2),"y") + "Mask"
							Wave/Z ROIMask = $ROIMaskPath
							
							If(!WaveExists(ROIMask))
								
								break
							EndIf
		
							//Check validity of the ROI points, are they outside the image? 
							LeftPt = (LeftPt > DimSize(ROIMask,0) - 1) ? DimSize(ROIMask,0) - 1 : LeftPt
							LeftPt = (LeftPt < 0) ? 0 : LeftPt
							RightPt = (RightPt > DimSize(ROIMask,0) - 1) ? DimSize(ROIMask,0) - 1 : RightPt
							RightPt = (RightPt < 0) ? 0 : RightPt
							TopPt = (TopPt > DimSize(ROIMask,1) - 1) ? DimSize(ROIMask,1) - 1 : TopPt
							TopPt = (TopPt < 0) ? 0 : TopPt
							BottomPt = (BottomPt > DimSize(ROIMask,1) - 1) ? DimSize(ROIMask,1) - 1 : BottomPt
							BottomPt = (BottomPt < 0) ? 0 : BottomPt
							
							ROIMask = 0
							ROIMask[LeftPt,RightPt][TopPt,BottomPt] = (maxImageROI[p - LeftPt][q-TopPt] >= threshold) ? 1 : 0
						EndIf
						
					EndFor
				EndIf
				
				//Make sure to break here, to avoid actually zooming the image during ROI creation
				break
			EndIf
			
		 	sw = whichSubWindow()
		 	
		 	If(sw == -1)
		 		return hookResult
		 	EndIf
		 	
		 	graphRef = "IEDisplay#image" + num2str(sw) + "#graph" + num2str(sw)
		 	String imageName = StringFromList(sw,imageList,";")
		 	
		 	//Get center point of the graph
		 	GetWindow $graphRef wsize		 	
		 	
		 	//reduce by 1% per scroll event
		 	Variable newMax,newMin
		 	
			GetAxis/W=$graphRef/Q left
			delta = (V_max - V_min) * 0.06
			
			If(s.wheeldy > 0)
				newMin = V_min + delta
				newMax = V_max - delta
			ElseIf(s.wheeldy < 0)
				newMin = V_min - delta
				newMax = V_max + delta
			Else
				return hookResult
			EndIf

			SetAxis/W=$graphRef left,newMin,newMax
			yMin = newMin
			yMax = newMax
			 
			GetAxis/W=$graphRef/Q top
			delta = (V_max - V_min) * 0.06

			If(s.wheeldy > 0)
				newMin = V_min + delta
				newMax = V_max - delta
			ElseIf(s.wheeldy < 0)
				newMin = V_min - delta
				newMax = V_max + delta
			Else
				return 1
			EndIf
	
			SetAxis/W=$graphRef top,newMin,newMax
			
			xMin = newMin
			xMax = newMax
		
			scaleLength = round(0.1 * abs(xMax - xMin) * 10^6) * 10^-6 //should get rounded microns number at 10% total width					
			leftPos = xMin + 0.5 * scaleLength //5% from left side of the image
			rightPos = leftPos + scaleLength
			
			
			//Adjust a scale bar position
			SetDrawLayer/W=$graphRef UserFront
			DrawAction/W=$graphRef/L=UserFront getgroup=$("ScaleBar_" + num2str(sw)),delete
			SetDrawEnv/W=$graphRef gname=$("ScaleBar_" + num2str(sw)),gstart
			SetDrawEnv/W=$graphRef linefgc=(0xffff,0xffff,0xffff),textrgb=(0xffff,0xffff,0xffff),linethick=2,textxjust=0,xcoord=top,ycoord=abs
			
			DrawLine/W=$graphRef leftPos,30,rightPos,30
			SetDrawEnv/W=$graphRef textxjust=1,textrgb=(0xffff,0xffff,0xffff),linethick=2,xcoord=top,ycoord=abs
			DrawText/W=$graphRef 0.5 * (rightPos + leftPos),25,num2str(scaleLength * 10^6) + " µm"
			SetDrawEnv/W=$graphRef gstop
			
		 	return 1
			break
	endswitch
	
End

//returns the string of the subwindow that the mouse is over
static Function whichSubWindow()
	GetMouse/W=IEDisplay
	Variable mouseX,mouseY
	mouseX = V_left;mouseY = V_top
	
	DFREF NTSI = $IE
	NVAR numImages = NTSI:numImages

	Variable i
	For(i=0;i<numImages;i+=1)
		String subPanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		GetWindow IEDisplay#$subPanel#$graph wsize
		If(mouseX > V_left && mouseX < V_right && mouseY > V_top && mouseY < V_bottom)
			return i//"IEDisplay#" + subPanel + "#" + graph
		EndIf
	EndFor
	
	return -1	
End

//Kills wave even if it is a part of a graph or window
static Function ReallyKillWaves(w)
  Wave w
	
  If(!WaveExists(w))
  	return 0
  EndIf
  
  string name=nameofwave(w)
  string graphs=WinList("*",";","WIN:1") // A list of all graphs
  
  //NeuroToolsPlus Viewer needs to be checked
  graphs += "NTP#Nav#Viewer"
  
  variable i,j
  for(i=0;i<itemsinlist(graphs);i+=1)
    string graph=stringfromlist(i,graphs)
    string traces=TraceNameList(graph,";",3)
    string images = ImageNameList(graph,";")

    if(whichlistitem(name,traces) != -1) // Assumes that each wave is plotted at most once on a graph.  
      RemoveFromGraph/Z /W=$graph $name
    endif
    
    if(whichlistitem(name,images) != -1) // Assumes that each wave is plotted at most once on a graph.  
      RemoveImage/Z /W=$graph $name
    endif

  endfor

  string tables=WinList("*",";","WIN:2") // A list of all tables
  for(i=0;i<itemsinlist(tables);i+=1)
    string table=stringfromlist(i,tables)
    j=0
    do
      
      String info = TableInfo(table,j)
      
      If(!strlen(info))
      	break
      EndIf
      
      //Name of wave in the column
      name = StringByKey("WAVE",info,":",";")
      
      If(!cmpstr(GetWavesDataFolder(w,2),name))
      	 RemoveFromTable/Z/W=$table $name.d,$name.l
      	
      EndIf
      
      
      string column=StringFromList(j,table)
      if(!strlen(column))
      	break
      endif
      
      if(cmpstr(column,name) == 0)
        RemoveFromTable/Z/W=$table $column
        break
      else
      	j+=1
      endif
      
      //Check 
    while(1)
  endfor 

  killwaves /z w
End  

//Replaces the indicated list item with the replaceWith string
static Function/S ReplaceListItem(index,listStr,separator,replaceWith,[noEnding])
	Variable index
	String listStr,separator,replaceWith
	Variable noEnding
	
	noEnding = (ParamIsDefault(noEnding)) ? 0 : 1
	
	listStr = RemoveListItem(index,listStr,separator)
	listStr = AddListItem(replaceWith,listStr,separator,index)
	If(index == ItemsInList(listStr,separator) - 1)
		listStr = RemoveEnding(listStr,separator)
	EndIf
	
	If(noEnding)
		listStr = RemoveEnding(listStr,separator)
	EndIf
	
	return listStr
End

//Animates a drawn square the size of the ROI onto the image when mousing over it.
Function IE_DrawROISquare(target,hPixel,vPixel)
	String target //target window
	Variable hPixel,vPixel
	
	If(!strlen(target))
		return 0
	EndIf
	
	DoWindow/Z/W=IEDisplay#ROIPanel ROIPanel
	If(!V_flag)
		return 0
	EndIf
	
	//Get the size of the square, this might slow things down
	ControlInfo/W=IEDisplay#ROIPanel roiWidth
	Variable width = V_Value
	ControlInfo/W=IEDisplay#ROIPanel roiHeight
	Variable height = V_Value
	
	
	Variable left = hPixel - 0.5 * (width *1e-6)
	Variable right = hPixel + 0.5 * (width *1e-6)
	Variable bottom = vPixel - 0.5 * (height *1e-6)
	Variable top = vPixel + 0.5 * (height *1e-6)
	
	
	DrawAction/L=Overlay/W=$target getgroup=roiIndicator,delete
	SetDrawLayer/W=$target Overlay
	SetDrawEnv/W=$target gname=roiIndicator,gstart
	SetDrawEnv/W=$target xcoord=top,ycoord=left,linethick=2,linefgc=(0,0xffff,0),fillfgc=(0,0,0,0)
	DrawRect/W=$target left,top,right,bottom
	SetDrawEnv/W=$target gstop
End

Function IE_ClearROISquare()
	DFREF NTSI = $IE
	NVAR numImages = NTSI:numImages

	Variable i
	For(i=0;i<numImages;i+=1)
		String target = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
		DrawAction/L=Overlay/W=$target getgroup=roiIndicator,delete
	EndFor
	
End

Function IE_ClearMarquee()
	DFREF NTSI = $IE
	NVAR numImages = NTSI:numImages

	Variable i
	For(i=0;i<numImages;i+=1)
		String target = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
		GetMarquee/K/W=$target
	EndFor
	
End

//Returns the X and Y ROI waves at the provided axis coordinates on the image graph 'graphRef'
Function/WAVE IE_ClickedROI(mx,my,graphRef)
	Variable mx,my
	String graphRef
	
	DFREF NTSI = $IE
	Make/O/WAVE/N=2 NTSI:clickedROIRef/Wave=clickedROIRef
	
	String info = TraceFromPixel(mx,my,"WINDOW:" + graphRef + ";DELTAX:4;DELTAY:4")
	If(strlen(info))
		Wave yROI = TraceNameToWaveRef(graphRef,StringByKey("TRACE",info,":",";"))
		Wave xROI = $RemoveEnding(GetWavesDataFolder(yROI,2),"y") + "x"
		clickedROIRef[0] = xROI
		clickedROIRef[1] = yROI
	EndIf
	
	return clickedROIRef
End

//Finds all of the existing ROI groups and returns a listwave of them
Function/Wave IE_GetROIGroups()
	DFREF NTR = $(IE + ":ROIs")

	String groups = ""	
	
	//No ROIs defined, return
	If(!DataFolderRefStatus(NTR))
		Make/N=0/T/FREE listWave
		return listWave
	EndIf
	
	DFREF saveDF = GetDataFolderDFR()
	SetDataFolder NTR
	
	//Get the names of the ROI groups
	Variable i,numGroups =	CountObjectsDFR(NTR,4)
	For(i=0;i<numGroups;i+=1)
		groups += GetIndexedObjNameDFR(NTR,4,i) + ";"
	EndFor
	
	//Convert to a alphanumerically sorted list wave
	Wave/T listWave = StringListToTextWave(groups,";")
	Sort/A listWave,listWave
	
	SetDataFolder saveDF
	
	return listWave
End 

//Finds all the available ROIs, and returns a listwave of them
Function/Wave IE_GetROIs(group)
	String group //this can be a list of groups
	
	DFREF NTSI = $IE
	
	DFREF saveDF = GetDataFolderDFR()
	
	String roiFolder = "root:Packages:ImageExplorer:ROIs:"
		
	//No group, return empty
	If(!strlen(group))
		Make/FREE/N=(0,1,2)/T listWave
		return listWave
	EndIf
		

	String roiList = ""
	String groupList = ""
	
	//LOOP THROUGH ROI GROUPS
	Variable i,j,numGroups = ItemsInList(group,";")
	
	For(j=0;j<numGroups;j+=1)
		String currentGroup = StringFromList(j,group,";")
		DFREF NTR = $(roiFolder + currentGroup)
		
		//Invalid ROI group or no ROIs defined, return empty
		If(!DataFolderRefStatus(NTR))
			Make/N=(0,1,2)/T/FREE listWave
			return listWave
		EndIf

		SetDataFolder NTR
	
		String roiX = WaveList("*_x",";","MINROWS:5")
		String roiY = WaveList("*_y",";","MINROWS:5")
		String somaROIs = WaveList("*_soma",";","")
		
		Wave/T roiData = IE_ValidROILists(roiList,groupList,currentGroup,roiX,roiY,somaROIs)
		roiList = roiData[0]
		groupList = roiData[1]
	EndFor
	
	//no group, take ROIs from the base folder
	If(!strlen(group))
		DFREF NTR = $roiFolder
		
		SetDataFolder NTR
	
		roiX = WaveList("*_x",";","MINROWS:5")
		roiY = WaveList("*_y",";","MINROWS:5")
		somaROIs = WaveList("*_soma",";","")
		
		Wave/T roiData = IE_ValidROILists(roiList,groupList,"",roiX,roiY,somaROIs)
		roiList = roiData[0]
		groupList = roiData[1]
	EndIf
	
	
	Wave/T listWave = StringListToTextWave(roiList,";")
	Make/FREE/N=(DimSize(listWave,0),1,2)/T returnList
	If(strlen(roiList))
		returnList[][0][0] = listWave[p][0][0]
	
		Wave/T listWave = StringListToTextWave(groupList,";")
		Redimension/N=(DimSize(returnList,0)) listWave //ensures same dimensions in case group is empty
		returnList[][0][1] = listWave[p][0][0]
	EndIf
		
	SetDataFolder saveDF
	
	return returnList
End

Function/WAVE IE_ValidROILists(roiList,groupList,currentGroup,roiX,roiY,somaROIs)
	String roiList,groupList,currentGroup,roiX,roiY,somaROIs
	Variable i
	
	//LOOP THROUGH ROIs
	String currentROIList = ""
	For(i=0;i<ItemsInList(roiX,";");i+=1)
		String roi = StringFromList(i,roiX,";")
		String compROI = "*" + ReplaceListItem(ItemsInList(roi,"_")-1,roi,"_","y") + "*" 
		
		//if there was no y coordinate wave
		If(stringmatch(roiY,compROI))
			currentROIList += RemoveEnding(ParseFilePath(1,roi,"_",1,0),"_") + ";"
			groupList += currentGroup + ";"
		EndIf
	EndFor
	
	//Sort the list for each ROI group individually
	currentROIList = SortList(currentROIList,";",16)
	roiList += currentROIList
	
	//append group assignments to the somatic ROI list
	For(i=0;i<ItemsInList(somaROIs,";");i+=1)
		groupList += currentGroup + ";"
	EndFor
	roiList += somaROIs
	
	Make/FREE/T/N=2 roiData
	roiData[0] = roiList
	roiData[1] = groupList
	
	return roiData
End

//Returns the full paths of the selected scan fields
Function/S IE_GetSelectedImages()
	DFREF NTSI = $IE
	Wave/T listWave = NTSI:ScanFieldListWave
	Wave selWave = NTSI:ScanFieldSelWave
	
	Variable i
	String imageList = ""
	For(i=0;i<DimSize(selWave,0);i+=1)
		If(selWave[i] > 0)
			imageList += listWave[i][0][1] + ";"
		EndIf
	EndFor
	return imageList
End

//Takes string list, and creates a text wave with its contents
static Function/WAVE StringListToTextWave(strList,separator)
	String strList,separator
	Variable size,i
	
	If(!strlen(strList))
		Make/FREE/N=0/T textWave
		return textWave
	EndIf
	
	size = ItemsInList(strList,separator)
	Make/FREE/T/N=(size) textWave
	For(i=0;i<size;i+=1)
		textWave[i] = StringFromList(i,strList,separator)
	EndFor

	return textWave
End

//Takes text wave, and creates a string list with its contents
static Function/S textWaveToStringList(textWave,separator,[col,layer,noEnding])
	Wave/T/Z textWave
	String separator
	Variable col,layer,noEnding
	
	Variable size,i
	String strList = ""
	
	If(!WaveExists(textWave))
		return ""
	EndIf
	
	If(WaveType(textWave,1) !=2)
		Abort "Input must be a text wave"
	EndIf
	
	size = DimSize(textWave,0)
	
	If(ParamIsDefault(col))
		col = 0
	EndIf
	
	If(ParamIsDefault(layer))
		layer = 0
	EndIf
	
	noEnding = ParamIsDefault(noEnding) ? 0 : 1
	
	For(i=0;i<size;i+=1)
		strList += textWave[i][col][layer] + separator
	EndFor
	
	If(noEnding)
		strList = RemoveEnding(strList,separator)
	EndIf
	
	return strList
End

Function IE_StopFramePlay()
	NVAR isPlaying = root:Packages:ImageExplorer:isPlaying
	CtrlNamedBackground play, stop
	isPlaying = 0
	
	//change button title
	Button playFrames win=IEDisplay#control,title="Play"
				
End

//Puts in the real images to the SIDisplay panel instead of max projections
Function IE_FlipMaxProj()
	DFREF NTSI = $IE
	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
	NVAR plane = NTSI:imagePlane
	NVAR isMaxProj = NTSI:isMaxProj
	NVAR isInverted = NTSI:isInverted
	
	Variable i
	For(i=0;i<ItemsInList(SIDisplay_ImagePaths,";");i+=1)
		String maxProjName = "maxProj" + num2str(i)
		String subpanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		
		Wave theImage = $StringFromList(i,SIDisplay_ImagePaths,";")
		ReplaceWave/W=IEDisplay#$subpanel#$graph image=$maxProjName,$StringFromList(i,SIDisplay_ImagePaths,";")
		
		Variable maxVal = WaveMax(theImage)
		Variable minVal = WaveMin(theImage)
				
		plane = 0
		
		ModifyImage/W=IEDisplay#$subPanel#$graph $NameOfWave(theImage) plane=plane,ctab= {minVal,maxVal,$"",isInverted}
	EndFor
	isMaxProj = 0
End

//Steps a single frame of the image stacks
Function IE_StepFrame()
	DFREF NTSI = $IE
	NVAR plane = NTSI:imagePlane
	NVAR numImages = NTSI:numImages
	
	SVAR imagePaths = NTSI:SIDisplay_ImagePaths
	SVAR imageList = NTSI:SIDisplay_ImageNameList
	
	//Make sure to remove max projection images with actual images
	NVAR isMaxProj = NTSI:isMaxProj
	If(isMaxProj)
		IE_FlipMaxProj()
	EndIf
	
	plane += 1
	
	Variable i
	For(i=0;i<numImages;i+=1)
		String name = StringFromList(i,imageList,";")
		Wave theImage = $StringFromList(i,imagePaths,";")
		
		If(plane > DimSize(theImage,2))
			plane = 0
		EndIf
		
		String panel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		name = StringFromList(i,imageList,";")
		ModifyImage/Z/W=IEDisplay#$("image" + num2str(i))#$("graph" + num2str(i)) $name plane=plane
	EndFor
	
	TitleBox frameCounter, win=IEDisplay#control, title = "Frame: " + num2str(plane) + ", " + num2str(plane * DimDelta(theImage,2)) + "s"
	return 0
	
End

//Background task for playing the image frames
Function IE_PlayFramesBackroundTask(s)
	STRUCT WMBackgroundStruct &s
	DFREF NTSI = $IE
	
	NVAR plane = NTSI:imagePlane
	NVAR deltaPlane = NTSI:deltaPlane
	
	plane += deltaPlane
	
	NVAR numImages = NTSI:numImages
	
	SVAR imagePaths = NTSI:SIDisplay_ImagePaths
	SVAR imageList = NTSI:SIDisplay_ImageNameList
	
	DoWindow IEDisplay
	If(!V_Flag)
		IE_StopFramePlay()
		return 0
	EndIf

	Variable i
		
	For(i=0;i<numImages;i+=1)
		String name = StringFromList(i,imageList,";")
		Wave theImage = $StringFromList(i,imagePaths,";")
		
		If(plane > DimSize(theImage,2))
			plane = 0
		EndIf
		
		String panel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		name = StringFromList(i,imageList,";")
		
		ModifyImage/Z/W=IEDisplay#$("image" + num2str(i))#$("graph" + num2str(i)) $name plane=plane
		
	EndFor
	
	TitleBox frameCounter, win=IEDisplay#control, title = "Frame: " + num2str(plane) + ", " + num2str(plane * DimDelta(theImage,2)) + "s"
	
	return 0
End

//Removes the wave from the SIDisplay window
Function IE_RemoveFromSIDisplay(w)
	Wave/Z w
	
	If(!WaveExists(w))
		return 0
	EndIf
	
	DFREF NTSI = root:Packages:ImageExplorer
	NVAR numImages = NTSI:numImages
	numImages = IE_GetNumImages("SIDisplay","image")
	
	Variable i

	For(i=0;i<numImages;i+=1)
		If(WaveDims(w) == 2)
			RemoveImage/Z/W=$("IEDisplay#image" + num2str(i) + "#graph" + num2str(i)) $NameOfWave(w)
		Else
			RemoveFromGraph/Z/W=$("IEDisplay#image" + num2str(i) + "#graph" + num2str(i)) $NameOfWave(w)
		EndIf
	EndFor
End

//Updates the list boxes on the ScanImage image browser
Function IE_UpdateImageBrowserLists([skipROIList])
	Variable skipROIList //Set to 1 to skip updating the ROI list, if you only want an update to the scan fields/groups/cells
	
	skipROIList = (ParamIsDefault(skipROIList)) ? 0 : 1
	
	DFREF NTSI = $IE
		
	Wave/T ScanFolderListWave = NTSI:ScanFolderListWave
	Wave ScanFolderSelWave = NTSI:ScanFolderSelWave
	
	Wave/T ScanGroupListWave = NTSI:ScanGroupListWave
	Wave ScanGroupSelWave = NTSI:ScanGroupSelWave
	
	Wave/T ScanFieldListWave = NTSI:ScanFieldListWave
	Wave ScanFieldSelWave = NTSI:ScanFieldSelWave
	
	Wave/T ROIListWave = NTSI:ROIListWave
	Wave ROISelWave = NTSI:ROISelWave
	
	Wave/T ROIGroupListWave = NTSI:ROIGroupListWave
	Wave ROIGroupSelWave = NTSI:ROIGroupSelWave
	
	//Refresh the ROI lists
	If(!skipROIList)
		Wave/T roiGroupList = IE_GetROIGroups()
		Redimension/N=(DimSize(roiGroupList,0)) ROIGroupListWave,ROIGroupSelWave
		ROIGroupListWave = roiGroupList
		
		//Ensure their is a selection
		If(sum(ROIGroupSelWave) == 0 && DimSize(ROIGroupSelWave,0) > 0)
			ROIGroupSelWave[0] = 1
		EndIf
		
		String groupList = IE_SelectedROIGroup()
		
		Wave/T roiList = IE_GetROIs(groupList)
		Redimension/N=(DimSize(roiList,0),-1,-1) ROIListWave,ROISelWave
	
		ROIListWave = roiList
		//Set selection to the first ROI after refreshing
		If(DimSize(ROISelWave,0) > 0)
			ROISelWave = 0
			ROISelWave[0] = 1 
		EndIf
	EndIf
	
	//Refresh the scan folders, if any have been added or deleted
	Wave/T listWave = IE_GetScanFolders()
	Redimension/N=(DimSize(listWave,0),-1,-1) ScanFolderListWave,ScanFolderSelWave
	ScanFolderListWave = listWave
	
	ControlInfo/W=IE scanFolders
	Variable folderSelect = V_Value
	
	//If there are no scan folders, reset everything
	If(DimSize(ScanFolderListWave,0) == 0)
		Redimension/N=(0,-1,-1) ScanFolderListWave,ScanFolderSelWave,ScanGroupListWave,ScanGroupSelWave,ScanFieldListWave,ScanFieldSelWave
		return 0
	EndIf
	
	String folder = IE_SelectedScanFolder()  
	Wave/T groups = IE_GetScanGroups(folder=ScanFolderListWave[folderSelect][0][0])

	Redimension/N=(DimSize(groups,0),-1,-1) ScanGroupListWave,ScanGroupSelWave
	ScanGroupListWave = groups
	
	//If there are no scan folders, reset everything
	If(DimSize(ScanGroupListWave,0) == 0)
		Redimension/N=(0,-1,-1) ScanGroupListWave,ScanGroupSelWave,ScanFieldListWave,ScanFieldSelWave
		return 0
	EndIf
	
	String selGroups = IE_SelectedScanGroups()
	Wave/T fields = IE_GetScanFields(ScanFolderListWave[folderSelect][0][0],group=selGroups)
	
	//Matches the scan fields according to the match string
	SVAR scanFieldMatchStr = NTSI:scanFieldMatchStr
	If(strlen(scanFieldMatchStr))
		IE_matchScanFields(scanFieldMatchStr)
	EndIf

End

//Handles the variable inputs on the SI display panel
Function IE_VarProc(sva) : SetVariableControl
	STRUCT WMSetVariableAction &sva
	DFREF NTSI = $IE
	
	switch( sva.eventCode )
		case 1: // mouse up
		case 2: // Enter key
		case 3: // Live update
			Variable dval = sva.dval
			String sval = sva.sval
			NVAR numImages = NTSI:numImages
			NVAR imagePlane = NTSI:imagePlane
			SVAR imagePaths = NTSI:SIDisplay_ImagePaths
			SVAR imageList = NTSI:SIDisplay_ImageNameList
			Variable i
			
			
			strswitch(sva.ctrlName)
				
				case "dynamicROISize":
					SVAR dynamicROI_Image = NTSI:dynamicROI_Image
					
					If(!strlen(dynamicROI_Image))
						return 0
					EndIf
					
					Wave/Z theImage = $dynamicROI_Image
					NVAR isDynamicROI = NTSI:isDynamicROI
					
					If(isDynamicROI)
						If(!WaveExists(theImage))
							isDynamicROI = 0
							return 1
						EndIf
			
						IE_StartDynamicROI(theImage)
					EndIf
					break
				case "scanFieldMatch":
					//refreshes the list boxes in case of any deleted or loaded scans
					IE_UpdateImageBrowserLists(skipROIList = 1)
					
					//Matches the scan fields according to the match string
					IE_MatchScanFields(sval)
					break
				case "selectROI":
					//turns the indicated ROI yellow and the rest cyan.
					String list = ImageNameList("IEDisplay#image0#graph0",";")
					String roiName = StringFromList(1,list,";")
					
					If(!strlen(roiName))
						break
					EndIf

					If(dval == 0)
						break
					EndIf
										
					Wave color = NTSI:ROI_ColorTable
	
					//Set all to cyan transparency 25% initially
					color[0][] = 0
					color[1,*][0] = 0
					color[1,*][1] = 43690
					color[1,*][2] = 65535
					color[1,*][3] = 16384
					
					//Set specific ROI to yellow
					color[dval][0] = 52425
					color[dval][1] = 52425
					color[dval][2] = 0
					
					break
			endswitch
			break
		case -1: // control being killed
			break
	endswitch

	return 0
End

Constant ROIPanelWidth = 110
Constant ROIPanelHeight = 300

//Handles list box selections in the ScanImage package
Function IE_ButtonProc(ba) : ButtonControl
	STRUCT WMButtonAction &ba
	
	DFREF NTSI = root:Packages:ImageExplorer
	NVAR ROI_Width = NTSI:ROI_Width
	NVAR ROI_Height = NTSI:ROI_Height
	NVAR ROI_PctThreshold = NTSI:ROI_PctThreshold
	NVAR ROI_Engaged = NTSI:ROI_Engaged
	NVAR Nudge_Engaged = NTSI:Nudge_Engaged
	NVAR drawROI_Engaged = NTSI:drawROI_Engaged
	
	Variable hookResult = 0
	switch( ba.eventCode )
		case 2: // mouse up
			// click code here
			strswitch(ba.ctrlName)
				case "displayScanfield":
					//first ensure that the mouse drag monitor is set to 0
					NVAR mouseDrag = NTSI:mouseDrag
					mouseDrag = 0
					
					//displays the selected scanfield in a new panel
					String imageList = IE_GetSelectedImages()
					IE_DisplayScanField(imageList)
					
					break
				case "updateImageBrowser":
					//refreshes the list boxes in case of any deleted or loaded scans
					IE_UpdateImageBrowserLists()

					break
				case "getNewScans":
					#ifdef JXONXOP
						IE_BrowseScanImage() //enters main function to browse scans to load
					#endif
					IE_UpdateImageBrowserLists()
					break
				case "autoScale":
					//resets all the scaling on the displayed images
					NVAR numImages = NTSI:numImages
					Variable i
					For(i=0;i<numImages;i+=1)
						String graphRef = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
						SetAxis/W=$graphRef/A					
						SetAxis/W=$graphRef/A/R left
							
						DoUpdate/W=$graphRef
						
						GetAxis/Q/W=$graphRef left
						Variable yMin = V_min
						Variable yMax = V_max
						
						GetAxis/Q/W=$graphRef top
						Variable xMin = V_min
						Variable xMax = V_max
						
						Variable scaleLength = round(0.1 * abs(xMax - xMin) * 10^6) * 10^-6 //should get rounded microns number at 10% total width					
						Variable leftPos = xMin + 0.5 * scaleLength //5% from left side of the image
						Variable rightPos = leftPos + scaleLength
						
						//Adjust a scale bar position
						SetDrawLayer/W=$graphRef UserFront
						DrawAction/W=$graphRef/L=UserFront getgroup=$("ScaleBar_" + num2str(i)),delete
						SetDrawEnv/W=$graphRef gname=$("ScaleBar_"  + num2str(i)),gstart
						SetDrawEnv/W=$graphRef linefgc=(0xffff,0xffff,0xffff),textrgb=(0xffff,0xffff,0xffff),linethick=2,textxjust=0,xcoord=top,ycoord=abs

						DrawLine/W=$graphRef leftPos,30,rightPos,30
						SetDrawEnv/W=$graphRef textxjust=1,textrgb=(0xffff,0xffff,0xffff),linethick=2,xcoord=top,ycoord=abs
						DrawText/W=$graphRef 0.5 * (rightPos + leftPos),25,num2str(scaleLength * 10^6) + " µm"
						SetDrawEnv/W=$graphRef gstop
					EndFor
					break
					
				case "liveROIs":
					
//					Button somaROI win=IEDisplay#control,pos={315,20},size={80,20},font=$IE_LIGHT_FONT,valueColor=(0,0x9999,0),title="Find Somas",disable=0,proc=siButtonProc
					String target = "IEDisplay"
				
					
					//Set the window hook to the target window
					SetWindow $target hook(zoomScrollHook) = IE_ZoomScrollHook
					
					//Build a new external panel for controlling ROI creation
					DoWindow/W=IEDisplay#ROIPanel ROIPanel
					If(V_flag)
						//Reset any ROI creation variables
						ROI_Engaged = 0
						Nudge_Engaged = 0
						SVAR type = NTSI:ROIType
						type = ""
						
						//Set the window hook to the target window back to normal
						SetWindow $target hook(zoomScrollHook) = IE_zoomScrollHook
						
						//Remove they ROI hover square
						NVAR numImages = NTSI:numImages
						numImages = IE_GetNumImages("IEDisplay","image")
							
						For(i=0;i<numImages;i+=1)		
							String subpanel = "image" + num2str(i)
							String graph = "graph" + num2str(i)
							
							SetDrawLayer/W=IEDisplay#$subpanel#$graph/K Overlay
						EndFor
						
						KillWindow/Z IEDisplay#ROIPanel
						break
					EndIf
					
					NewPanel/EXT=1/HOST=IEDisplay/N=ROIPanel/W=(ROIPanelWidth,0,0,ROIPanelHeight) as "Create ROIs"
					
					Variable strWidth = FontSizeStringWidth(IE_LIGHT_FONT, 12, 0, "Type")
					PopUpMenu roiType,win=IEDisplay#ROIPanel, pos = {2, 2}, bodywidth = ROIPanelWidth - strWidth - 6,  title="Type",font=$IE_LIGHT_FONT,value="Click;Marquee;Draw;",proc=IE_PopProc
			
					//Disable the movement hook for marquee mode
					ControlInfo/W=IEDisplay#ROIPanel roiType
					
					If(!cmpstr(S_Value,"Marquee"))
						
						SetWindow IEDisplay hook(zoomScrollHook) = $""
					EndIf

					SetVariable roiWidth,win=IEDisplay#ROIPanel,pos={100,35},size={93,20},bodywidth=60,title="W (µm)",align=1,font=$IE_LIGHT_FONT,limits={0.5,inf,1},value=ROI_Width,disable=1
					SetVariable roiHeight,win=IEDisplay#ROIPanel,pos={100,55},size={93,20},bodywidth=60,title="H (µm)",align=1,font=$IE_LIGHT_FONT,limits={0.5,inf,1},value=ROI_Height,disable=1
					SetVariable ROIname win=IEDisplay#ROIPanel,pos={100,35}, size={93,20},bodywidth=60, align = 1, font=$IE_LIGHT_FONT,value=_STR:"",title="Name",disable=0
								
					IE_UpdateImageBrowserLists()
					IE_RefreshROIGroupList()
	 
					Wave/T ROIGroupListWave = NTSI:ROIGroupListWave
					String/G NTSI:GroupList 
					SVAR GroupList = NTSI:GroupList
					GroupList = TextWaveToStringList(ROIGroupListWave,";")
					
					strWidth = FontSizeStringWidth(IE_LIGHT_FONT, 12, 0, "Group")
					PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,pos={2,55},bodywidth=ROIPanelWidth-strWidth - 6,title="Group",font=$IE_LIGHT_FONT,value=#"root:Packages:ImageExplorer:GroupList",proc=IE_PopProc
					Button addGroup win=IEDisplay#ROIPanel,pos={9,80},size={ROIPanelWidth - 4,20},font=$IE_LIGHT_FONT,title="Add Group",proc=IE_ButtonProc
					SetVariable pctFillThreshold,win=IEDisplay#ROIPanel,pos={47,35},size={60,20},bodywidth=30,title="%",value=ROI_PctThreshold,disable=1
					Button nudgeROI win=IEDisplay#ROIPanel,pos={9,77},size={93,20},font=$IE_LIGHT_FONT,valueColor=(0,0x9999,0),title="Nudge ROIs",disable=0,proc=IE_ButtonProc
					Button addROI win=IEDisplay#ROIPanel,pos={2,100},size={ROIPanelWidth - 4,20},font=$IE_LIGHT_FONT,valueColor=(0,0x9999,0),title="Add ROI",disable=0,proc=IE_ButtonProc
					Button confirmROI win=IEDisplay#ROIPanel,pos={2,100},size={ROIPanelWidth - 4,20},font=$IE_LIGHT_FONT,valueColor=(0,0x9999,0),title="Done",disable=0,proc=IE_ButtonProc
					
					IE_SwitchROIControls(S_Value)
					break
				
				case "addGroup":
					//Pops up a dialog for the user to name a new ROI group
					String newGroupName = ""
					Prompt newGroupName, "New group name:"
					DoPrompt "Add ROI Group", newGroupName
					
					If(V_flag) //user cancelled
						break
					EndIf
					
					//Sanitize the name for use as a data folder
					newGroupName = CleanupName(newGroupName, 0)
					
					If(!strlen(newGroupName))
						break
					EndIf
					
					//Create the ROI group folder
					If(!DataFolderExists("root:Packages:ImageExplorer:ROIs"))
						NewDataFolder root:Packages:ImageExplorer:ROIs
					EndIf
					
					String roiFolder = "root:Packages:ImageExplorer:ROIs:"
					
					If(DataFolderExists(roiFolder + newGroupName))
						DoAlert 0, "A group named '" + newGroupName + "' already exists."
						break
					EndIf
					
					NewDataFolder $(roiFolder + newGroupName)
					
					//Refresh lists and select the new group
					IE_UpdateImageBrowserLists()
					IE_RefreshROIGroupList()
					
					SVAR GroupList = NTSI:GroupList
					Variable groupIndex = WhichListItem(newGroupName, GroupList, ";")
					If(groupIndex != -1)
						PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,mode=groupIndex+1
						ControlUpdate/A
					EndIf
					
					break
				
				case "addROI":
					//Adds new marquee ROI

					//Get target image for drawing
					target = "IEDisplay"
					
					i = 0	
					If(!cmpstr(target,"IEDisplay"))
						Do
							GetWindow/Z $("IEDisplay#image" + num2str(i) + "#graph" + num2str(i)) activeSW
							
							If(!strlen(S_Value))
								V_flag = 0
								break
							EndIf
							
							GetMarquee/W=$("IEDisplay#image" + num2str(i) + "#graph" + num2str(i))/K left,top
							i += 1
						While(!V_flag)
						
					Else
						GetMarquee/W=$target/K/Z left,top
					EndIf
					
					Variable sw = i - 1
					Variable leftEdge,rightEdge,topEdge,bottomEdge
					leftEdge = V_left;rightEdge=V_right;topEdge=V_top;bottomEdge=V_bottom
					
					 
					If(V_flag)
					
						//Which group will we put the new ROI in?
						ControlInfo/W=IEDisplay#ROIPanel ROIGroupSelect
						String group = S_Value
						
						ControlInfo/W=IEDisplay#ROIPanel ROIname
						String baseName = S_Value
						Wave/Wave/Z roiRefs = IE_CreateROI(leftEdge,topEdge,rightEdge,bottomEdge,group=group,baseName=baseName)
						
						If(!WaveExists(roiRefs))
							return 0
						EndIf
						
						//Make the ROI mask
						SVAR SIDisplay_ImagePaths =NTSI:SIDisplay_ImagePaths
						
						
						Wave/Z image = $StringFromList(sw,SIDisplay_ImagePaths,";")
	
						//Append the ROI onto the image
						
						Wave roiX = roiRefs[0]
						Wave roiY = roiRefs[1]
						
						//Make the ROI mask wave
						Make/N=(DimSize(image,0),DimSize(image,1))/O $(RemoveEnding(GetWavesDataFolder(roiY,2),"y") + "Mask")/Wave=roiMask
						CopyScales/P image,roiMask
						roiMask = 0
						
						Variable leftPt = ScaleToIndex(roiMask,leftEdge,0)
						Variable rightPt = ScaleToIndex(roiMask,rightEdge,0)
						Variable topPt = ScaleToIndex(roiMask,topEdge,1)
						Variable bottomPt = ScaleToIndex(roiMask,bottomEdge,1)
						
						leftPt = (leftPt > DimSize(roiMask,0) - 1) ? DimSize(roiMask,0) - 1 : leftPt
						leftPt = (leftPt < 0) ? 0 : leftPt
						rightPt = (rightPt > DimSize(roiMask,0) - 1) ? DimSize(roiMask,0) - 1 : rightPt
						rightPt = (rightPt < 0) ? 0 : rightPt
						topPt = (topPt > DimSize(roiMask,1) - 1) ? DimSize(roiMask,1) - 1 : topPt
						topPt = (topPt < 0) ? 0 : topPt
						bottomPt = (bottomPt > DimSize(roiMask,1) - 1) ? DimSize(roiMask,1) - 1 : bottomPt
						bottomPt = (bottomPt < 0) ? 0 : bottomPt
						
						roiMask[leftPt,rightPt][topPt,bottomPt] = 1
						
						NVAR numImages = NTSI:numImages
						
						If(!cmpstr(target,"IEDisplay"))
							For(i=0;i<numImages;i+=1)
								graph = "graph" + num2str(i)
								subPanel = "image" + num2str(i)
								
								AppendToGraph/W=IEDisplay#$subPanel#$graph/L/T roiY vs roiX
								
//								IE_AppendROIMask("IEDisplay#" + subPanel + "#" + graph,roiMask)
							EndFor
						Else
							String imageName = StringFromList(0,ImageNameList(target,";"),";")
							If(!strlen(imageName))
								return 0
							EndIf
							String info = ImageInfo(target,imageName,0)
							String axisflags = StringByKey("AXISFLAGS",info)
							
							strswitch(axisflags)
								case "/T":
									AppendToGraph/W=$target/T roiY vs roiX
									break
								case "/R":
									AppendToGraph/W=$target/R roiY vs roiX
									break
								case "/T/L":
									AppendToGraph/W=$target/T/L roiY vs roiX
									break
								case "/B/L":
									AppendToGraph/W=$target/B/L roiY vs roiX
									break
								case "/T/R":
									AppendToGraph/W=$target/T/R roiY vs roiX
								default:
								AppendToGraph/W=$target/L/B roiY vs roiX
							endswitch
						EndIf						
						IE_UpdateImageBrowserLists()
					EndIf		
					
					//Refresh the ROI group list
					IE_RefreshROIGroupList()
					
					break
				case "confirmROI":
					//Confirms the ROI creation, or engages click/grid ROI for creating many ROIs quickly
					
					//Get target image for drawing
					target = "IEDisplay"
					
					ControlInfo/W=IEDisplay#ROIPanel roiType
					
					SVAR type = NTSI:ROIType
					type = ""
					
					strswitch(S_Value) //ROI Type
						case "Marquee":
							Button confirmROI win=IEDisplay#ROIPanel,title="Done"
							KillWindow/Z IEDisplay#ROIPanel
							//Set the window hook to the target window back to normal
							SetWindow $target hook(zoomScrollHook) = IE_ZoomScrollHook
							ROI_Engaged = 0							
							break
						case "Grid":
				
							//always use the first image in the SIDisplay panel if multiples are displayed
							If(!cmpstr(target,"IEDisplay"))
								target = "IEDisplay#image0#graph0" 
							EndIf
							
							//Get base ROI name
							ControlInfo/W=IEDisplay#ROIPanel roiName
							baseName = S_Value
						
							//Get group name
							ControlInfo/W=IEDisplay#ROIPanel roiGroupSelect
							group = S_Value			
						
							IE_CreateROIGrid(ROI_Width,ROI_Height,ROI_PctThreshold,target,group,baseName)
							
							//Refresh the ROI group list
							IE_RefreshROIGroupList()
							IE_UpdateImageBrowserLists()
							
							break
						case "Click":
							If(ROI_Engaged)
								ROI_Engaged = 0 //set back to zero, finished creating ROIs
								
								Button confirmROI win=IEDisplay#ROIPanel,title="Start"						
								KillWindow/Z IEDisplay#ROIPanel
								
								//Set the window hook to the target window to null
								SetWindow $target hook(zoomScrollHook) = $""
								
								//Enable the mouse movement hook for the SIDisplay window
								SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook	
								
								//Remove the ROI square
								//Remove they ROI hover square
								NVAR numImages = NTSI:numImages
								numImages = IE_GetNumImages("IEDisplay","image")
									
								For(i=0;i<numImages;i+=1)		
									subpanel = "image" + num2str(i)
								 	graph = "graph" + num2str(i)
									
									SetDrawLayer/W=IEDisplay#$subpanel#$graph/K Overlay
								EndFor
							Else
								//Begin creating ROIs
								ROI_Engaged = 1
								
								Button confirmROI win=IEDisplay#ROIPanel,title="Finish"
							EndIf
							break
						case "Draw":
							If(ROI_Engaged)
								ROI_Engaged = 0 //set back to zero, finished creating ROIs
								
								Button confirmROI win=IEDisplay#ROIPanel,title="Start"						
								
								//Set the window hook to the target window to null
								SetWindow $target hook(zoomScrollHook) = $""
								
								//Enable the mouse movement hook for the SIDisplay window
								SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook	
								
								//Finish creating the ROI
								ControlInfo/W=IEDisplay#ROIPanel roiName
								baseName = S_Value
								
								ControlInfo/W=IEDisplay#ROIPanel roiGroupSelect
								group = S_Value
								Wave drawROIX = NTSI:drawROIX
								Wave drawROIY = NTSI:drawROIY
								
								//Close the ROI loop
								Variable dim = DimSize(drawROIX,0)
								Redimension/N=(dim + 1) drawROIX,drawROIY
								drawROIX[dim] = drawROIX[0]
								drawROIY[dim] = drawROIY[0]
								
								IE_CreateDrawnROI(drawROIX,drawROIY,target,group,baseName)
						
							Else
								//Begin creating ROIs
								ROI_Engaged = 1
								
								Button confirmROI win=IEDisplay#ROIPanel,title="Finish"
								
								//Reset the drawROI waves
								Wave drawROIX = NTSI:drawROIX
								Wave drawROIY = NTSI:drawROIY
								Redimension/N=0 drawROIX,drawROIY
								
							EndIf
							break
					endswitch					
					break
					
				case "nudgeROI":
					//allows the user to adjust the ROI position by click/drag
					NVAR Nudge_Engaged = NTSI:Nudge_Engaged
					If(Nudge_Engaged)
						Nudge_Engaged = 0
						Button nudgeROI win=IEDisplay#ROIPanel,title="Nudge ROI"
					Else
						Nudge_Engaged = 1
						Button nudgeROI win=IEDisplay#ROIPanel,title="Done Nudging"
					EndIf
					
					break
				case "maxProj":
					//Displays the max projection of the scan frames
					NVAR isMaxProj = NTSI:isMaxProj
					
					If(isMaxProj)
						break
					EndIf  
					
					//pauses the frame video and subs in the max projection images
					IE_StopFramePlay()
					
					//Get max projection of every image in the SIDisplay
					SVAR SIDisplay_ImagePaths = root:Packages:ImageExplorer:SIDisplay_ImagePaths
					
					IE_GetMaxProj(SIDisplay_ImagePaths)
					
					break
				case "dynamicROI":
					//Sets up live viewing of time-varying according to the mouse position over the image
					SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
					Wave theImage = $StringFromList(0,SIDisplay_ImagePaths,";")
					NVAR isDynamicROI = NTSI:isDynamicROI
					
					//Kill the indicator box
					NVAR numImages = NTSI:numImages
					numImages = IE_GetNumImages("IEDisplay","image")

					For(i=0;i<numImages;i+=1)
						target = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
						DrawAction/L=Overlay/W=$target getgroup=dynamicROIIndicator,delete
					EndFor
									
									
					If(isDynamicROI)
						isDynamicROI = 0
						KillWindow/Z dynamicROI	
						return 1
					Else
						isDynamicROI = 1
					EndIf
					IE_StartDynamicROI(theImage)
					break
				case "scale94pct":
					//auto-stretches the image to 94% margins
					IE_autoStretch(98)
					break
				case "invert":
					IE_invertImage()
								
					break
			
				case "crossHairs":
					IE_AddCrossHairs()
					break
				case "lockAspectRatio":
					NVAR lockAspect = NTSI:lockAspect
					lockAspect = (lockAspect) ? 0 : 1
					
					If(lockAspect)
						Button lockAspectRatio win=IEDisplay#control,fcolor=(0x0000,0xa000,0x0000)
						GetWindow/Z IEDisplay wsize
						MoveWindow/W=IEDisplay V_left,V_top,V_right+1,V_bottom
						GetWindow/Z IEDisplay wsize
						MoveWindow/W=IEDisplay V_left,V_top,V_right-1,V_bottom
					Else
						Button lockAspectRatio win=IEDisplay#control,fcolor=(0,0,0)
					EndIf
					
					break
				case "extractImageDisplay":
					IE_DisplayImageBrowserGraph()
					break
				case "NeuronTracer":
					Execute/Q/Z "NeuronTracer()" //in case it isn't part of the Scanimage package of the user for some reason
					break
				case "extractROIs":
					// Extract ROI function
					IE_ExtractROIs()
					break
			endswitch
			break
		case -1: // control being killed
			break
	endswitch

	return hookResult
End

Function IE_DisplayImageBrowserGraph()
	String subPanel = "image0"
	String graph = "graph0"
		
	String winRec = WinRecreation("IEDisplay#" + subPanel + "#" + graph,0)
	
	Variable pos1 = strsearch(winRec,"/W",0)
	Variable pos2 = strsearch(winRec,"/HOST",0) - 1
	
	String matchStr = winRec[pos1,pos2]
	winRec = ReplaceString(matchStr,winRec,"/K=1/W=(" + num2str(10) + "," + num2str(10) + "," + num2str(360) + "," + num2str(200) + ")")
	winRec = ReplaceString("/HOST=#",winRec,"")
	Execute/Q/Z winRec
	ModifyGraph width={Plan,1,top,left}
End

//Handles all check box clicks
Function IE_CheckBoxProc(cba) : CheckBoxControl
	STRUCT WMCheckboxAction &cba
	DFREF NTSI = $IE
	
	Wave ScanFieldSelWave = NTSI:ScanFieldSelWave
	Wave ScanGroupSelWave = NTSI:ScanGroupSelWave
	
	switch( cba.eventCode )
		case 2: // mouse up
			Variable checked = cba.checked
			
			strswitch(cba.ctrlName)
				case "selectAllScans":
					Wave ScanLoadSelWave = NTSI:ScanLoadSelWave
					If(cba.checked)
						ScanLoadSelWave = 1
					Else
						ScanLoadSelWave = 0
					EndIf
					break
				case "selectAllScanGroups":
					ScanGroupSelWave = checked
					
					If(!checked && DimSize(ScanGroupSelWave,0) > 0)
						ScanGroupSelWave[0] = 1
					EndIf
					
					//Update the scan fields list box
					String folder = IE_SelectedScanFolder()
					String groups = IE_SelectedScanGroups()
					IE_GetScanFields(folder,group=groups)
					
					ControlInfo/W=SI selectAllScanFields
					Variable selectAll = V_Value
					If(selectAll)
						ScanFieldSelWave = 1
					EndIf
					
					break
				case "selectAllScanFields":
					Wave ScanFieldSelWave = NTSI:ScanFieldSelWave
					ScanFieldSelWave = checked
					break
				case "CumulativeScale":
					NVAR scaleCumulative = NTSI:scaleCumulative
					scaleCumulative = checked
					break

				case "displayROIMasks":
					NVAR displayROIMasks = NTSI:displayROIMasks
					
					If(!NVAR_Exists(displayROIMasks))
						Variable/G NTSI:displayROIMasks
						NVAR displayROIMasks = NTSI:displayROIMasks
					EndIf
					
					displayROIMasks = checked
					
					//Number of images currently displayed
					NVAR numImages = NTSI:numImages
					
					//Full paths to the displayed images
					SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
					
					Variable i,j
				
					//Append or remove the ROI Masks from the SIDisplay window
					For(i=0;i<numImages;i+=1)
						//Graph window
						String graphStr = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)
						
						//List of ROIs currently displayed		
						String ROIList = TraceNameList(graphStr,";",1)
						
						Wave image = $StringFromList(i,SIDisplay_ImagePaths,";")
						
						For(j=0;j<ItemsInList(ROIList,";");j+=1)
							Wave/Z roiY = TraceNameToWaveRef(graphStr,StringFromList(j,ROIList,";"))
							Wave/Z roiX = XWaveRefFromTrace(graphStr,StringFromList(j,ROIList,";"))
							
							String ROIFolder = GetWavesDataFolder(roiY,1)
							If(WaveExists(roiY))
								String ROIPath = RemoveEnding(GetWavesDataFolder(roiY,2),"y") + "Mask"
								Wave/Z ROIMask = $ROIPath
								If(WaveExists(ROIMask))
									//Remove it first to ensure we aren't double appending
									RemoveImage/W=$graphStr/Z $NameOfWave(ROIMask)
									
									If(displayROIMasks)
//										IE_AppendROIMask(graphStr,ROIMask)
									EndIf
								Else
									If(displayROIMasks)
										//Mask doesn't exist, create one and then append it. This will happen for all old ROIs that use this version of the package.
										String ROIName = RemoveEnding(NameOfWave(roiY),"_y")
										Wave/Z ROIMask = IE_CreateROIMask(image,ROIFolder,ROIName,roiX[0],roiX[2],roiY[0],roiY[1])
		
										If(WaveExists(ROIMask))
//											IE_AppendROIMask(graphStr,ROIMask)
										EndIf
									EndIf
								EndIf
							EndIf
						EndFor
					EndFor
		
					break
			endswitch
			break
		case -1: // control being killed
			break
	endswitch

	return 0
End

//Handles list box selections in the ScanImage package
Function IE_ListBoxProc(lba) : ListBoxControl
	STRUCT WMListboxAction &lba 
 
	Variable row = lba.row
	Variable col = lba.col
	WAVE/T/Z listWave = lba.listWave
	WAVE/Z selWave = lba.selWave
	Variable errorCode = 0
	
	DFREF NTSI = root:Packages:ImageExplorer
	DFREF SIR = root:Packages:ImageExplorer:ROIs //ROI folder
	
	//list and selection waves for the ScanImage list boxes
	Wave/T ScanFolderListWave = NTSI:ScanFolderListWave
	Wave ScanFolderSelWave = NTSI:ScanFolderSelWave
	
	Wave/T ScanFieldListWave = NTSI:ScanFieldListWave
	Wave ScanFieldSelWave = NTSI:ScanFieldSelWave
	Wave/T ScanGroupListWave = NTSI:ScanGroupListWave
	Wave ScanGroupSelWave = NTSI:ScanGroupSelWave				
	
	//ROI List and selection waves
	Wave/T ROIGroupListWave = NTSI:ROIGroupListWave
	Wave ROIGroupSelWave = NTSI:ROIGroupSelWave
	
	//ROI List and selection waves
	Wave/T ROIListWave = NTSI:ROIListWave
	Wave ROISelWave = NTSI:ROISelWave
					
	//ROI color table wave
	Wave color = NTSI:ROI_ColorTable
		
	Variable hookResult = 0
	NVAR doubleClick = NTSI:doubleClick
	
	If(doubleClick)
		doubleClick = 0
		return 1
	EndIf
		
	switch( lba.eventCode )
		case -1: // control being killed
			break
		case 1: // mouse down
			String optionStr = ""
			
			strswitch(lba.ctrlName)
				case "scanFolders":	
					optionStr = "GoTo"
				case "scanGroups":
				case "scanFields":
					
					If(!strlen(optionStr))
						optionStr = "GoTo;"
					EndIf
					
					If(lba.eventMod == 16 || lba.eventMod == 17)
						//Goto ROI contextual menu
						PopupContextualMenu/C=(lba.mouseLoc.h, lba.mouseLoc.v) optionStr
						
						If(V_flag)
							strswitch(S_selection)
								case "GoTo":
									strswitch(lba.ctrlName)	
										case "scanFolders":
											String folderPath = "root:Scans:" + ScanFolderListWave[row]
											break
										case "scanGroups":
											String scanFolder = IE_SelectedScanFolder()
											folderPath = "root:Scans:" + scanFolder + ":" + ScanGroupListWave[row]
											break
										case "scanFields":
											String scanfieldPath = ScanFieldListWave[row][0][1]
											folderPath = ParseFilePath(1,scanfieldPath,":",1,0)
											break
									endswitch
								
									ModifyBrowser collapseAll//close all folders first
									CreateBrowser //activates the data browser focus
									ModifyBrowser setDataFolder = folderPath
									
									If(!cmpstr(lba.ctrlName,"scanFields"))
										ModifyBrowser clearSelection,selectList=scanfieldPath //selects waves 
									EndIf
									
									break
								
							endswitch
															
						EndIf
					EndIf
					break
				case "roiGroups":
					If(lba.eventMod == 16 || lba.eventMod == 17)
						//Rename/Goto ROI contextual menu
						PopupContextualMenu/C=(lba.mouseLoc.h, lba.mouseLoc.v) "Rename;GoTo;Get Center;Delete;"
						If(V_flag)
							strswitch(S_Selection)
								case "Rename":
									ROIGroupSelWave[row] = 2 //set to editable
									ListBox roiGroups win=IE,setEditCell={row,0,-1,0}
									break
								case "GoTo":	
									//Browse to the selected wave
									String roiGroup = ROIGroupListWave[row][0][1]
									ModifyBrowser collapseAll//close all folders first
									CreateBrowser //activates the data browser focus									
									ModifyBrowser setDataFolder="root:Packages:ImageExplorer:ROIs:" + roiGroup
									break
								case "Get Center":
									//find the center of mass coordinates of each ROI in the group and put it in a wave
									roiGroup = ROIGroupListWave[row][0][1]
									IE_GetCenter(group=roiGroup)
									break
								case "Delete":
									DoAlert/T="Delete ROI Group" 1,"Are you sure you want to delete the ROI group?"
									If(V_flag == 1)
										roiGroup = ROIGroupListWave[row]
										Wave/T ROIListWave = IE_GetROIs(roiGroup)
										
										String ROIFolder = "root:Packages:ImageExplorer:ROIs:" + roiGroup + ":"
										
										If(DimSize(ROIListWave,0) > 0)										
											ROIListWave = ROIFolder + ROIListWave[p]
											
											// This removes any ROIs that are currently displayed in the image display
											// so that they actually get deleted
											IE_AppendROIsToImage("", "")
										
											Variable i
											For(i=0;i<DimSize(ROIListWave,0);i+=1)
												Wave/Z roiY = $(ROIListWave[i] + "_y")
												IE_RemoveFromSIDisplay(roiY) //Remove from the SIDisplay
												
												Wave/Z ROIMask = $(ROIListWave[i] + "_Mask")
												If(WaveExists(ROIMask))
													IE_RemoveFromSIDisplay(ROIMask) //Remove from the SIDisplay
													
													ReallyKillWaves(ROIMask) //Remove from any other plot and kill
												EndIf
												
												NVAR/Z ROIThreshold = $(ROIListWave[i] + "_Threshold")	
												If(NVAR_Exists(ROIThreshold))
													KillVariables/Z ROIThreshold
												EndIf
																			
												ReallyKillWaves($(ROIListWave[i] + "_x")) //Remove from any other plot and kill
												ReallyKillWaves($(ROIListWave[i] + "_y")) //Remove from any other plot and kill
												
												
											EndFor
										EndIf
																				
										//Kill the ROI Group folder
										KillDataFolder/Z ROIFolder
										
										//Update the ROI panel
										IE_updateImageBrowserLists()
										IE_refreshROIGroupList()
									EndIf
									break
							endswitch
						EndIf
					EndIf
					hookResult = 1
					break
				case "rois":
					If(lba.eventMod == 16 || lba.eventMod == 17)
						
						//Rename/Goto/Delete ROI contextual menu
						PopupContextualMenu/C=(lba.mouseLoc.h, lba.mouseLoc.v)/N "roiRightClickMenu" //"Rename;GoTo;Move To;Delete;"
						If(V_flag)		
							IE_HandleSelectionRightClick(S_Selection,ROIListWave,ROISelWave)			
							break
						EndIf
					EndIf
					hookResult = 1
					break
			endswitch
			break
		case 2: // mouse up
			
			//Uses the mouse up instead of selection hook because this process may take computation time and cause..
			//...lag if it's called on every selection hook.
			strswitch(lba.ctrlName)
				case "rois":		
					String roiList = IE_SelectedROIs()
					String groupList = IE_SelectedROIs(groups=1)			
					
					//Number of images currently displayed
					NVAR numImages = NTSI:numImages
					
					//Full paths to the displayed images
					SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
					
					If(numImages > 0)
						//Graph window
						String graphStr = "IEDisplay#image" + num2str(i) + "#graph" + num2str(i)				
						
						//This will be the reference image with which to create the ROI mask
						Wave image = $StringFromList(0,SIDisplay_ImagePaths,";")
						
						//If there is no ROI mask, create one
						For(i=0;i<ItemsInList(roiList,";");i+=1)
							DFREF ROIGroupFolder = $("root:Packages:ImageExplorer:ROIs:" + StringFromList(i,groupList,";"))
							ROIFolder = StringFromList(i,groupList,";")
							
							String ROIName = StringFromList(i,roiList,";")
							
							Wave/Z roiY = ROIGroupFolder:$(ROIName + "_y")
							Wave/Z roiX = ROIGroupFolder:$(ROIName + "_x")
							Wave/Z ROIMask = ROIGroupFolder:$(ROIName + "_Mask")
							
							If(!WaveExists(ROIMask) && WaveExists(roiY) && WaveExists(roiX))
								Wave/Z ROIMask = IE_CreateROIMask(image,ROIFolder,ROIName,roiX[0],roiX[2],roiY[0],roiY[1])
							EndIf
						EndFor 
					EndIf
					
					IE_AppendROIsToImage(groupList,roiList)
	 
					hookResult = 1
					break
			endswitch
			
			break
		case 3: // double click
			strswitch(lba.ctrlName)
				case "roiGroups":
					//select all ROIs in the group
					groupList = IE_SelectedROIGroup()
					Wave/T listWave = IE_GetROIs(groupList)
					Redimension/N=(DimSize(listWave,0),-1,-1) ROIListWave,ROISelWave
					ROIListWave = listWave
					
					If(DimSize(ROISelWave,0) > 0)
						ROISelWave = 1
					EndIf
					
					roiList = IE_SelectedROIs()
					groupList = IE_SelectedROIs(groups=1)			
					
					IE_appendROIsToImage(groupList,roiList)
					
					//Sets this variable to block the very next call to WMListBoxAction, which is Cell Selection			
					doubleClick = 1
					break
			endswitch
			
			break
		case 4: // cell selection
		case 5: // cell selection plus shift key
			strswitch(lba.ctrlName)
				case "scanFolders":
					String folder = IE_SelectedScanFolder()
					Wave/T scanGroups = IE_GetScanGroups(folder=folder)
					
					//update the scan field list box according to the scan group selection
					Redimension/N=(DimSize(scanGroups,0),-1,-1) ScanGroupListWave,ScanGroupSelWave
					ScanGroupListWave = scanGroups
					
					String groups = IE_SelectedScanGroups()
					Wave/T scanFields = IE_GetScanFields(folder,group=groups)
					
					//update the scan field list box according to the scan group selection
					Redimension/N=(DimSize(scanFields,0),-1,-1) ScanFieldListWave,ScanFieldSelWave
					ScanFieldListWave = scanFields
					
					break
				case "scanGroups":
					ControlInfo/W=IE selectAllScanGroups
					Variable selectAll = V_Value
					
					If(row > DimSize(listWave,0) - 1)
						return 0
					ElseIf(selectAll)
						ScanGroupSelWave = 1
					EndIf

					folder = IE_SelectedScanFolder()
					groups = IE_SelectedScanGroups()
					IE_GetScanFields(folder,group=groups)
					
					ControlInfo/W=IE selectAllScanFields
					selectAll = V_Value
					If(selectAll)
						ScanFieldSelWave = 1
					EndIf
					
					break		
				case "scanFields":
					ControlInfo/W=IE selectAllScanFields
					selectAll = V_Value
					If(selectAll)
						ScanFieldSelWave = 1
						return 0
					EndIf
					break
				case "roiGroups":
					//Updates the ROI list box for the selected ROI groups
					
					If(lba.eventMod == 19) //shift held during right click
						//Rename/Goto/Delete ROI contextual menu
						PopupContextualMenu/C=(lba.mouseLoc.h, lba.mouseLoc.v) "Delete;"

						If(V_flag)
							
							DoAlert/T="Delete ROI Group" 1,"Are you sure you want to delete the ROI group?"
							If(V_flag == 1)
								For(i=0;i<DimSize(ROIGroupSelWave,0);i+=1)
									If(ROIGroupSelWave[i] > 0)
										roiGroup = ROIGroupListWave[i]
										
										ROIFolder = "root:Packages:ImageExplorer:ROIs:" + roiGroup + ":"
										
										//All the ROIs in the selected ROI group
										Wave/T ROIListWave = IE_GetROIs(roiGroup)
										If(DimSize(ROIListWave,0) > 0)					
											//Make this a full path list wave for the ROIs					
											ROIListWave = ROIFolder + ROIListWave[p]
											
											//Remove any of the ROIs from the SIDisplay or other graphs, then kill them
											Variable j
											For(j=0;j<DimSize(ROIListWave,0);j+=1)
												IE_RemoveFromSIDisplay($(ROIListWave[j] + "_y")) //Remove from the SIDisplay
												ReallyKillWaves($(ROIListWave[j] + "_x")) //Remove from any other plot and kill
												ReallyKillWaves($(ROIListWave[j] + "_y")) //Remove from any other plot and kill
											EndFor
										EndIf
										
										//Kill the ROI Group folder
										KillDataFolder/Z ROIFolder		
									EndIf	
								EndFor
							EndIf
						EndIf
						
						//Update the ROI panel
						IE_updateImageBrowserLists()
						IE_refreshROIGroupList()
						
						hookResult = 1
						return hookResult
					EndIf
					
					groupList = IE_SelectedROIGroup()
					Wave/T listWave = IE_GetROIs(groupList)
					Redimension/N=(DimSize(listWave,0),-1,-1) ROIListWave,ROISelWave
					ROIListWave = listWave
					
					If(DimSize(ROISelWave,0) > 0)
						ROISelWave = 0
						ROISelWave[0] = 1
					Else
						//remove any ROI displays from the selected image
						IE_AppendROIsToImage("","")
					EndIf
					
					hookResult = 1
					break
				case "rois":
					If(lba.eventMod == 19) //shift held during right click
						
						//Rename/Goto/Delete ROI contextual menu
						PopupContextualMenu/C=(lba.mouseLoc.h, lba.mouseLoc.v)/N "roiRightClickMenu" //"Rename;GoTo;Move To;Delete;"
						If(V_flag)
							IE_HandleSelectionRightClick(S_Selection,ROIListWave,ROISelWave)
						EndIf
					ElseIf(lba.eventMod == 0) //Keyboard selection
						roiList = IE_SelectedROIs()
						groupList = IE_SelectedROIs(groups=1)			
						
						IE_AppendROIsToImage(groupList,roiList)
						hookResult = 1
					EndIf
					break
			endswitch
			
			SVAR scanFieldMatchStr = NTSI:scanFieldMatchStr
			IE_MatchScanFields(scanFieldMatchStr)
			break
		case 6: // begin edit
			SVAR oldROIGroupName = NTSI:oldROIGroupName //this will work for both groups and rois
			strswitch(lba.ctrlName)
				case "roiGroups":
					oldROIGroupName = ROIGroupListWave[row]
					break
				case "rois":
					oldROIGroupName = ROIListWave[row]
					break
			endswitch
			break
		case 7: // finish edit
			SVAR oldROIGroupName = NTSI:oldROIGroupName
		
			
			strswitch(lba.ctrlName)
				case "roiGroups":			
					String newName = ROIGroupListWave[row][0][0]
					
					//Can't overwrite itself
					If(!cmpstr(newName,oldROIGroupName))
						ROIGroupSelWave[row] = 1
						return 0
					EndIf
					
					roiGroup = ROIGroupListWave[row][0][1]
							
					ROIGroupSelWave[row] = 1
									
					RenameDataFolder root:Packages:ImageExplorer:ROIs:$oldROIGroupName,$ROIGroupListWave[row]
					
					If(DataFolderExists("root:Analysis:'" + oldROIGroupName + "'"))
						RenameDataFolder root:Analysis:$oldROIGroupName,$ROIGroupListWave[row]
					EndIf	
					
					break
				case "rois":
					newName = ROIListWave[row][0][0]
					
					//Can't overwrite itself
					If(!cmpstr(newName,oldROIGroupName))
						ROISelWave[row] = 1
						return 0
					EndIf
					
					roiGroup = ROIListWave[row][0][1]
					
					DFREF NTR = root:Packages:ImageExplorer:ROIs:$roiGroup					
					
					Wave origROIx = NTR:$(oldROIGroupName + "_x")
					Wave origROIy = NTR:$(oldROIGroupName + "_y")
					Duplicate/O origROIx, NTR:$(newName + "_x")
					Duplicate/O origROIy, NTR:$(newName + "_y")

					IE_RemoveFromSIDisplay(origROIy)
					ReallyKillWaves(origROIy)
					ReallyKillWaves(origROIx)
					
					ROISelWave[row] = 1
					break
			endswitch
			
			IE_updateImageBrowserLists()
			IE_refreshROIGroupList()
			break
		case 13: // checkbox clicked (Igor 6.2 or later)
			break
	endswitch
	return hookResult
End

//returns names of the selected ROIs in the Image Browser
Function/S IE_SelectedROIs([groups])
	Variable groups //1 to return the group assignment list
	
	groups = (ParamIsDefault(groups)) ? 0 : 1
	
	String roiList = ""
	Wave selWave = root:Packages:ImageExplorer:ROISelWave
	Wave/T listWave = root:Packages:ImageExplorer:ROIListWave
	
	Variable i
	For(i=0;i<DimSize(selWave,0);i+=1)
		If(selWave[i] > 0)
			If(groups)
				roiList += listWave[i][0][1] + ";"
			Else
				roiList += listWave[i][0][0] + ";"
			EndIf
		EndIf
	EndFor
	return roiList
End

//Returns the currently selected scan folder
Function/S IE_SelectedScanFolder()
	Wave/T listWave = root:Packages:ImageExplorer:ScanFolderListWave
	ControlInfo/W=IE scanFolders
	
	If(DimSize(listWave,0) == 0)
		return ""
	EndIf
	
	If(V_Value > DimSize(listWave,0) - 1)
		V_Value = 0
	EndIf
	
	return listWave[V_Value][0][0]
End

//Returns the currently selected scan groups
Function/S IE_SelectedScanGroups()
	Wave/T listWave = root:Packages:ImageExplorer:ScanGroupListWave
	Wave selWave = root:Packages:ImageExplorer:ScanGroupSelWave
	
	Variable i
	String groups = ""
	For(i=0;i<DimSize(selWave,0);i+=1)
		If(selWave[i] > 0)
			groups += listWave[i][0][0] + ";"
		EndIf
	EndFor
	
	groups = RemoveEnding(groups,";") //extra semi-colon on the end
	return groups
End

//returns names of the selected ROI Group in the Image Browser
Function/S IE_SelectedROIGroup()
	String groupList = ""
	Wave selWave = root:Packages:ImageExplorer:ROIGroupSelWave
	Wave/T listWave = root:Packages:ImageExplorer:ROIGroupListWave
	
	Variable i
	For(i=0;i<DimSize(selWave,0);i+=1)
		If(selWave[i] > 0)
			groupList += listWave[i] + ";"
		EndIf
	EndFor
	
	return groupList
End

//Returns the currently selected scan fields
Function/S IE_SelectedScanFields([fullpath])
	Variable fullpath //returns full path to the scans, not just the names
	
	If(ParamIsDefault(fullpath))
		fullpath = 0
	Else
		fullpath = 1
	EndIf
	
	Wave/T listWave = root:Packages:ImageExplorer:ScanFieldListWave
	Wave selWave = root:Packages:ImageExplorer:ScanFieldSelWave
	
	Variable i
	String fields = ""
	For(i=0;i<DimSize(selWave,0);i+=1)
		If(selWave[i] > 0)
			If(fullpath)
				fields += listWave[i][0][1] + ";"
			Else
				fields += listWave[i][0][0] + ";"
			EndIf
		EndIf
	EndFor
	
	fields = RemoveEnding(fields,";") //extra semi-colon on the end
	return fields
End

//Returns a text wave with all available scan folders (immediate subfolders within the root:Scans: directory)
Function/Wave IE_GetScanFolders()
	
	DFREF NTS = root:Scans
	
	If(!DataFolderRefStatus(NTS))
		Make/FREE/N=0/T listWave
		return listWave
	EndIf
	
	String folders = StringByKey("FOLDERS",DataFolderDir(1,NTS),":",";")
	Wave/T listWave = StringListToTextWave(folders,",")
	
	Sort listWave,listWave
	
	return listWave
End
//Returns a text wave with all available scan groups within the selected or indicated scan folder
Function/Wave IE_GetScanGroups([folder])
	String folder
	
	DFREF NTSI = $IE
	Wave/T ScanFolderListWave = NTSI:ScanFolderListWave
	
	DFREF saveDF = GetDataFolderDFR()
	
	If(ParamIsDefault(folder))
		folder = TextWaveToStringList(ScanFolderListWave,";")
	EndIf
	
	If(!strlen(folder))
		return $""
	EndIf

	DFREF NTS =  $("root:Scans:" + folder)

	String folders = StringByKey("FOLDERS",DataFolderDir(1,NTS),":",";")
	
	Variable i
	
	For(i=ItemsInList(folders,",")-1;i>-1;i-=1) //go backwards
		SetDataFolder NTS:$StringFromList(i,folders,",")
		String scanList = WaveList("*",";","DIMS:3")
		scanList += WaveList("*",";","DIMS:4")
		If(ItemsInList(scanList,";") == 0)
			folders = RemoveListItem(i,folders,",")
		EndIf
	EndFor
	
	Wave/T listWave = StringListToTextWave(folders,",")
	
	Sort listWave,listWave
	
	SetDataFolder saveDF
	return listWave
End

//Returns a text wave with all available scans that are within each scan group
Function/Wave IE_GetScanFields(folder[,group])
	String folder,group 
	
	DFREF saveDF = $GetDataFolder(1)
	DFREF NTSI = $IE
	Wave/T ScanGroupListWave = NTSI:ScanGroupListWave
	
	Wave/T ScanFieldListWave = NTSI:ScanFieldListWave
	Wave ScanFieldSelWave = NTSI:ScanFieldSelWave
	
	If(ParamIsDefault(group))
		group = TextWaveToStringList(ScanGroupListWave,";")
	EndIf
	
	If(!strlen(group))
		Make/FREE/N=0/T listWave
		return listWave
	EndIf
		
	String baseFolder = "root:Scans:"
	
	Variable i,j
	String list = ""
	String fullList = ""
	String path = ""
	For(i=0;i<ItemsInList(group,";");i+=1)
		String groupPath = baseFolder + folder + ":" + StringFromList(i,group,";")

		If(!DataFolderExists(groupPath))
			Abort "Scan group '" + group + "' does not exist"
		EndIf
		
		SetDataFolder $groupPath
		
		//Get all 3D waves 
		list = WaveList("*",";","DIMS:3")//takes care of extra-semicolon on the end
		list +=  WaveList("*",";","DIMS:4")
		fullList += list
		
		//Adds the path of the group to a separate list to combine later
		For(j=0;j<ItemsInList(list,";");j+=1)
			path += groupPath + ";"
		EndFor
		
	EndFor
	
	SetDataFolder saveDF
	
	//Add the full path to the scans in the second layer of the scan field list wave
	Wave/T listWave = StringListToTextWave(fullList,";")
	Wave/T pathWave = StringListToTextWave(path,";")
	
	Redimension/N=(DimSize(listWave,0),-1,-1) ScanFieldListWave,ScanFieldSelWave
	
	If(DimSize(listWave,0) > 0)
		ScanFieldListWave[][][0] = listWave
		ScanFieldListWave[][][1] = pathWave[p] + ":" + listWave[p]
	EndIf
	
	return listWave
End

//Returns a string list of matched items from the listWave or listString
Function/S IE_MatchScanFields(matchStr)
	String matchStr
	
	DFREF NTSI = $IE
	
	If(!strlen(matchStr))
		matchStr = "*"
	EndIf
	
	//List and seletion waves in the SI panel
	Wave/T fieldList = NTSI:ScanFieldListWave
	Wave/T fieldSel = NTSI:ScanFieldSelWave
	
	String nameList = "",fullPathList = ""
	
	nameList = TextWaveToStringList(fieldList,";",layer=0)
	fullPathList = TextWaveToStringList(fieldList,";",layer=1)

	//Are there any OR statements in the match string?
	Variable i,j,numORs = ItemsInList(matchStr,"||")

	
	//Set empty temporary list
	String tempList = ""
	String indexList = ""
	
	//Match each OR element in the match string separately 
	For(j=0;j<numORs;j+=1)
		String matchItem = StringFromList(j,matchStr,"||")
		indexList += filterTable(matchItem,fieldList)
	EndFor 
	
	//Delete items from the scanfield table in all layers
	For(i=DimSize(fieldList,0)-1;i>-1;i-=1) //go backwards
		Variable inList = WhichListItem(num2str(i),indexList,";")
		If(inList == -1)
			DeletePoints/M=0 i,1,fieldList
		EndIf
	EndFor
	
	Redimension/N=(DimSize(fieldList,0)) fieldSel
	Redimension/N=(DimSize(fieldList,0),1,2) fieldList
End

//Opens the scanfield display and displays the selected image stack
Function IE_DisplayScanField(imageList[,add])
	String imageList
	Variable add
	
	DFREF NTSI = $IE
	
	//Reset the mouse drag indicator to prevent image from moving with mouse...
	//...immediately without a click
	Variable/G NTSI:mouseDrag
	NVAR mouseDrag = NTSI:mouseDrag
	mouseDrag = 0
	
	NVAR numImages = NTSI:numImages
	
	NVAR isInverted = NTSI:isInverted 
	isInverted = 0
	
	//Make the master panel or resize it if it exists already
	GetWindow/Z IEDisplay wsize
	Variable alreadyOpen = V_flag == 0
	
	Variable originalLeft = (alreadyOpen) ? V_left : 0
	Variable originalTop = (alreadyOpen) ? V_top : 0
	
	//Kill original panel
	KillWindow/Z IEDisplay
	

	If(ParamIsDefault(add))
		add = 0
		numImages = ItemsInList(imageList)
	Else
		numImages += ItemsInList(imageList)
	EndIf
	
	Variable i,j,totalY = 0,maxX = 0
	String yDimList = "",xDimList = ""
	//Check that images all exist and get x/y dimension size
	For(i=ItemsInList(imageList,";")-1;i>-1;i-=1) //step backwards
		Wave theImage = $StringFromList(i,imageList,";")
		If(!WaveExists(theImage))
			imageList = RemoveListItem(i,imageList,";")
		Else
			xDimList += num2str(DimSize(theImage,0) * DimDelta(theImage,0)) + ";"
			yDimList += num2str(DimSize(theImage,1) * DimDelta(theImage,1)) + ";"
			totalY += DimSize(theImage,1) * DimDelta(theImage,1)
			
			//maximum x dimension
			If(DimSize(theImage,0) * DimDelta(theImage,0) > maxX)
				maxX = DimSize(theImage,0) * DimDelta(theImage,0)
				Variable maxXScan = i
			EndIf
		EndIf
	EndFor
	
	//reverse the list for correct order
	xDimList = SortList(xDimList, ";", 16)
	yDimList = SortList(yDimList, ";", 16)
	
	//No valid images
	If(ItemsInList(imageList,";") == 0)
		return 0
	EndIf
	
	
	Variable xPixels,yPixels,frames,xSize,ySize,xDim,yDim,sizeRatio,numCols,baseWidth
	 
	baseWidth = 405
	 
	//sizing of the panel
	numCols = ceil(numImages / 3)
	If(numCols > 1)
		Variable numRows = 3

		xSize = baseWidth * numCols
	
		ySize = ceil((xSize/numCols) * numRows)
	Else
		numRows = numImages
		xsize = baseWidth
		If(numRows < 3)
			ySize = ceil(xSize * numImages)
		Else
			baseWidth *= 0.9
			ySize = ceil(xSize * numImages)
		EndIf
	EndIf
	
	
	Variable segmentY = floor(ySize/numImages)
	Variable fractionY,fractionX
	
	//put the new image panel next to the Image Browser window
	Variable r = ScreenResolution / 72
	GetWindow/Z SI wsize
	Variable leftOffset = V_right * r + 10
	
	//Make a new SI Display panel
	if (alreadyOpen)
		NewPanel/K=1/W=(originalLeft * r, originalTop * r, originalLeft*r + xSize, originalTop*r + ySize)/N=IEDisplay as "Scanfield Display"	
	else
		NewPanel/K=1/W=(leftOffset,0,leftOffset + xSize,ySize)/N=IEDisplay as "Scanfield Display"	
	endif
	
	ModifyPanel/W=IEDisplay frameStyle=0
	
	//Make the image part of the panel
	DefineGuide/W=IEDisplay imageDivide = {FT,50}
	
	//Make the control bar part of the panel
	NewPanel/HOST=IEDisplay/N=control/FG=(FL,FT,FR,imageDivide)
	ModifyPanel/W=IEDisplay#control frameStyle=0

	//Define bottom guides for each image in a column
	For(i=0;i<numRows;i+=1)
		fractionY = (i+1) / numRows
		fractionY = (fractionY > 1) ? 1 : fractionY //ensure less than 1
		DefineGuide/W=IEDisplay $("imageBottom" + num2str(i)) = {imageDivide,fractionY,FB}	
	EndFor
	
	//Define right guides for each image in a row
	For(i=0;i<numCols;i+=1)
		fractionX = (i+1) / numCols
		fractionX = (fractionX > 1) ? 1 : fractionX //ensure less than 1
		DefineGuide/W=IEDisplay $("imageRight" + num2str(i)) = {FL,fractionX,FR}	
	EndFor
	
	//will save the image name list to an SVAR
	String/G NTSI:SIDisplay_ImageNameList
	SVAR SIDisplay_ImageNameList = NTSI:SIDisplay_ImageNameList
	SIDisplay_ImageNameList = ""
	
	//full paths of the waves in the SI display panel
	String/G NTSI:SIDisplay_ImagePaths
	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
	SIDisplay_ImagePaths = imageList
	
	Variable count = 0
	For(i=0;i<numCols;i+=1)
		String guideRight = "imageRight" + num2str(i)
		
		If(i == 0)
			String guideLeft = "FL"
		Else
			guideLeft = "imageRight" + num2str(i-1)
		EndIf
			
		For(j=0;j<numRows;j+=1)
			
			String guideBottom = "imageBottom" + num2str(j)
			
			If(j == 0)
				String guideTop = "imageDivide"
			Else
				guideTop = "imageBottom" + num2str(j-1)
			EndIf
			
			//Make each image subpanel below the control panel
			String subPanel = "image" + num2str(count)

			NewPanel/HOST=IEDisplay/N=$subPanel/FG=($guideLeft,$guideTop,$guideRight,$guideBottom)	
			SetWindow IEDisplay#$subPanel activeChildFrame = 0
			
			//Display an image window within the subpanel
			String graph = "graph" + num2str(count)
			Display/HOST=IEDisplay#$subPanel/FG=(FL,FT,FR,FB)/N=$graph
			ModifyGraph/W=IEDisplay#$subPanel#$graph gbRGB=(15000,15000,15000)

			//What are the autoscaled min max values? Directly telling Igor what these are greatly speeds up frame play.
			Wave theImage = $StringFromList(count,imageList,";")
			
			Variable maxVal = WaveMax(theImage)
			Variable minVal = WaveMin(theImage)
			

			//Append and modify the image being appended
			//append the image
			AppendImage/W=IEDisplay#$subPanel#$graph/L/T theImage
			ModifyImage/W=IEDisplay#$subPanel#$graph $NameOfWave(theImage) plane=0,ctab= {minVal,maxVal,Grays,0}
			SetAxis/W=IEDisplay#$subPanel#$graph/R/Z/A left
			
			ModifyGraph/W=IEDisplay#$subPanel#$graph noLabel=2,axThick=0,standoff=0,btLen=2,margin=-1
			
			//append the image to the image list
			SIDisplay_ImageNameList += NameOfWave(theImage) + ";"
			
			//Set window hook for scroll zoom
			SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook
			SetWindow IEDisplay hook(killWindowHook) = IE_KillWindowHook

			
			//total x width for scale bar
			Variable width = round((xRange(theImage) * 0.1 * 10^6)) * 10^-6 //should get rounded microns number at 10% total width
			Variable leftPos = leftx(theImage) + 0.5 * width //5% from left side of the image
			Variable rightPos = leftPos + width
			Variable yPos = DimOffset(theImage,1) + yRange(theImage) * 0.05
			Variable textPos = yPos * 1.02
			
			//Append a scale bar
			SetDrawLayer/W=IEDisplay#$subPanel#$graph UserFront
			SetDrawEnv/W=IEDisplay#$subPanel#$graph gname=$("ScaleBar_" + num2str(count)),gstart
			SetDrawEnv/W=IEDisplay#$subPanel#$graph linefgc=(0xffff,0xffff,0xffff),textrgb=(0xffff,0xffff,0xffff),linethick=2,xcoord=top,ycoord=abs
			
			DrawLine/W=IEDisplay#$subPanel#$graph leftPos,30,rightPos,30
			SetDrawEnv/W=IEDisplay#$subPanel#$graph textxjust=1,textrgb=(0xffff,0xffff,0xffff),linethick=2,xcoord=top,ycoord=abs
			DrawText/W=IEDisplay#$subPanel#$graph 0.5 * (rightPos + leftPos),25,num2str(width * 10^6) + " µm"
			SetDrawEnv/W=IEDisplay#$subPanel#$graph gstop
			
			count += 1
			
			If(count == numImages)
				break
			EndIf
		EndFor
	EndFor
	

	//Make the control panel
	Button playFrames win=IEDisplay#control,pos={5,0},size={45,20},font=$IE_LIGHT_FONT,title="Play",disable=0,proc=IE_HandlePlayFrames
	Button slowFrames win=IEDisplay#control,pos={55,0},size={25,20},font=$IE_LIGHT_FONT,title="<<",disable=0,proc=IE_HandlePlayFrames
	Button speedFrames win=IEDisplay#control,pos={85,0},size={25,20},font=$IE_LIGHT_FONT,title=">>",disable=0,proc=IE_HandlePlayFrames
	Button stepFrame win=IEDisplay#control,pos={115,0},size={40,20},font=$IE_LIGHT_FONT,title="Step",disable=0,proc=IE_HandlePlayFrames
	Button autoScale win=IEDisplay#control,pos={160,0},size={40,20},font=$IE_LIGHT_FONT,title="Auto",disable=0,proc=IE_ButtonProc
	Button maxProj win=IEDisplay#control,pos={205,0},size={60,20},font=$IE_LIGHT_FONT,title="Max Proj",disable=0,proc=IE_ButtonProc
	Button liveROIs win=IEDisplay#control,pos={270,25},size={40,20},font=$IE_LIGHT_FONT,title="ROIs",disable=0,proc=IE_ButtonProc
	Button dynamicROI win=IEDisplay#control,pos={270,0},size={40,20},font=$IE_LIGHT_FONT,title="Live",disable=0,proc=IE_ButtonProc
	Button crossHairs win=IEDisplay#control,pos={315,25},size={27,20},font=$IE_LIGHT_FONT,fsize=20,title="⊕",disable=0,proc=IE_ButtonProc 
	Button lockAspectRatio win=IEDisplay#control,pos={345,25},size={27,20},font=$IE_LIGHT_FONT,fsize=10,title="L.A.",disable=0,proc=IE_ButtonProc 
	Button extractImageDisplay win=IEDisplay#control,pos={375,25},size={27,20},font=$IE_LIGHT_FONT,fsize=24,title="\W5005",disable=0,proc=IE_ButtonProc 
	
	If(strlen(FunctionInfo("NeuronTracer")))
		String PictPathStr = SpecialDirPath("Igor Pro User Files",0,0,0) + "User Procedures:NeuroTools+:Functions:ScanImage:"
		NewPath/O/Z/Q PictPath,PictPathStr
		LoadPICT/P=PictPath/O/Q "NeuronTracerPict.png",NeuronTracerPict
	
		Button NeuronTracer win=IEDisplay#control,pos={360,0},size={40,20},title="\\$PICT$name=NeuronTracerPict$/PICT$",proc=IE_ButtonProc
	EndIf
	
	
	NVAR size = NTSI:dynamicROI_Size

	SetVariable dynamicROISize win=IEDisplay#control,pos={315,3},size={40,20},fsize=10,font=$IE_LIGHT_FONT,title=" ",value=size,disable=0,proc=IE_VarProc
	
	String/G NTSI:availableImages
	SVAR availableImages = NTSI:availableImages
	availableImages = WinList("*",";","WIN:1")
	
	Variable numGraphs = ItemsInList(availableImages,";")
	For(i=numGraphs-1;i>-1;i-=1) //go backwards
		String images = ImageNameList(StringFromlist(i,availableImages,";"),";")
		If(!strlen(images))
			availableImages = RemoveListItem(i,availableImages,";")
		EndIf
	EndFor
	availableImages = "IEDisplay;" + availableImages
		
	//Create custom color table wave for ROI masks
	Make/O/N=(10,4) NTSI:ROI_ColorTable
	Wave color = NTSI:ROI_ColorTable
	
	//Set all to cyan transparency 25% initially
	color[0][] = 0
	color[1,*][0] = 0
	color[1,*][1] = 43690
	color[1,*][2] = 65535
	color[1,*][3] = 16384
	
	Slider darkValueSlider win=IEDisplay#control,pos={5,25},size={75,20},vert=0,side=0, thumbColor=(13107,13107,13107), limits={0,100,1}, focusRing = 0, value=0,proc=IE_SliderProc
	Slider brightValueSlider win=IEDisplay#control,pos={5,35},size={75,20},vert=0,side=0, thumbColor=(48059,48059,48059), limits={0,100,1}, focusRing = 0, value=100,proc=IE_SliderProc
	Button scale94pct win=IEDisplay#control,pos={90,25},size={20,20},title="S",proc=IE_ButtonProc
	Button invert win=IEDisplay#control,pos={120,25},size={20,20},font=$IE_LIGHT_FONT,title="-1",disable=0,proc=IE_ButtonProc
	TitleBox frameCounter win = IEDisplay#control, pos = {145, 25}, size={120, 20}, frame=0, title="",disable = 0
	
	//Bring panel to the front
	DoWindow/F IEDisplay
	
	//Set the resize hook to allow for aspect ratio lock functionality
	SetWindow IEDisplay hook(ImageResizeHook) = IE_ImageResizeHook	
	NVAR lockAspect = NTSI:lockAspect
	If(lockAspect)
		Button lockAspectRatio win=IEDisplay#control,fcolor=(0x0000,0xa000,0x0000)
		GetWindow/Z IEDisplay wsize
		MoveWindow/W=IEDisplay V_left,V_top,V_right+1,V_bottom
		GetWindow/Z IEDisplay wsize
		MoveWindow/W=IEDisplay V_left,V_top,V_right-1,V_bottom
	Else
		Button lockAspectRatio win=IEDisplay#control,fcolor=(0,0,0)
	EndIf
End

//Returns the X range of a wave
static Function xRange(w)
	Wave w
	
	return abs((rightx(w) - DimDelta(w,0)) - leftx(w))
End

//Returns the X range of a wave
static Function yRange(w)
	Wave w
	return (DimSize(w,1)-1) * DimDelta(w,1)
End

//Returns the last X point in a wave
static Function xEnd(w)
	Wave w
	return rightx(w) - DimDelta(w,0)
End

//Returns the last Y point in a wave
static Function yEnd(w)
	Wave w
	return (DimSize(w,1) - 1) * DimDelta(w,1) + DimOffset(w,1)
End

//handles resize events on the SI Display
Function IE_ImageResizeHook(s)
	STRUCT WMWinHookStruct &s
	DFREF NTSI = $IE
	
	//paths to the currently displayed images		
	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
	NVAR numImages = NTSI:numImages
	NVAR lockAspect = NTSI:lockAspect
	
	Variable hookResult = 0
	switch(s.eventCode)
		case 0: //Activate
			break
		case 1: //Deactivate
			break
		case 6: //Resize
			Variable r = ScreenResolution / 72
			
			If(lockAspect)
				//current sizing info for the window
				GetWindow/Z IEDisplay wsize
				
				//get the aspect ratio of the image. Only can be done with single image for now
				Wave theImage = $StringFromList(0,SIDisplay_ImagePaths,";")
				Variable aspect = abs(yRange(theImage) / xRange(theImage))
				
				Variable numCols = ceil(numImages / 3)
				If(numCols > 1)
					Variable numRows = 3
				Else
					numRows = numImages
				EndIf
				
				//Use the x size as the base size for controlling the aspect of the image.
				//Image paneling starts 50 pixels from the top of the window.
				Variable newHeight = round(((V_right - V_left)/numCols) * aspect * numRows) + 50
				MoveWindow/W=IEDisplay V_left,V_top,V_right,V_top + newHeight
			EndIf
			break
	endswitch
	
	return hookResult
End

//handles resize events on the Image Browser
Function IE_ResizeHook(s)
	STRUCT WMWinHookStruct &s
	DFREF NTSI = $IE
	Wave resizeListBoxPositions = NTSI:resizeListBoxPositions
		
	//Are we currently resizing a column?
	NVAR columnDrag = NTSI:columnDrag

	Variable hookResult = 0
	switch(s.eventCode)
		case 0: //Activate
			break
		case 1: //Deactivate
			break
		case 6: //Resize
			Variable r = ScreenResolution / 72
			
			//prevents horizontal resize, allows vertical resize. 575 is the set width of the panel
			GetWindow IE wsize
			
			Variable height = s.winRect.bottom
			height = (height < 125) ? 125 : height
			
			Variable width = s.winRect.right
			width = (width < 575) ? 575 : width
			
			If(width == 575 && columnDrag)
				break
			EndIf
			
			//MoveWindow/W=SI V_left,V_top,V_left + 460/r,V_top + height/r
			MoveWindow/W=IE V_left,V_top,V_left + width/r,V_top + height/r
			
			Variable vSize = height - 45
			Variable vPos = height - 19
			
			//Get the right edge of the ROI box to know how much width we need to expand to
			ControlInfo/W=IE rois
			Variable xExpand = width - (V_right + 5)
			
			//This distance is evenly divided by the 5 list boxes
			Variable controlExpand = floor(xExpand / 5)

			//Remainder Gets assigned to the Scan Fields list box
			Variable remainder = mod(controlExpand,5)
			
			//Set the vertical size of the list boxes
			ControlInfo/W=IE scanFolders
			resizeListBoxPositions[0] = V_right + controlExpand
			ListBox scanFolders win=IE,size={V_width + controlExpand,vSize},pos = {5,V_top}
			
			ControlInfo/W=IE scanGroups
			resizeListBoxPositions[1] = V_right + controlExpand
			ListBox scanGroups win=IE,size={V_width + controlExpand,vSize},pos = {V_left + controlExpand,V_top}
			
			ControlInfo/W=IE scanFields
			resizeListBoxPositions[2] = V_right + controlExpand
			ListBox scanFields win=IE,size={V_width + controlExpand,vSize-19},pos = {V_left + 2 * controlExpand,V_top}
			
			ControlInfo/W=IE roiGroups
			resizeListBoxPositions[3] = V_right + controlExpand
			ListBox roiGroups win=IE,size={V_width + controlExpand,vSize-19},pos = {V_left + 3 * controlExpand,V_top}
				
			ControlInfo/W=IE rois
			resizeListBoxPositions[4] = V_right + controlExpand + remainder
			ListBox rois win=IE,size={V_width + controlExpand + remainder,vSize-19},pos = {V_left + 4 * controlExpand,V_top}
			
			ControlInfo/W=IE scanFieldMatch
			SetVariable scanFieldMatch win=IE,pos={V_left + 2 * controlExpand,vPos},size = {V_width + controlExpand,20}
			
			ControlInfo/W=IE extractROIs
			Button extractROIs win=IE, pos={V_left + 3 * controlExpand, vPos - 3}

			//Shift the checkboxes
			ControlInfo/W=IE selectAllScanGroups
			CheckBox selectAllScanGroups win=IE,pos = {V_left + controlExpand,V_top}
							
			ControlInfo/W=IE selectAllScanFields
			CheckBox selectAllScanFields win=IE,pos = {V_left + controlExpand * 2,V_top}

			//Set the drawn text labels
			DrawAction/W=IE delete
			
			SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs
			DrawText/W=IE 34,39,"Experiment"
			
			ControlInfo/W=IE roiGroups
			SetDrawEnv/W=IE fname=$IE_LIGHT_FONT,fsize=12,xcoord=abs,ycoord=abs
			DrawText/W=IE V_right - 37,39,"ROI Groupings"
			
			break
	endswitch
	return hookResult
End

//this allows us to kill all of our other hook functions as we kill the display window
Function IE_KillWindowHook(s)
	STRUCT WMWinHookStruct &s
	
	Variable hookResult = 0
	switch(s.eventCode)
		case 0: //Activate
			break
		case 1: //Deactivate
			break
		case 2: //killing window
			DFREF NTSI = $IE
			NVAR numImages = NTSI:numImages
			NVAR isMaxProj = NTSI:isMaxProj
	 		Variable i
	 		IE_StopFramePlay()
	 		
	 		//remove all the window hooks
	 		For(i=0;i<numImages;i+=1)
				String subPanel = "image" + num2str(i)
				String graph = "graph" + num2str(i)
				SetWindow IEDisplay#$subPanel#$graph hook(zoomScrollHook) = $""
				isMaxProj = 0
			EndFor
			
			//Kill dynamic ROI window if it exists
			NVAR isDynamicROI = NTSI:isDynamicROI
			isDynamicROI = 0
			KillWindow/Z dynamicROI
			break
	endswitch
	return hookResult
End

//Switches control display on the ROI Creation Panel according to ROI type
Function IE_SwitchROIControls(roiType)
	String roiType
	
	DFREF NTSI = $IE
	SVAR roiTypeStr = NTSI:ROIType
	roiTypeStr = roiType
	NVAR ROI_Engaged = NTSI:ROI_Engaged
	
	ControlInfo/W=IEDisplay#ROIPanel roiType
	Variable controlYPosStart = V_top + 25
	Variable controlYPos = controlYPosStart

	strswitch(roiType)
		case "Marquee":
			controlYPos = controlYPosStart
			
			SetVariable ROIname,win=IEDisplay#ROIPanel,pos={100,controlYPos},disable=0; controlYPos += 25
			Button addGroup,win=IEDisplay#ROIPanel,pos={2,controlYPos}; controlYPos += 25
			PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,pos={2, controlYPos}, disable=0; controlYPos += 25
			Button addROI,win=IEDisplay#ROIPanel,pos={2, controlYPos}, disable=0; controlYPos += 25
			Button confirmROI,win=IEDisplay#ROIPanel,pos={2,controlYPos},title="Done"; controlYPos += 25

		
			// Disabled in this mode
			SetVariable roiWidth,win=IEDisplay#ROIPanel,disable=1;
			SetVariable roiHeight,win=IEDisplay#ROIPanel,disable=1;
			SetVariable pctFillThreshold,win=IEDisplay#ROIPanel,disable=1
			Button nudgeROI win=IEDisplay#ROIPanel,pos={2,controlYPos}, disable=1

			IE_ClearROISquare()
//			SetWindow IEDisplay hook(zoomScrollHook) = $""
			ROI_Engaged = 1
			SetWindow IEDisplay hook(zoomScrollHook) = zoomScrollHook
			break
		case "Click":
			controlYPos = controlYPosStart
			
			SetVariable ROIname,win=IEDisplay#ROIPanel,pos={100,controlYPos},disable=0;controlYPos += 25
			SetVariable roiWidth,win=IEDisplay#ROIPanel,pos={100,controlYPos},disable=0; controlYPos+= 25
			SetVariable roiHeight,win=IEDisplay#ROIPanel,pos={100,controlYPos},disable=0; controlYPos+= 25
			Button addGroup,win=IEDisplay#ROIPanel,pos={2,controlYPos}; controlYPos += 25
			PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,pos={2, controlYPos}, disable=0; controlYPos += 25
			Button confirmROI,win=IEDisplay#ROIPanel,pos={2,controlYPos}, title="Start"; controlYPos += 25
			
			// Disabled in this mode
			Button nudgeROI win=IEDisplay#ROIPanel,pos={2,controlYPos}, disable=1
			Button addROI,win=IEDisplay#ROIPanel,pos={2,controlYPos},disable=1;
			SetVariable pctFillThreshold,win=IEDisplay#ROIPanel,disable=1
			
			IE_ClearMarquee()
			SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook
			break
		case "Draw":
			controlYPos = controlYPosStart
			
			SetVariable ROIname,win=IEDisplay#ROIPanel,pos={100,controlYPos},disable=0; controlYPos += 25
			Button addROI,win=IEDisplay#ROIPanel,pos={2, controlYPos},disable=0; controlYPos += 25
			PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,pos={2,controlYPos},disable=0; controlYPos += 25
			Button addGroup,win=IEDisplay#ROIPanel,pos={2,controlYPos}; controlYPos += 25
			Button confirmROI,win=IEDisplay#ROIPanel,pos={2,controlYPos},title="Start"; controlYPos += 25

			
			// Disabled in this mode
			SetVariable roiWidth,win=IEDisplay#ROIPanel,disable=1
			SetVariable roiHeight,win=IEDisplay#ROIPanel,disable=1
			Button nudgeROI win=IEDisplay#ROIPanel, disable =1
			SetVariable pctFillThreshold,win=IEDisplay#ROIPanel,disable=1
			
			IE_ClearMarquee()
			SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook
			break
		case "Grid":
			SetVariable roiWidth,win=IEDisplay#ROIPanel,disable=0
			SetVariable roiHeight,win=IEDisplay#ROIPanel,disable=0
			SetVariable ROIname,win=IEDisplay#ROIPanel,pos={100,75},disable=0
			Button nudgeROI win=IEDisplay#ROIPanel,pos={9,117}
			Button addROI,win=IEDisplay#ROIPanel,disable=1
			Button confirmROI,win=IEDisplay#ROIPanel,pos={9,140},title="Start"
			PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,pos={54,95},bodywidth=72,disable=0
			Button addGroup,win=IEDisplay#ROIPanel,pos={2,120}
			SetVariable pctFillThreshold,win=IEDisplay#ROIPanel,disable=0
			SetWindow IEDisplay hook(zoomScrollHook) = IE_ZoomScrollHook
			break
	endswitch
End

Function/WAVE IE_CreateROIMask(BaseImage,ROIFolder,ROIName,left,right,top,bottom)
	//Creates an ROI mask image from coordinates and a base image
	
	Wave/Z BaseImage //image on which the ROI mask is based
	String ROIFolder,ROIName //ROI folder and name for creating the wave
	Variable left,right,top,bottom //coordinates for the mask
	
	DFREF saveDF = GetDataFolderDFR()
	
	//ROI parent folder
	DFREF SIR = $(IE + ":ROIs")
	SetDataFolder SIR
	
	DFREF Folder = $ROIFolder
	
	If(!WaveExists(BaseImage))
		return $""
	EndIf
	
	Make/O/N=(DimSize(BaseImage,0),DimSize(BaseImage,1))/B/U Folder:$(ROIName + "_Mask")/Wave=ROIMask = 0
	CopyScales/P BaseImage,ROIMask	
	
	//Convert scaled values to pixel values	
	Variable LeftPt = ScaleToIndex(BaseImage,left,0)
	Variable RightPt = ScaleToIndex(BaseImage,right,0)
	Variable TopPt = ScaleToIndex(BaseImage,top,1)
	Variable BottomPt = ScaleToIndex(BaseImage,bottom,1)
	
	//Check validity of the ROI points, are they outside the image? 
	LeftPt = (LeftPt > DimSize(ROIMask,0) - 1) ? DimSize(ROIMask,0) - 1 : LeftPt
	LeftPt = (LeftPt < 0) ? 0 : LeftPt
	RightPt = (RightPt > DimSize(ROIMask,0) - 1) ? DimSize(ROIMask,0) - 1 : RightPt
	RightPt = (RightPt < 0) ? 0 : RightPt
	TopPt = (TopPt > DimSize(ROIMask,1) - 1) ? DimSize(ROIMask,1) - 1 : TopPt
	TopPt = (TopPt < 0) ? 0 : TopPt
	BottomPt = (BottomPt > DimSize(ROIMask,1) - 1) ? DimSize(ROIMask,1) - 1 : BottomPt
	BottomPt = (BottomPt < 0) ? 0 : BottomPt
	
	//Create the threshold variable
	Variable/G Folder:$(ROIName + "_Threshold")
	NVAR threshold = Folder:$(ROIName + "_Threshold")
	threshold = 0
		
	//Mask the ROI within the coordinates
	ROIMask[LeftPt,RightPt][TopPt,BottomPt] = 1	
	
	//Thresholding of the mask
	ROIMask[LeftPt,RightPt][TopPt,BottomPt] = (BaseImage[p][q] >= threshold) ? 1 : 0
	
	SetDataFolder saveDF
	
	return ROIMask
End

Function IE_AppendROIMask(graphStr,mask)
	String graphStr
	Wave mask
	
	If(!WaveExists(mask))
		return 0
	EndIf
	
	//Append the mask to the display and color it
	AppendImage/W=$graphStr/L/T mask
	
	String imageList = ImageNameList(graphStr,";")
	imageList = ListMatch(imageList,"*Mask",";")
	Variable nMasks = ItemsInList(imageList,";")
	
	Wave color = root:Packages:NeuroToolsPlus:CustomColors:Geo
	Duplicate/FREE color,colorRotate
	
	Rotate -round(0.25 * DimSize(color,0)),colorRotate
	 
	Variable delta = round(DimSize(colorRotate,0) / 15)
	Variable row = (nMasks - 1) * delta
	Variable modRow = mod(row,DimSize(colorRotate,0) - 1)
	
	Variable R = colorRotate[modRow][0]
	Variable G = colorRotate[modRow][1]
	Variable B = colorRotate[modRow][2]
	ModifyImage/W=$graphStr $NameOfWave(mask) explicit=1,eval={0,0,0,0,0},eval={1,R,G,B,32768},eval={255,-1,-1,-1}
End

//Append the ROIs in the list to the top image plot
Function IE_AppendROIsToImage(groupList,roiList)
	String groupList,roiList
	
	//If groupList or roiList are empty strings, just removes all ROIs
	
	DFREF NTSI = root:Packages:ImageExplorer
	NVAR numImages = NTSI:numImages
		
	String graphName = "IEDisplay"
	numImages = IE_GetNumImages(graphName,"image")
	
	//ROI color table wave
	Wave color = NTSI:ROI_ColorTable
	
	Variable i,j
	
	//Check it's IEDisplay
	DoWindow $graphName
	
	If(V_flag == 0)
		return 0
	EndIf
	
	//in case of errors in image number
	If(numImages == 0)
		numImages = 1
	EndIf
	
	For(i=0;i<numImages;i+=1)
	
		String subpanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		
		//First remove all ROI traces to clear the image graph
		String tracelist = TraceNameList("IEDisplay#" + subpanel + "#" + graph,";",1)
		For(j=ItemsInList(tracelist,";")-1;j>-1;j-=1) //step backwards
			RemoveFromGraph/W=IEDisplay#$subpanel#$graph/Z $StringFromList(j,tracelist,";")
		EndFor
		
		//First remove all ROI image masks to clear the image graph
		tracelist = ImageNameList("IEDisplay#" + subpanel + "#" + graph,";")
		For(j=ItemsInList(tracelist,";")-1;j>0;j-=1) //step backwards
			RemoveImage/Z/W=IEDisplay#$subpanel#$graph $StringFromList(j,tracelist,";")
		EndFor
		
		For(j=0;j<ItemsInList(roiList,";");j+=1)
			String roiName = StringFromList(j,roiList,";")
			String roiGroup = StringFromList(j,groupList,";")
		

			Wave roiX = $("root:Packages:ImageExplorer:ROIs:" + roiGroup + ":" + roiName + "_x")
			Wave roiY = $("root:Packages:ImageExplorer:ROIs:" + roiGroup + ":" + roiName + "_y")
			Wave/Z roiMask = $("root:Packages:ImageExplorer:ROIs:" + roiGroup + ":" + roiName + "_Mask")
										
			If(!WaveExists(roiX) || !WaveExists(roiY))
				continue
			EndIf
			AppendToGraph/W=IEDisplay#$subpanel#$graph/L/T roiY vs roiX
			
			NVAR displayROIMasks = NTSI:displayROIMasks
			
			If(WaveExists(roiMask) && displayROIMasks)
//				IE_AppendROIMask("IEDisplay#" + subpanel + "#" + graph,roiMask)
			EndIf
		EndFor	
	EndFor
	
	return 0
End

Function IE_RefreshROIGroupList()
	DFREF NTSI = $IE
	
	//Refresh the ROI group list
	Wave/T ROIGroupListWave = NTSI:ROIGroupListWave
	String/G NTSI:GroupList 
	SVAR GroupList = NTSI:GroupList
	GroupList = TextWaveToStringList(ROIGroupListWave,";")

End

//Creates an ROI at the point of the mouse click
Function IE_CreateROIFromClick(mh,mv,width,height,target,group,baseName)
	Variable mh,mv,width,height
	String target,group,baseName
	
	String imageName = StringFromList(0,ImageNameList(target,";"),";")
	String info = ImageInfo(target,imageName,0)
	String xAxis = StringByKey("XAXIS",info,":",";")
	String yAxis = StringByKey("YAXIS",info,":",";")
	
	Variable xCenter = AxisValFromPixel(target,xAxis,mh)
	Variable yCenter = AxisValFromPixel(target,yAxis,mv)
	
	Wave image = ImageNameToWaveRef(target,imageName)
	Variable left = xCenter - (width * 10^-6) * 0.5
	Variable top  = yCenter - (height * 10^-6) * 0.5
	Variable right = xCenter + (width * 10^-6) * 0.5
	Variable bottom  = yCenter + (height * 10^-6) * 0.5
	
	//Prevent ROI creation if the click was outside the image boundaries
	If(xCenter < DimOffset(image,0) || xCenter > IndexToScale(image,DimSize(image,0)-1,0))
		return 0
	ElseIf(yCenter < DimOffset(image,1) || yCenter > IndexToScale(image,DimSize(image,1)-1,1))
		return 0
	EndIf
	
	If(!strlen(baseName))
		baseName = "ROI"
	EndIf
	
	Wave/WAVE roiRefs = IE_CreateROI(left,top,right,bottom,group=group,baseName=baseName,autoname=1)
	
	AppendToGraph/W=$target/T=$xAxis/L=$yAxis roiRefs[1] vs roiRefs[0]
	
	DFREF NTSI = $IE
	
	NVAR displayROIMasks = NTSI:displayROIMasks
	
	If(!NVAR_Exists(displayROIMasks))
		Variable/G NTSI:displayROIMasks
		NVAR displayROIMasks = NTSI:displayROIMasks
		
		ControlInfo/W=SI displayROIMasks
		displayROIMasks = V_Value
	EndIf
	
	Variable LeftPt,RightPt,TopPt,BottomPt,LeftScale,RightScale,TopScale,BottomScale
	
	//Make the mask ROI image wave and the threshold variables for dendrite masking
	String ROIFolder = GetWavesDataFolder(roiRefs[0],0)
	String indexStr = RemoveEnding(ParseFilePath(0,NameOfWave(roiRefs[0]),"_",1,1),"_")
	String ROIName = baseName + "_" + indexStr
	
	//Create the ROI Mask
	Wave/Z ROIMask = IE_CreateROIMask(image,ROIFolder,ROIName,left,right,top,bottom)

	If(displayROIMasks && WaveExists(ROIMask))	
//		IE_AppendROIMask(target,ROIMask)
	EndIf
	
	//Update lists
	IE_UpdateImageBrowserLists()
	IE_RefreshROIGroupList()
End

//Creates a new ROI using marquee coordinates, returns a wave reference wave with the X and Y coordinate waves
Function/WAVE IE_CreateROI(left,top,right,bottom,[group,baseName,autoName])
	Variable left,top,right,bottom
	String baseName,group
	Variable autoName
	
	If(ParamIsDefault(autoName))
		autoName = 0
	EndIf
	
	If(ParamIsDefault(group))
		group = ""
	EndIf
	
	If(!DataFolderExists("root:Packages:ImageExplorer:ROIs"))
		NewDataFolder root:Packages:ImageExplorer:ROIs
	EndIf		
	
	String roiFolder = "root:Packages:ImageExplorer:ROIs:"
	
	DFREF saveDF = GetDataFolderDFR()
	
	//If no valid group, prompt user to create one
	If(!strlen(group) || !DataFolderExists(roiFolder + group))
		String newGroupName = ""
		Prompt newGroupName, "New group name:"
		DoPrompt "Add ROI Group", newGroupName
		If(V_flag) //user cancelled
			return $""
		EndIf
		newGroupName = CleanupName(newGroupName, 0)
		If(!strlen(newGroupName))
			return $""
		EndIf
		If(!DataFolderExists(roiFolder + newGroupName))
			NewDataFolder $(roiFolder + newGroupName)
		EndIf
		group = newGroupName
	EndIf
	
	//If automatic naming is indicated or we're creating a new group
	If(autoName)

		DFREF NTR = $(roiFolder + group)
		
		//Make numerically named ROIs
		SetDataFolder NTR
		
		Variable index = 0
		Do
			If(!WaveExists($(roiFolder + group + ":" + baseName + "_" + num2str(index) + "_x")) && !WaveExists($(roiFolder + group + ":" + baseName + num2str(index) + "_y")))
				baseName +=  "_" + num2str(index)
				break
			EndIf
			index += 1
			
			If(index == 1000)
				Abort "Couldn't find an available ROI name"
			EndIf
		While(1)
		
	Else
		DFREF NTR = $(roiFolder + group)
		
		If(!DataFolderRefStatus(NTR))
			//If the ROI group doesn't actually exist for some reason, create one with that name
			NewDataFolder $(roiFolder + group)
			DFREF NTR = $(roiFolder + group)
		EndIf
	
		//Make ROI coordinates X and Y waves
		ControlInfo/W=IEDisplay#ROIPanel ROIname
		baseName = S_Value
		
		If(!strlen(S_Value))
			Abort "Must enter a name for the ROI"
			return $""
		EndIf
	EndIf
		
	If(WaveExists($(roiFolder + group + ":" + basename + "_x")) || WaveExists($(roiFolder + group + ":" + basename + "_y")))
		DoAlert/T="Overwrite ROI?" 1,"ROI already exists. Overwrite?"
		If(V_flag == 2) //clicked no
			return $""
		EndIf
	EndIf
	
	
	Make/O/N=5 NTR:$(baseName + "_x") /Wave = roiX
	Make/O/N=5 NTR:$(baseName + "_y") /Wave = roiY

	//Fill the ROI wave with the coordinates to form a rectangle
	roiX[0] = left //top left
	roiY[0] = top //top left
	
	roiX[1] = left //bottom left
	roiY[1] = bottom //bottom left
	
	roiX[2] = right //bottom right
	roiY[2] = bottom //bottom right
	
	roiX[3] = right //top right
	roiY[3] = top //top right
	
	roiX[4] = left //top left
	roiY[4] = top //top left
	
	Make/FREE/WAVE roiRefs
	roiRefs[0] = roiX
	roiRefs[1] = roiY
	
	SetDataFolder saveDF
	
	//Set the ROI Group Selector to the current group.
	//This will allow you to click-create ROIs in a new group without having to choose the group that
	//was just created. 
	DFREF NTSI = $IE
	
	IE_UpdateImageBrowserLists()
	IE_RefreshROIGroupList()
	
	SVAR ROIGroupList = NTSI:GroupList
	index = WhichListItem(group,ROIGroupList,";")
	
	If(index != -1)
		PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,mode=index+1
		ControlUpdate/A
	EndIf
	
	return roiRefs
End

//Creates an ROI according to the drawn shape on the image
Function IE_CreateDrawnROI(drawROIX,drawROIY,target,group,baseName)
	Wave drawROIX,drawROIY
	String target,group,baseName
	
	If(!DataFolderExists("root:Packages:ImageExplorer:ROIs"))
		NewDataFolder root:Packages:ImageExplorer:ROIs
	EndIf
	
	String roiFolder = "root:Packages:ImageExplorer:ROIs:"

	
	DFREF saveDF = GetDataFolderDFR()
	
	//If no valid group, prompt user to create one
	If(!strlen(group) || !DataFolderExists(roiFolder + group))
		String newGroupName = ""
		Prompt newGroupName, "New group name:"
		DoPrompt "Add ROI Group", newGroupName
		If(V_flag) //user cancelled
			return 0
		EndIf
		newGroupName = CleanupName(newGroupName, 0)
		If(!strlen(newGroupName))
			return 0
		EndIf
		If(!DataFolderExists(roiFolder + newGroupName))
			NewDataFolder $(roiFolder + newGroupName)
		EndIf
		group = newGroupName
	EndIf
	
	
	DFREF NTR = $(roiFolder + group)
	
	//If the ROI group doesn't actually exist for some reason, create one with that name
	If(!DataFolderRefStatus(NTR))
		NewDataFolder $(roiFolder + group)
		DFREF NTR = $(roiFolder + group)
	EndIf
	
	//If no ROI name base was given
	If(!strlen(baseName))
		baseName = "ROI"
	EndIf
		
	If(WaveExists($(roiFolder + group + ":" + basename + "_x")) || WaveExists($(roiFolder + group + ":" + basename + "_y")))
		DoAlert/T="Overwrite ROI?" 1,"ROI already exists. Overwrite?"
		If(V_flag == 2) //clicked no
			return 0
		EndIf
	EndIf
	
	//Make the X and Y ROI waves
	Make/O/N=(DimSize(drawROIX,0)) NTR:$(baseName + "_x") /Wave = roiX
	Make/O/N=(DimSize(drawROIY,0)) NTR:$(baseName + "_y") /Wave = roiY
	
	roiX = drawROIX
	roiY = drawROIY
	
	//Replace the temporary draw ROI waves with the new ROI waves
	IE_AppendROIsToImage(group,baseName)
	
	SetDataFolder saveDF
	
	//Set the ROI Group Selector to the current group.
	//This will allow you to click-create ROIs in a new group without having to choose the group that
	//was just created. 
	DFREF NTSI = $IE
	
	IE_UpdateImageBrowserLists()
	IE_RefreshROIGroupList()
	
	SVAR ROIGroupList = NTSI:GroupList
	Variable index = WhichListItem(group,ROIGroupList,";")
	
	If(index != -1)
		PopUpMenu roiGroupSelect,win=IEDisplay#ROIPanel,mode=index+1
		ControlUpdate/A
	EndIf
End

//Returns a mask wave for the input scan
Function/WAVE IE_GetDendriticMask(theWave,[threshold])
	Wave theWave
	Variable threshold

	Variable rows,cols,frames,i,j
	
	rows = DimSize(theWave,0)
	cols = DimSize(theWave,1)
	frames = DimSize(theWave,2)

	//For 2D images, max projections, etc. 
	If(DimSize(theWave,2) == 0)
		Duplicate/O theWave,:tempWave
		Wave tempWave = :tempWave
		
		ImageStats/Q theWave
		
		//Min/max of the image
		Variable minVal = V_min
		Variable maxVal = V_max
		
		If(ParamIsDefault(threshold))
			threshold = minVal + (maxVal - minVal) * 0.03//1.25 is a mask threshold and can be changed
		EndIf
		
		Multithread tempWave = (theWave < threshold) ? 0 : 1
		
		Note/K tempWave,"Threshold=" + num2str(threshold)
		return tempWave
	Else
		//Get max projection image
		MatrixOP/S/O maxProj = sumBeams(theWave)
		Redimension/S maxProj
	EndIf
	
	Multithread maxProj /= DimSize(theWave,2)
	
	
	Make/FREE/N=(DimSize(theWave,0),DimSize(theWave,1)) varMap
	varMap = 0


	For(i=0;i<frames;i+=1)
		MultiThread varMap += (maxProj - theWave[p][q][i])^2
	EndFor
	
	MultiThread varMap /= (frames - 1)	
		
	ImageStats/Q varMap
		
	//Min/max of the image
	minVal = V_min
	maxVal = V_max
	
	//Simple value thresholding based on those values
	If(ParamIsDefault(threshold))
		threshold = minVal + (maxVal - minVal) * 0.03//1.25 is a mask threshold and can be changed
	EndIf
	
	Multithread varMap = (varMap < threshold) ? 0 : maxProj
	
	//Eliminate isolated points
	Make/FREE/N=(5,5) block
	block = 0

	For(i=0;i<rows;i+=1)	
		For(j=0;j<cols;j+=1)
			//skip zeros
			If(varMap[i][j] == 0)
				continue
			EndIf			
	
			//check for image edges
			If(i-2 < 0 || i+2 > rows-2 || j-2 < 0 || j+2 > cols-2)
				continue
			Else
				//Get data block surrounding point
				block = varMap[i-2 + p][j-2 + q]
				
				//Check for isolated point
				If(sum(block) < 3*varMap[i][j])
					varMap[i][j] = 0
				EndIf	
			EndIf
		
			block = 0
		EndFor
	EndFor
	
	//2D median filter 3x3
	MatrixFilter/N=3 median varMap
	
	//Create mask wave
	String maskName = NameOfWave(theWave) + "_mask"
	If(strlen(maskName) > 31)
		maskName = "Scan_mask"
	EndIf
	
	Make/O/N=(rows,cols) theMask
	MultiThread theMask = (varMap == 0) ? 0 : 1
	
	//Scaling
	SetScale/P x,DimOffset(theWave,0),DimDelta(theWave,0),theMask
	SetScale/P y,DimOffset(theWave,1),DimDelta(theWave,1),theMask
	
	return theMask
End

//Create an ROI grid across an image
Function IE_CreateROIGrid(w,h,threshold,target,group,baseName)
	Variable w,h,threshold
	String target,group,baseName
	
	DFREF NTSI = $IE
	
	//Get the image wave (time-varying scan, not max projection)
	SVAR SIDisplay_ImagePaths = NTSI:SIDisplay_ImagePaths
	Wave image = $StringFromList(0,SIDisplay_ImagePaths,";")
	
	If(!WaveExists(image))
		return 0
	EndIf

	String roiFolder = "root:Packages:ImageExplorer:ROIs:"
	
	//If no valid group, prompt user to create one
	If(!strlen(group) || !DataFolderExists(roiFolder + group))
		String newGroupName = ""
		Prompt newGroupName, "New group name:"
		DoPrompt "Add ROI Group", newGroupName
		If(V_flag) //user cancelled
			return 0
		EndIf
		newGroupName = CleanupName(newGroupName, 0)
		If(!strlen(newGroupName))
			return 0
		EndIf
		If(!DataFolderExists(roiFolder + newGroupName))
			NewDataFolder $(roiFolder + newGroupName)
		EndIf
		group = newGroupName
	EndIf
	
	//First get a variance map of the image, in order to create a dendritic mask
	Wave mask = IE_GetDendriticMask(image)
	Redimension/B/U mask
	CopyScales/P image,mask
	
	threshold /= 100
	
	//Fill the entire image with ROIs in square grid first, then prune them away
	Variable row=0,col=0,strideX=(w*1e-6)/DimDelta(mask,0),strideY=(h*1e-6)/DimDelta(mask,1),index=0
	Do
		col = 0
		If(row + strideX - 1 > DimSize(mask,0)-1)
			break
		EndIf
		Do
		
			If(col + strideY - 1 > DimSize(mask,1)-1)
				break
			EndIf
			
			Make/N=5/FREE roiX,roiY			
			
			//X ROI coordinates
			roiX[0] = IndexToScale(mask,row,0)
			roiX[1] = IndexToScale(mask,row,0)
			roiX[2] = IndexToScale(mask,row+strideX-1,0)
			roiX[3] = IndexToScale(mask,row+strideX-1,0)
			roiX[4] = IndexToScale(mask,row,0)
			
			//Y ROI coordinates
			roiY[0] = IndexToScale(mask,col,1)
			roiY[1] = IndexToScale(mask,col+strideY-1,1)
			roiY[2] = IndexToScale(mask,col+strideY-1,1)
			roiY[3] = IndexToScale(mask,col,1)
			roiY[4] = IndexToScale(mask,col,1)

			
			//Create mask for the ROI and compare it with the image mask to see if there is enough fluorescence to merit an ROI.
			Variable xSeed = IndexToScale(mask,row+3,0)
			Variable ySeed = IndexToScale(mask,col+3,1)
			
			ImageBoundaryToMask width=DimSize(mask,0),height=DimSize(mask,1),xwave=roiX,ywave=roiY,scalingwave=mask,seedX=xSeed,seedY=ySeed
			Wave ROIMask = M_ROIMask
			MatrixOP/FREE bit = bitAND(mask,ROIMask)
			
			//thresholding
			If(sum(bit) < threshold * (w * h))
				KillWaves/Z roiX,roiY
				col += strideY
				continue
			EndIf
			
			If(!strlen(baseName))
				baseName = "ROI"
			EndIf
			
			//Make new waves
			Make/N=5/O $(roiFolder + group + ":" + baseName + "_" + num2str(index) + "_y")/Wave=yROI
			Make/N=5/O $(roiFolder + group + ":" + baseName + "_" + num2str(index) + "_x")/Wave=xROI
			
			//put into the wave reference waves
			xROI = roiX
			yROI = roiY
			
			col += strideY
			index += 1
		While(1)
		
		row += strideX
	While(1)

End

//Determines the number of subwindows in the window
Function IE_GetNumImages(windowNameStr,baseNameStr)
	String windowNameStr,baseNameStr
	Variable i
	
	i = 0
	Do
		String fullWinName = windowNameStr + "#" + baseNameStr + num2str(i)
		GetWindow/Z $fullWinName wsize //keyword doesn't matter, but need to reserve V_flag for existence or not
		i += 1
	While(!V_flag)
	
	
	return i - 1
End

Function/WAVE IE_GetMaxProj(imageList[,noReplace])
	String imageList
	Variable noReplace
	
	If(ParamIsDefault(noReplace))
		noReplace = 0
	EndIf
	
	DFREF NTSI = $IE
	NVAR isMaxProj = NTSI:isMaxProj
	NVAR isInverted = NTSI:isInverted
	
	Variable i
	
	For(i=0;i<ItemsInList(imageList,";");i+=1)
		Wave theImage = $StringFromList(i,imageList,";")
		
		If(!WaveExists(theImage))
			continue
		EndIf
		
		String outName = "maxProj" + num2str(i)
		
		Variable numDims = WaveDims(theImage)
		
		If(numDims == 3)
			MatrixOP/O NTSI:$outName = sumBeams(theImage)
			Wave maxProj = NTSI:$outName
		
			Redimension/S maxProj //must be 32 bit float to be divided by frame number
			maxProj /= DimSize(theImage,2)
		ElseIf(numDims == 4)
			//max project along the fourth dimension
			Variable j, numLayers = DimSize(theImage,2)
			Make/FREE/N=(DimSize(theImage,0),DimSize(theImage,1),DimSize(theImage,3)) temp
			Make/O/N=(DimSize(theImage,0),DimSize(theImage,1),numLayers) NTSI:$outName/Wave=maxProj
			Redimension/S maxProj
			For(j=0;j<numLayers;j+=1)
				Multithread temp = theImage[p][q][j][r]
				MatrixOP/FREE theSum = sumbeams(temp)
				Multithread maxProj[][][j] = theSum[p][q][0] / DimSize(theImage,3)
			EndFor
		EndIf
		
		CopyScales/P theImage,maxProj
		
		If(!noReplace)
			String subpanel = "image" + num2str(i)
			String graph = "graph" + num2str(i)
			ReplaceWave/W=IEDisplay#$subpanel#$graph image=$NameOfWave(theImage),maxProj
	
			ModifyImage/Z/W=IEDisplay#$subpanel#$graph $NameOfWave(maxProj),ctab={*,*,$"",isInverted}
		EndIf
				
		//Put a copy in the image's home folder
		Duplicate/O maxProj,$(GetWavesDataFolder(theImage,2) + "_max")
	
	EndFor
	
	isMaxProj = 1 
	
	return maxProj
End

//Auto stretches the image to the given percent values on the histogram
Function IE_invertImage()

	DFREF NTSI = $IE
	NVAR numImages = NTSI:numImages
	
	NVAR isInverted = NTSI:isInverted
	
	DoWindow IEDisplay
		
	If(V_flag == 0)
		return 0
	EndIf
	
	//in case of errors in image number
	If(numImages == 0)
		numImages = 1
	EndIf
	
	isInverted = (isInverted) ? 0 : 1

	Variable i
	For(i=0;i<numImages;i+=1)
	
		String subpanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		
		String target = "IEDisplay#" + subpanel + "#" + graph
		String imageList = ImageNameList(target,";")
		
		Wave theImage = ImageNameToWaveRef(target,StringFromList(0,imageList,";"))
		
		String info = ImageInfo(target,NameOfWave(theImage),0)
		info = StringByKey("RECREATION:ctab",info,"=",";")
		info = ReplaceString("{",info,"")
		info = ReplaceString("}",info,"")
		
		Variable tempMin = str2num(StringFromList(0,info,","))
		Variable tempMax = str2num(StringFromList(1,info,","))
		Variable minVal = min(tempMin,tempMax)
		Variable maxVal = max(tempMin,tempMax)
		
		ModifyImage/W=$target $NameOfWave(theImage) ctab= {minVal,maxVal,Grays,isInverted}
	EndFor

	Button invert win=IEDisplay#control,fColor=(0,0xa000 * isInverted,0)
	
	//switch the sliders
	ControlInfo/W=IEDisplay#control darkValueSlider
	Variable darkVal = V_Value
	ControlInfo/W=IEDisplay#control brightValueSlider
	Variable brightVal = V_Value
	
	Slider darkValueSlider win=IEDisplay#control,value=brightVal
	Slider brightValueSlider win=IEDisplay#control,value=darkVal
End

//Auto stretches the image to the given percent values on the histogram
Function IE_AutoStretch(pct)
	Variable pct

	DFREF NTSI = root:Packages:ImageExplorer
	NVAR numImages = NTSI:numImages
	NVAR imagePlane = NTSI:imagePlane
	NVAR isMaxProj = NTSI:isMaxProj
	NVAR isInverted = NTSI:isInverted
	
	
	numImages = IE_GetNumImages("IEDisplay","image")
	
	//ROI color table wave
	Wave color = NTSI:ROI_ColorTable
	
	pct = 0.5 * (100-pct)/100
	
	Variable i,j
	
	//Check it's IEDisplay or another image plot
	DoWindow IEDisplay
	
	If(V_flag == 0)
		return 0
	EndIf
	
	//in case of errors in image number
	If(numImages == 0)
		numImages = 1
	EndIf
	
	Make/FREE/N=(numImages)/WAVE imageRefs
	
	For(i=0;i<numImages;i+=1)
	
		String subpanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		
		String imageList = ImageNameList("IEDisplay#" + subpanel + "#" + graph,";")
		
		//assumes 1 image per graph slot in IEDisplay
		imageRefs[i] = ImageNameToWaveRef("IEDisplay#" + subpanel + "#" + graph,StringFromList(j,StringFromList(0,imageList,";"),";"))
	
	EndFor

	
	For(i=0;i<DimSize(imageRefs,0);i+=1)
		Wave theImage = imageRefs[i]
		
		If(!isMaxProj)
			Wave maxImage = IE_GetMaxProj(GetWavesDataFolder(theImage,2),noReplace=1)			
			isMaxProj = 0
		EndIf
		
		subpanel = "image" + num2str(i)
		graph = "graph" + num2str(i)
		
		If(!isMaxProj)
			ImageHistogram/I maxImage
		Else
			If(DimSize(theImage,2) > 1)	
				ImageHistogram/I/P=(imagePlane) theImage
			Else
				ImageHistogram/I theImage
			EndIf	
		EndIf
				
		Wave hist = W_ImageHist
		Integrate hist/D=cumHist
		Variable minLevel=cumHist[0]
		Variable maxLevel=cumHist[dimsize(cumHist,0)-1]
		Variable range=maxLevel-minLevel
		FindLevel/Q cumHist, minLevel + range*pct
		Variable lowerThreshold=V_levelX 
		FindLevel/Q cumHist,minLevel+range*(1-pct)
		Variable upperThreshold=V_levelX
	
	
		Variable sliderLeft,sliderRight

		ModifyImage/W=IEDisplay#$subpanel#$graph $NameOfWave(theImage) ctab= {lowerThreshold,upperThreshold,Grays,isInverted}
		
		//Set the slider positions
		ImageStats/Q theImage
		range = V_max - V_min
		
		sliderLeft = round(100 * (lowerThreshold - V_min)/range)
		sliderRight = round(100 * (upperThreshold - V_min)/range)
		
		If(isInverted)
			Slider darkValueSlider win=IEDisplay#control,value=sliderRight
			Slider brightValueSlider win=IEDisplay#control,value=sliderLeft
		Else
			Slider darkValueSlider win=IEDisplay#control,value=sliderLeft
			Slider brightValueSlider win=IEDisplay#control,value=sliderRight
		EndIf
	EndFor
//
	KillWaves hist,cumHist
End

//Dynamic ROI
Function IE_StartDynamicROI(theImage)
	Wave theImage
	
	DFREF NTSI = $IE
	NVAR scaleCumulative = NTSI:scaleCumulative
	
	//Make a new display window
	GetWindow/Z dynamicROI wsize
	Variable winExists = V_flag
	KillWindow/Z dynamicROI
	
	If(!winExists)
		//window exists
		Display/N=dynamicROI/W=(V_left,V_top,V_right,V_bottom)/K=2 as "Dynamic ROI"
	Else
		//window doesn't exists
		GetWindow/Z IEDisplay wsize
		If(V_top < 0)
			V_top = 0
		EndIf
		Display/N=dynamicROI/W=(V_right,V_top,V_right+300,V_top+200)/K=2 as "Dynamic ROI"
	EndIf
	
	ControlBar/T/W=dynamicROI 20
	Checkbox CumulativeScale,win=dynamicROI,pos={5,0},size={100,20},side=1,title="Cumulative Scaling ",value=scaleCumulative,proc=IE_CheckBoxProc 
	
	Variable frames = DimSize(theImage,2)
	Variable delta = DimDelta(theImage,2)
	Variable offset = DimOffset(theImage,2)
	
	Make/O/N=(frames) NTSI:dynamicROI/Wave=dROI
	
	SetScale/P x,offset,delta,"s",dROI
	
	//Size in microns	
	NVAR size = NTSI:dynamicROI_Size
	
	Variable xSize = round((size * 1e-6) / DimDelta(theImage,0))
	Variable ySize = round((size * 1e-6) / DimDelta(theImage,1))
	Make/O/N=(xSize,ySize,frames) NTSI:block
	
	Variable/G NTSI:isDynamicROI
	NVAR isDynamicROI = NTSI:isDynamicROI
	isDynamicROI = 1
	
	Variable/G NTSI:dynamicROI_MaxVal
	NVAR maxVal = NTSI:dynamicROI_MaxVal
	Variable/G NTSI:dynamicROI_MinVal
	NVAR minVal = NTSI:dynamicROI_MinVal
	maxVal = -5000
	minVal = 5000
	
	String/G NTSI:dynamicROI_Image
	SVAR dynamicROI_Image = NTSI:dynamicROI_Image
	dynamicROI_Image = GetWavesDataFolder(theImage,2)
	
	AppendToGraph/W=dynamicROI dROI
	SetAxis/W=dynamicROI left,0,maxVal
End

//Gets the middle point of an ROI defined by an x wave and a y wave
//Returns the X/Y coordiate waves as a wave reference wave
Function/Wave IE_GetCenter([group])
	String group
	
	Variable i
	
	If(ParamIsDefault(group))
		//Selected ROIs and their group assignments
		String roiList = IE_SelectedROIs()
		String roiGroupList = IE_SelectedROIs(groups=1)
	Else
		Wave/T roiListWave = IE_GetROIs(group)
		roiList = TextWaveToStringList(roiListWave,";")
		roiGroupList = ""
		For(i=0;i<ItemsInList(roiList,";");i+=1)
			roiGroupList += group + ";"
		EndFor
	EndIf
	
	If(!strlen(roiList))
		return $""
	EndIf
	
	DFREF NTR = root:Packages:ImageExplorer:ROIs:
	String path = "root:Packages:ImageExplorer:ROIs:"
	
	If(ParamIsDefault(group))
		Make/O/N=(ItemsInList(roiList,";")) NTR:ROIx,NTR:ROIy
		Wave ROIx = NTR:ROIx
		Wave ROIy = NTR:ROIy
	Else
		Make/O/N=(ItemsInList(roiList,";")) NTR:$(group + "_ROIx"),NTR:$(group + "_ROIy")
		Wave ROIx = NTR:$(group + "_ROIx")
		Wave ROIy = NTR:$(group + "_ROIy")
	EndIf

	For(i=0;i<ItemsInList(roiList,";");i+=1)
		DFREF NTR = $(path + StringFromList(i,roiGroupList,";"))
		
		String roi = StringFromList(i,roiList,";")
		Wave yROI = NTR:$(roi + "_y")
		Wave xROI = NTR:$(roi + "_x")
		
		If(DimSize(xROI,0) == 5) //square ROI
			ROIx[i] = 0.5*(xROI[0] + xROI[2])
			ROIy[i] = 0.5*(yROI[0] + yROI[2])
		Else
			ROIx[i] = median(xROI)
			ROIy[i] = median(yROI)
		EndIf

	EndFor
	
	Make/N=2/FREE/WAVE refs
	
	SetDimLabel 0,0,X,refs
	SetDimLabel 0,1,Y,refs
	
	refs[0] = ROIx
	refs[1] = ROIy
	
	return refs

End

//Start the background task for playing image frames
Function IE_StartFramePlay()
	NVAR isPlaying = root:Packages:ImageExplorer:isPlaying
	NVAR numTicks = root:Packages:ImageExplorer:numTicks //refresh speed
	
	//Make sure to remove max projection images with actual images
	DFREF NTSI = $IE
	NVAR isMaxProj = NTSI:isMaxProj
	If(isMaxProj)
		IE_FlipMaxProj()
		isMaxProj = 0
	EndIf
	
	CtrlNamedBackground play, period=numTicks, proc=IE_PlayFramesBackroundTask
	CtrlNamedBackground play, start
	isPlaying = 1
	
	//change button title
	Button playFrames win=IEDisplay#control,title="Pause"
	
End

//Starts and stops the frame play background task
Function IE_HandlePlayFrames(ba) : ButtonControl
	STRUCT WMButtonAction &ba
	DFREF NTSI = $IE
	
	switch( ba.eventCode )
		case 2: // mouse up
			// click code here
			strswitch(ba.ctrlName)
				case "playFrames":
					NVAR numImages = NTSI:numImages
					NVAR imagePlane = NTSI:imagePlane
					
					
					NVAR isPlaying = NTSI:isPlaying
					NVAR deltaPlane = NTSI:deltaPlane
					
					SVAR imageList = NTSI:SIDisplay_ImageNameList 
					
					//Get the current image plane 
					Variable i
					String name = StringFromList(0,imageList,";")
					String graphRef = "IEDisplay#image0#graph0"
					imagePlane = str2num(StringByKey("plane",ImageInfo(graphRef,name,0),"=",";"))
	
					If(isPlaying)
						IE_StopFramePlay()
					Else
						IE_StartFramePlay()
					EndIf
					break
				case "slowFrames":
					NVAR isPlaying = NTSI:isPlaying
					NVAR deltaPlane = NTSI:deltaPlane
					NVAR numTicks = NTSI:numTicks
					If(isPlaying)
						//first reduce the frames
						deltaPlane -= 2
						
						//once that is minimized, start slowing the refresh
						If(deltaPlane <= 0)
							deltaPlane = 1
							numTicks += 2
							CtrlNamedBackground play, period=numTicks, proc=IE_PlayFramesBackroundTask
						EndIf
					EndIf
					break
				case "speedFrames":
					NVAR isPlaying = NTSI:isPlaying
					NVAR deltaPlane = NTSI:deltaPlane
					NVAR numTicks = NTSI:numTicks
					If(isPlaying)
						//first increase the frame refresh rate
						numTicks -= 2
						
						//once that is maxed out, start skipping frames
						If(numTicks <= 0 )
							numTicks = 1
							deltaPlane += 1
						EndIf
						
						CtrlNamedBackground play, period=numTicks, proc=IE_PlayFramesBackroundTask 
					EndIf
					break
				case "stepFrame":
					NVAR isPlaying = NTSI:isPlaying
					
					//first pause the movie
					If(isPlaying)
						IE_StopFramePlay()
						Button playFrames win=IEDisplay#control,title="Play"
					EndIf
					
					//step to the next frame
					IE_StepFrame()
					
					break
			endswitch
			break
		case -1: // control being killed
			break
	endswitch

	return 0
End

//Handle ROI selection right clicks
Function IE_HandleSelectionRightClick(selection,ROIListWave,ROISelWave)
	String selection
	Wave/T ROIListWave
	Wave ROISelWave
	
	Variable i
	//Get the selected ROIs
	String roiNameList="",roiGroupList="",roiName="",roiGroup="",selectedRowList=""
	For(i=0;i<DimSize(ROISelWave,0);i+=1)
		If(ROISelWave[i] > 0)
			roiNameList += ROIListWave[i][0][0] + ";"
			roiGroupList += ROIListWave[i][0][1] + ";"
			selectedRowList += num2str(i) + ";"
		EndIf
	EndFor
	
	Variable numROIs = ItemsInList(roiNameList,";")
	For(i=0;i<numROIs;i+=1)
		roiGroup = StringFromList(i,roiGroupList,";")
		roiName = StringFromList(i,roiNameList,";")
		Variable row = str2num(StringFromList(i,selectedRowList,";"))
		
		//Set the correct path to the ROI folder
		DFREF NTR = root:Packages:ImageExplorer:ROIs:$roiGroup
		String baseROIPath = "root:Packages:ImageExplorer:ROIs:"
	
		
		//Handle the right click selection
		strswitch(selection)
			case "Rename":
				ROISelWave[row] = 2 //set to editable
				break
			case "Delete":

				Wave roiX = NTR:$(roiName + "_x")
				Wave roiY = NTR:$(roiName + "_y")
				Wave/Z roiMask = NTR:$(roiName + "_Mask")
				NVAR/Z roiThreshold = NTR:$(roiName + "_Threshold")
				
				IE_RemoveFromSIDisplay(roiY) //Remove from the SIDisplay
				ReallyKillWaves(roiY) //Remove from any other plot and kill
				ReallyKillWaves(roiX)
				
				If(WaveExists(roiMask))
					IE_RemoveFromSIDisplay(roIMask) //Remove from the SIDisplay
					ReallyKillWaves(roiMask)
				EndIf
				
				If(NVAR_Exists(roiThreshold))
					KillVariables/Z roiThreshold
				EndIf
				
				IE_UpdateImageBrowserLists()
				break
			case "GoTo":
				//Browse to the selected wave

				Wave roiX = NTR:$(roiName + "_x")
				ModifyBrowser collapseAll//close all folders first
				CreateBrowser //activates the data browser focus
				ModifyBrowser setDataFolder="root:Packages:ImageExplorer:ROIs:" + roiGroup		
				ModifyBrowser clearSelection,selectList=GetWavesDataFolder(roiX,2) //selects waves
				break
			default:
				//Move To option was selected if it's default
				If(!strlen(selection))
					return 0
				EndIf
				
				//First pass
				If(i == 0)
					String moveToROI = selection
				EndIf
				
				Wave roiX = NTR:$(roiName + "_x")
				Wave roiY = NTR:$(roiName + "_y")
				
				If(!cmpstr(moveToROI,"New Group"))
					If(i == 0)
						SetDataFolder $baseROIPath
						moveToROI = UniqueName("Group",11,0)
						NewDataFolder $(baseROIPath + moveToROI)
					EndIf
				EndIf
				
				MoveWave roiX,$(baseROIPath + moveToROI + ":")
				MoveWave roiY,$(baseROIPath + moveToROI + ":")
				
				//Kills the ROI group if its now empty
				If(!CountObjectsDFR(NTR,1))
					KillDataFolder/Z $(baseROIpath + roiGroup)
				EndIf
				
				IE_UpdateImageBrowserLists()
				break
		endswitch
	EndFor
	
End

//Adds crosshairs for center of image and scaled center 
Function IE_AddCrossHairs()
	
	DFREF NTSI = $IE
	NVAR numImages = NTSI:numImages
	
	numImages = IE_GetNumImages("IEDisplay","image")
	
	Variable i
	
	Variable/G NTSI:crosshairsActive
	NVAR crosshairsActive = NTSI:crosshairsActive
	
	For(i=0;i<numImages;i+=1)		
		String subpanel = "image" + num2str(i)
		String graph = "graph" + num2str(i)
		
		//Kill the crosshairs; do a few times to ensure deletion - kind of finicky
		DrawAction/L=ProgFront/W=IEDisplay#$subpanel#$graph delete
		
		If(crosshairsActive == 0)
			
			//New draw group for crosshairs
			SetDrawEnv/W=IEDisplay#$subpanel#$graph gstart,gname=$("crossHairGroup" + num2str(i))
			SetDrawLayer/W=IEDisplay#$subpanel#$graph ProgFront
			
			//Draw vertical line through middle
			SetDrawEnv/W=IEDisplay#$subpanel#$graph linefgc=(0xffff,0xffff,0xffff),linethick=0.5,xcoord=rel,ycoord=rel
			DrawLine/W=IEDisplay#$subpanel#$graph 0.5,0,0.5,1
			
			//Draw horizontal line through middle
			SetDrawEnv/W=IEDisplay#$subpanel#$graph linefgc=(0xffff,0xffff,0xffff),linethick=0.5,xcoord=rel,ycoord=rel
			DrawLine/W=IEDisplay#$subpanel#$graph 0,0.5,1,0.5
			
			//Draw vertical line through X = 0
			SetDrawEnv/W=IEDisplay#$subpanel#$graph linefgc=(0,0xffff,0),linethick=0.5,xcoord=top,ycoord=rel
			DrawLine/W=IEDisplay#$subpanel#$graph 0,0,0,1
			
			//Draw horizontal line through Y = 0
			SetDrawEnv/W=IEDisplay#$subpanel#$graph linefgc=(0,0xffff,0),linethick=0.5,xcoord=rel,ycoord=left
			DrawLine/W=IEDisplay#$subpanel#$graph 0,0,1,0
			
			SetDrawEnv/W=IEDisplay#$subpanel#$graph gstop
			
			//Set back to user front
			SetDrawLayer/W=IEDisplay#$subpanel#$graph UserFront
		EndIf
	EndFor
	
	crosshairsActive = (crosshairsActive) ? 0 : 1
End

// Analysis functions
Function IE_ExtractROIs()
	String roiList = IE_SelectedROIs()
	String roiGroupList = IE_SelectedROIs(groups = 1)
	String imageList = IE_SelectedScanFields(fullpath = 1)
	
	//Make the ROI analysis folder if it doesn't already exist
	If(!DataFolderExists("root:Analysis"))
		NewDataFolder root:Analysis
	EndIf
	SetDataFolder root:Analysis	
	
	Variable nROIs = ItemsInList(roiList,";")
	
	Variable i,j
	Variable nImages = ItemsInList(imageList, ";")
	Variable useROIMask = 0
	For(i=0;i<nImages;i+=1)
		Variable ref = StartMSTimer
		
		Wave theScanTemp = $StringFromList(i,imageList,";") //signal fluorescence
		
		MatrixOP/FREE theScan = replaceNans(theScanTemp,0)
		CopyScales/P theScanTemp,theScan
		
		For(j=0;j<nROIs;j+=1)
			String theROI = StringFromList(j,ROIList,";")
			String roiGroup = StringFromList(j,ROIGroupList,";")
			
			//Make the ROI Group Analysis folder
			If(!DataFolderExists("root:Analysis:" + roiGroup))
				NewDataFolder $("root:Analysis:" + roiGroup)
			EndIf
			
			//Make the ROI analysis subfolder
			String ROIFolder = "root:Analysis:" + roiGroup + ":" + theROI
			
			If(!DataFolderExists(ROIFolder))
				NewDataFolder $ROIFolder
			EndIf
			
			DFREF RF = $"root:Packages:ImageExplorer:ROIs:" + roiGroup
			
			Wave/Z roiX = RF:$(theROI + "_x")
			Wave/Z roiY = RF:$(theROI + "_y")
			
			if (!WaveExists(roiX) || !WaveExists(roiY))
				print "Couldn't find ROI: " + theROI
				continue;
			endif
			
			If(UseROIMask)
				Wave/Z ROIMask =  RF:$(theROI + "_Mask")
				
				If(!WaveExists(roiMask))
					UseROIMask = 0
				EndIf
			EndIf
			
			SetDataFolder $ROIFolder
			
			//Seed values for filling out the ROI mask if we aren't using the dendritic mask wave
			If(!UseROIMask)
	
				Variable xSeed,ySeed		
				xSeed = (WaveMin(roiX) + WaveMax(roiX)) / 2
				ySeed = (WaveMin(roiY) + WaveMax(roiY)) / 2
			
				//ROI mask wave	
					
				ImageBoundaryToMask ywave=roiY,xwave=roiX,width=(DimSize(theScan,0)),height=(DimSize(theScan,1)),scalingwave=theScan,seedx=xSeed,seedy=ySeed			
		
				Wave/Z ROIMask = $(ROIFolder + ":M_ROIMask")	
			
				//Did the ROI mask actually get created?
				If(!WaveExists(ROIMask))
					DoAlert 0, "Couldn't find the ROI mask wave for: " + NameOfWave(theScan)
					continue
				EndIf
			EndIf
			
			//Make the raw ROI waves for signal and background
			Variable numFrames = DimSize(theScanTemp,2)
			Make/O/FREE/N=(numFrames) ROI_Signal
			
			//Set all the scales of the ROI waves
			SetScale/P x,DimOffset(theScanTemp,2),DimDelta(theScanTemp,2),"s",ROI_Signal	
		
			//Extract the signal and background fluorescence from the ROIs
			MatrixOP/FREE avgBeam = sum(theScan * ROIMask) / sum(ROIMask)	
			ROI_Signal = avgBeam[0][0][p]
			
			String outName = NameOfWave(theScanTemp) + "_" + theROI + "_F"
			
			Make/O/N=(numFrames) $outName
			Wave F = $outName
			
			//Set all the scales of the ROI waves
			SetScale/P x,DimOffset(theScanTemp,2),DimDelta(theScanTemp,2),"s",F
			
			F = ROI_Signal
			
			//Set the wave note with the original scan name
			Note F,"Scan: " + GetWavesDataFolder(theScan,2)
		EndFor
		
	
	EndFor
End

